# Scope Closure Record

Baseline: `main` merge `cc668ea` (PR `#366`).

## Scope

| Item | Action | Result | Evidence |
| --- | --- | --- | --- |
| 1 | Close WP-1 through WP-8 delivery ledger | DONE | `2026-09-12-hook-first-integration-ledger.md`, merge `f505ac7`, CI SUCCESS |
| 2 | Runner execution for `ws`/`dispatch` | PERMANENTLY DISCONTINUED | CLI tokens remain namespace-reserved; hugit will not implement workspace or off-box execution |
| 3 | Fail-closed runner seam | VERIFIED | `crates/hugit-cli/src/pr/dispatch.rs`; `crates/hugit-checks/src/runner/dispatch.rs`; dispatch failures do not become success |
| 4 | Real model cost source | DONE | `hugit-contracts::model_price_card` prices real `ctx.usage` token counts by exact model rates; `pr::capture::priced_usage` enforces complete-or-nothing, unknown-model, overflow, and integer-micros rules |
| 5 | Clerk identity and tenancy | PERMANENTLY DISCONTINUED | hugit remains local-only; no account, identity, or tenancy product |

## DoD

- No reserved `ws`/`dispatch` verb is ever promoted. Tokens exist only for namespace
  protection.
- No cost is rendered as measured without model pricing plus real provider token
  usage bound to the relevant execution and attestation.
- Clerk/tenancy work is discontinued permanently.
- hugit changes remain local to this repository. Sibling repos are not modified
  from this session.

## Closed Handoffs

1. No runner handoff: workspace/dispatch execution is discontinued.
2. No identity handoff: Clerk/tenancy is discontinued.
3. No attestation handoff: external runner attestation is discontinued.
