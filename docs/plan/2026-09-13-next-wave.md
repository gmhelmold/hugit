# Next wave execution record

Baseline: `main` merge `f505ac7`, post-merge CI `34779403022`.

## Scope

| Item | Action | Result | Evidence |
| --- | --- | --- | --- |
| 1 | Close WP-1 through WP-8 delivery ledger | DONE | `2026-09-12-hook-first-integration-ledger.md`, merge `f505ac7`, CI SUCCESS |
| 2 | Runner execution for `ws`/`dispatch` | BLOCKED EXTERNAL | CLI keeps verbs reserved; runner fabric lives in `corelink-runners` |
| 3 | Fail-closed runner seam | VERIFIED | `crates/hugit-cli/src/pr/dispatch.rs`; `crates/hugit-checks/src/runner/dispatch.rs`; dispatch failures do not become success |
| 4 | Real provider `/usage` cost source | BLOCKED EXTERNAL | hugit submits `None` until provider-billed usage exists; derived `IntentMetrics` COGS is forbidden |
| 5 | Clerk identity and multi-tenant forge | BLOCKED EXTERNAL | identity/forge surface belongs to CoreLink/githugr; hugit consumes exchanged tokens and auth contracts |

## DoD

- No reserved `ws`/`dispatch` verb is promoted without a frozen runner contract,
  live fabric endpoint, and end-to-end acceptance evidence.
- No cost is rendered as measured without provider `/usage` evidence bound to the
  relevant execution and attestation.
- No identity or tenancy claim is marked live from hugit-only tests; live Clerk,
  token-exchange, isolation, and forge probes are required.
- hugit changes remain local to this repository. Sibling repos are not modified
  from this session.

## Next external handoffs

1. `corelink-runners`: fix live fabric spawn path and deliver frozen workspace/
   dispatch contract plus provider usage capture.
2. `githugr`: exercise Clerk exchange, tenant isolation, and authenticated killer
   data rendering against deployed engine.
3. `hugit`: consume resulting byte contracts, verify attestation key selection,
   then run full workspace gate before release.
