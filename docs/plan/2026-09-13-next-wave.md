# Next wave execution record

Baseline: `main` merge `f505ac7`, post-merge CI `34779403022`.

## Scope

| Item | Action | Result | Evidence |
| --- | --- | --- | --- |
| 1 | Close WP-1 through WP-8 delivery ledger | DONE | `2026-09-12-hook-first-integration-ledger.md`, merge `f505ac7`, CI SUCCESS |
| 2 | Runner execution for `ws`/`dispatch` | DEFERRED PRODUCT SEAM | CLI keeps verbs reserved; execution engine is a separate runner product, with no CoreLink dependency |
| 3 | Fail-closed runner seam | VERIFIED | `crates/hugit-cli/src/pr/dispatch.rs`; `crates/hugit-checks/src/runner/dispatch.rs`; dispatch failures do not become success |
| 4 | Real model cost source | OPEN LOCAL IMPLEMENTATION | Each model carries pricing; combine model input/output rates with provider-reported token usage. Never use derived `IntentMetrics` COGS |
| 5 | Clerk identity and tenancy | DEFERRED | Clerk is an open-source plugin/integration, not a CoreLink dependency. Forge-layer tenancy is deferred; no cross-project work |

## DoD

- No reserved `ws`/`dispatch` verb is promoted without a frozen runner contract,
  live fabric endpoint, and end-to-end acceptance evidence.
- No cost is rendered as measured without model pricing plus real provider token
  usage bound to the relevant execution and attestation.
- Clerk/tenancy work is deferred; when resumed, integration must remain local to
  hugit and must not introduce a CoreLink dependency.
- hugit changes remain local to this repository. Sibling repos are not modified
  from this session.

## Next handoffs

1. Runner product: deliver workspace/dispatch execution contract and live runner
   acceptance. No CoreLink coupling.
2. Model/provider integration: expose token usage and model pricing locally,
   then bind cost to attestation.
3. Hugit: verify attestation key selection, then run full workspace gate before
   release. Clerk/tenancy remains deferred.
