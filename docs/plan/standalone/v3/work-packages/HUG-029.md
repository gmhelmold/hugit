# HUG-029 — JUnit com procedência por execução e agregação completa

**Estado:** planned; **owner:** F; **fase:** C; **prioridade:** first_slice.

**Dependências:** HUG-017, HUG-020, HUG-027

**Congelamento de runtime requerido:** true

**Revisões:** AR-10, AR-15

**Contratos vinculados:**
- P00@3.0 — sha256 `5d737267087e24bbcca9548488c719f4cca46fa3435a0ccf31b7b98696fde5cc`
- P06@3.0 — sha256 `2e6d36db28e0cd10d7ec732507b96a8428c4c6dcc6ad7e0c0a9b4cdb5cf27fc9`
- P08@3.0 — sha256 `0d6a0d18c9b72c52ff3eef81f0b156b20e753a501bbd95027a4da8ba2b0c46e2`

## Prontidão

Todos os predecessores possuem evidência integrada e interfaces versionadas compatíveis.

Owner F aceita write-set exato e budget B-PARSER; HUG-001 distingue arquivos existentes dos novos.

Os casos P/N/Q/R e os oráculos estão especificados antes do dispatch; fixtures e testes executáveis são entregues pelo próprio pacote ou predecessor. Documentação/descoberta usa oráculos de artefato, não um falso bug de runtime.

Qualquer dado de fonte ainda não observado bloqueia o consumidor; descobrir no pacote predecessor, sem inventar payload.

Freeze runtime de HUG-059 já integrado é obrigatório.

## Outputs e ownership

Caminhos propostos: HUG-001 precisa vincular sua presença/criação ao checkout real antes da implementação. Evidências B/C ficam fora do sujeito A.

- `crates/hugit-cli/src/reports/junit.rs`
- `conformance/junit-dialects.json`
- `crates/hugit-cli/tests/junit_provenance.rs`

**Leituras:** `novo módulo reports/junit`, `crates/hugit-contracts/`

**Recursos exclusivos:** path:crates/hugit-cli/src/reports/junit.rs, path:conformance/junit-dialects.json, path:crates/hugit-cli/tests/junit_provenance.rs

## Interfaces e erros

```json
{
  "interfaces": [
    {
      "protocol": "P00",
      "interfaces": [
        "validate_plan(plan)->validation_report",
        "dispatch(wp,source_manifest,dependency_evidence)->admission",
        "qualify(profile,candidate,evidence)->readiness"
      ]
    },
    {
      "protocol": "P06",
      "interfaces": [
        "classify(channel,content_type,policy)->class",
        "transform(content,policy_version)->derivation",
        "grant_delivery_and_publish(selection,expected_epoch)->delivered_or_ambiguous",
        "revoke_under_delivery_gate(subject)->ACK_or_PENDING",
        "cold_restore_policy(bundle,known_anchor,new_local_decision)->freshness_and_permissions"
      ]
    },
    {
      "protocol": "P08",
      "interfaces": [
        "collect_report(invocation,producer_witness)->report",
        "aggregate(shard_manifest,attempts)->suite",
        "read_range(cursor,generation,lease)->page_or_CURSOR_INVALIDATED",
        "restore(bundle,mode,authority_anchor)->installation"
      ]
    }
  ],
  "errors": [
    {
      "protocol": "P00",
      "codes": [
        "PLAN_INVALID",
        "DEPENDENCY_UNPROVEN",
        "CONTRACT_NOT_FROZEN",
        "SOURCE_DRIFT",
        "PROFILE_INCOMPLETE",
        "PROTOCOL_DIGEST_MISMATCH",
        "ASSERTION_UNCOVERED",
        "NORMATIVE_VIEW_DRIFT",
        "TEST_OWNER_MISMATCH"
      ]
    },
    {
      "protocol": "P06",
      "codes": [
        "RAW_NOT_AUTHORIZED",
        "KEY_UNAVAILABLE",
        "POLICY_CHANGED",
        "CONTENT_OMITTED",
        "REVOCATION_PENDING_REBUILD",
        "UNSAFE_OPAQUE",
        "REVOKE_PENDING",
        "DELIVERY_AMBIGUOUS",
        "POLICY_FRESHNESS_UNKNOWN"
      ]
    },
    {
      "protocol": "P08",
      "codes": [
        "REPORT_STALE",
        "SHARD_MISSING",
        "REPORT_INVALID",
        "CURSOR_EXPIRED",
        "RESTORE_TARGET_EXISTS",
        "UNSAFE_PATH",
        "BUNDLE_INCOMPLETE",
        "CURSOR_INVALIDATED",
        "INDEX_NOT_READY"
      ]
    }
  ]
}
```

## Unidades verificáveis

1. **HUG-029.S1** — Aceitar dialetos enumerados com version/parser; output dir exclusivo por invocation_id e producer witness quando integrado.
2. **HUG-029.S2** — Arquivo externo sem proveniência é imported/unverified_binding; mtime e XML válido não provam geração nesta execução.
3. **HUG-029.S3** — Test case, attempt e shard são entidades separadas; processo failure/timeout e shards ausentes impedem suite_pass, mesmo diante de XML verde.

## Completeness criteria

**HUG-029-CC01 — obrigatório.** Dialetos, IDs parametrizados, retries e shard manifest têm fixtures válidas e hostis.

Assertions: `A-HUG-029-CC01`. Famílias: T-HUG-029-P.

**HUG-029-CC02 — obrigatório.** Bytes originais permitidos preservados junto a derivação normalizada versionada.

Assertions: `A-HUG-029-CC02`. Famílias: T-HUG-029-P.

**HUG-029-CC03 — obrigatório.** Report discovery liga por witness/run dir, não recência.

Assertions: `A-HUG-029-CC03`. Famílias: T-HUG-029-P.

## Success criteria

**HUG-029-SC01 — obrigatório.** Dois runs reais geram relatórios em diretórios separados; casos/pass/fail/error/skipped/retry conservam contagens e primeira falha; suite completa exige todos os shards declarados.

Assertions: `A-HUG-029-SC01`. Famílias: T-HUG-029-P.

**HUG-029-SC02 — obrigatório.** Manter junit verde antigo e falhar compilação atual não cria testes aprovados para a execução nova; XXE, documentos parciais e counts inconsistentes são invalid/partial.

Assertions: `A-HUG-029-SC02`. Famílias: T-HUG-029-N.

## Quality standards

**HUG-029-QS01 — obrigatório.** Zero cases é no_tests, não sucesso presumido.

Assertions: `A-HUG-029-QS01`. Famílias: T-HUG-029-Q.

**HUG-029-QS02 — obrigatório.** Entidades externas/rede desabilitadas; budgets de bytes/profundidade/elementos P08.

Assertions: `A-HUG-029-QS02`. Famílias: T-HUG-029-Q.

**HUG-029-QS03 — obrigatório.** P08: 32 MiB/report, depth64, ≤100k cases/≤1M nodes; archive ≤100k entries e expansão limitada por quota; inválido nunca vira verde vazio.

Assertions: `A-HUG-029-QS03`. Famílias: T-HUG-029-Q.

## Definition of Done — DoD

**HUG-029-DOD01 — obrigatório.** Fechar HUG-029 somente quando a implementação/artefato de 'JUnit com procedência por execução e agregação completa' satisfaz suas assertions e observáveis obrigatórios; uma suite verde sem resultado individual não fecha os axiomas.

Assertions: `A-HUG-029-DOD01`. Famílias: T-HUG-029-Q.

**HUG-029-DOD02 — obrigatório.** Todos os outputs crates/hugit-cli/src/reports/junit.rs, conformance/junit-dialects.json, crates/hugit-cli/tests/junit_provenance.rs entregues nos write sets resolvidos; erros de P00, P06, P08 e mudanças de interface revisados pelo owner do contrato.

Assertions: `A-HUG-029-DOD02`. Famílias: T-HUG-029-Q.

**HUG-029-DOD03 — obrigatório.** Recovery deste pacote ensaiado: Revogar versão do parser e invalidar apenas derivados; reprocessar bytes preservados sob novo parser. Não transformar relatório antigo em evidência nova nem apagar primeira tentativa.

Assertions: `A-HUG-029-DOD03`. Famílias: T-HUG-029-R.

**HUG-029-DOD04 — obrigatório.** Qualificar HUG-029 sobre o sujeito A identificado por digest; anexar em B assertions verificadas e aprovação de revisor diferente do autor. Resultados não alteram A; revalidar no candidato final as obrigações atingidas. Nenhum axioma obrigatório dispensado.

Assertions: `A-HUG-029-DOD04`. Famílias: T-HUG-029-Q.

## Invariants

**HUG-029-INV01 — obrigatório.** Validade sintática não prova procedência.

Assertions: `A-HUG-029-INV01`. Famílias: T-HUG-029-N.

**HUG-029-INV02 — obrigatório.** Report verde não sobrescreve erro de processo.

Assertions: `A-HUG-029-INV02`. Famílias: T-HUG-029-N.

**HUG-029-INV03 — obrigatório.** Shard ausente mantém aggregate incompleto.

Assertions: `A-HUG-029-INV03`. Famílias: T-HUG-029-P.

## Budget e recuperação

P08: 32 MiB/report, depth64, ≤100k cases/≤1M nodes; archive ≤100k entries e expansão limitada por quota; inválido nunca vira verde vazio.

**Rollback/recovery:** Revogar versão do parser e invalidar apenas derivados; reprocessar bytes preservados sob novo parser. Não transformar relatório antigo em evidência nova nem apagar primeira tentativa.

**Rollout:** habilitar só depois das assertions, qualificação de célula e revisão. Flag não anuncia recurso não qualificado.

**Plano de evidências:** pacote B/C destacado do sujeito imutável A; não commitar resultados em A para exigir seu próprio hash.

## Assertions deste pacote

### A-HUG-029-CC01

Família `T-HUG-029-P`; etapa `pre_promotion`; estado `specified_not_executed`.

**Dado:** {
  "source": "Sujeito A e versões do contrato fixados; teste de descoberta usa seu artefato de fonte inspecionado, não inventa runtime.",
  "protocols": [
    "P00@3.0",
    "P06@3.0",
    "P08@3.0"
  ],
  "fixture_inputs": [
    "Predecessores admitidos e SHA fixado, ou checkout a obter por HUG-001; descoberta produz sua fonte/fixture, consumidores exigem a fonte qualificada.",
    "Contratos P00, P06, P08 desta revisão; freeze runtime exigido somente para consumidores pós-HUG-059. Evidence histórica não conta como novo ensaio."
  ],
  "fixture_scope": [
    "crates/hugit-cli/src/reports/junit.rs",
    "conformance/junit-dialects.json",
    "crates/hugit-cli/tests/junit_provenance.rs"
  ]
}

**Estímulo:** Aceitar dialetos enumerados com version/parser; output dir exclusivo por invocation_id e producer witness quando integrado. Arquivo externo sem proveniência é imported/unverified_binding; mtime e XML válido não provam geração nesta execução. Test case, attempt e shard são entidades separadas; processo failure/timeout e shards ausentes impedem suite_pass, mesmo diante de XML verde.

**Predicado e observáveis:**
```json
{
  "predicate": "Dialetos, IDs parametrizados, retries e shard manifest têm fixtures válidas e hostis.",
  "observable_paths": [
    "crates/hugit-cli/src/reports/junit.rs",
    "conformance/junit-dialects.json",
    "crates/hugit-cli/tests/junit_provenance.rs"
  ],
  "required_fields": [
    "assertion_id",
    "expected",
    "observed",
    "artifact_refs",
    "outcome",
    "verifier_identity"
  ],
  "verifier_kind": "structured_review_of_observables"
}
```

**Evidência:** resultado imutável por sujeito/assertion/célula/attempt, bytes referenciados e identidade do verificador. Uma suite verde sem esta assertion não fecha o critério.

**Oráculo SHA-256:** `31e01561b8a099b617f3cafc99ae076c7e4087e787e45a58feabc86b5518af07`

### A-HUG-029-CC02

Família `T-HUG-029-P`; etapa `pre_promotion`; estado `specified_not_executed`.

**Dado:** {
  "source": "Sujeito A e versões do contrato fixados; teste de descoberta usa seu artefato de fonte inspecionado, não inventa runtime.",
  "protocols": [
    "P00@3.0",
    "P06@3.0",
    "P08@3.0"
  ],
  "fixture_inputs": [
    "Predecessores admitidos e SHA fixado, ou checkout a obter por HUG-001; descoberta produz sua fonte/fixture, consumidores exigem a fonte qualificada.",
    "Contratos P00, P06, P08 desta revisão; freeze runtime exigido somente para consumidores pós-HUG-059. Evidence histórica não conta como novo ensaio."
  ],
  "fixture_scope": [
    "crates/hugit-cli/src/reports/junit.rs",
    "conformance/junit-dialects.json",
    "crates/hugit-cli/tests/junit_provenance.rs"
  ]
}

**Estímulo:** Aceitar dialetos enumerados com version/parser; output dir exclusivo por invocation_id e producer witness quando integrado. Arquivo externo sem proveniência é imported/unverified_binding; mtime e XML válido não provam geração nesta execução. Test case, attempt e shard são entidades separadas; processo failure/timeout e shards ausentes impedem suite_pass, mesmo diante de XML verde.

**Predicado e observáveis:**
```json
{
  "predicate": "Bytes originais permitidos preservados junto a derivação normalizada versionada.",
  "observable_paths": [
    "crates/hugit-cli/src/reports/junit.rs",
    "conformance/junit-dialects.json",
    "crates/hugit-cli/tests/junit_provenance.rs"
  ],
  "required_fields": [
    "assertion_id",
    "expected",
    "observed",
    "artifact_refs",
    "outcome",
    "verifier_identity"
  ],
  "verifier_kind": "structured_review_of_observables"
}
```

**Evidência:** resultado imutável por sujeito/assertion/célula/attempt, bytes referenciados e identidade do verificador. Uma suite verde sem esta assertion não fecha o critério.

**Oráculo SHA-256:** `4918a9e0e3f525dcf58722cfc91ab8a75508a3ed3ebbe1f2c4670119d78b5efa`

### A-HUG-029-CC03

Família `T-HUG-029-P`; etapa `pre_promotion`; estado `specified_not_executed`.

**Dado:** {
  "source": "Sujeito A e versões do contrato fixados; teste de descoberta usa seu artefato de fonte inspecionado, não inventa runtime.",
  "protocols": [
    "P00@3.0",
    "P06@3.0",
    "P08@3.0"
  ],
  "fixture_inputs": [
    "Predecessores admitidos e SHA fixado, ou checkout a obter por HUG-001; descoberta produz sua fonte/fixture, consumidores exigem a fonte qualificada.",
    "Contratos P00, P06, P08 desta revisão; freeze runtime exigido somente para consumidores pós-HUG-059. Evidence histórica não conta como novo ensaio."
  ],
  "fixture_scope": [
    "crates/hugit-cli/src/reports/junit.rs",
    "conformance/junit-dialects.json",
    "crates/hugit-cli/tests/junit_provenance.rs"
  ]
}

**Estímulo:** Aceitar dialetos enumerados com version/parser; output dir exclusivo por invocation_id e producer witness quando integrado. Arquivo externo sem proveniência é imported/unverified_binding; mtime e XML válido não provam geração nesta execução. Test case, attempt e shard são entidades separadas; processo failure/timeout e shards ausentes impedem suite_pass, mesmo diante de XML verde.

**Predicado e observáveis:**
```json
{
  "predicate": "Report discovery liga por witness/run dir, não recência.",
  "observable_paths": [
    "crates/hugit-cli/src/reports/junit.rs",
    "conformance/junit-dialects.json",
    "crates/hugit-cli/tests/junit_provenance.rs"
  ],
  "required_fields": [
    "assertion_id",
    "expected",
    "observed",
    "artifact_refs",
    "outcome",
    "verifier_identity"
  ],
  "verifier_kind": "structured_review_of_observables"
}
```

**Evidência:** resultado imutável por sujeito/assertion/célula/attempt, bytes referenciados e identidade do verificador. Uma suite verde sem esta assertion não fecha o critério.

**Oráculo SHA-256:** `9864ea40a1f2bc85929ac4df3cea761a6b6516b241cf249d124f2efe351be392`

### A-HUG-029-SC01

Família `T-HUG-029-P`; etapa `pre_promotion`; estado `specified_not_executed`.

**Dado:** {
  "source": "Sujeito A e versões do contrato fixados; teste de descoberta usa seu artefato de fonte inspecionado, não inventa runtime.",
  "protocols": [
    "P00@3.0",
    "P06@3.0",
    "P08@3.0"
  ],
  "fixture_inputs": [
    "Predecessores admitidos e SHA fixado, ou checkout a obter por HUG-001; descoberta produz sua fonte/fixture, consumidores exigem a fonte qualificada.",
    "Contratos P00, P06, P08 desta revisão; freeze runtime exigido somente para consumidores pós-HUG-059. Evidence histórica não conta como novo ensaio."
  ],
  "fixture_scope": [
    "crates/hugit-cli/src/reports/junit.rs",
    "conformance/junit-dialects.json",
    "crates/hugit-cli/tests/junit_provenance.rs"
  ]
}

**Estímulo:** Aceitar dialetos enumerados com version/parser; output dir exclusivo por invocation_id e producer witness quando integrado. Arquivo externo sem proveniência é imported/unverified_binding; mtime e XML válido não provam geração nesta execução. Test case, attempt e shard são entidades separadas; processo failure/timeout e shards ausentes impedem suite_pass, mesmo diante de XML verde.

**Predicado e observáveis:**
```json
{
  "predicate": "Dois runs reais geram relatórios em diretórios separados; casos/pass/fail/error/skipped/retry conservam contagens e primeira falha; suite completa exige todos os shards declarados.",
  "observable_paths": [
    "crates/hugit-cli/src/reports/junit.rs",
    "conformance/junit-dialects.json",
    "crates/hugit-cli/tests/junit_provenance.rs"
  ],
  "required_fields": [
    "assertion_id",
    "expected",
    "observed",
    "artifact_refs",
    "outcome",
    "verifier_identity"
  ],
  "verifier_kind": "runtime_or_artifact_predicate"
}
```

**Evidência:** resultado imutável por sujeito/assertion/célula/attempt, bytes referenciados e identidade do verificador. Uma suite verde sem esta assertion não fecha o critério.

**Oráculo SHA-256:** `6b5fc969a817320216da43281ba59853f74fa611c7cfd29cf3a2545585d53694`

### A-HUG-029-SC02

Família `T-HUG-029-N`; etapa `pre_promotion`; estado `specified_not_executed`.

**Dado:** {
  "source": "Sujeito A e versões do contrato fixados; teste de descoberta usa seu artefato de fonte inspecionado, não inventa runtime.",
  "protocols": [
    "P00@3.0",
    "P06@3.0",
    "P08@3.0"
  ],
  "fixture_inputs": [
    "Predecessores admitidos e SHA fixado, ou checkout a obter por HUG-001; descoberta produz sua fonte/fixture, consumidores exigem a fonte qualificada.",
    "Contratos P00, P06, P08 desta revisão; freeze runtime exigido somente para consumidores pós-HUG-059. Evidence histórica não conta como novo ensaio."
  ],
  "fixture_scope": [
    "crates/hugit-cli/src/reports/junit.rs",
    "conformance/junit-dialects.json",
    "crates/hugit-cli/tests/junit_provenance.rs"
  ]
}

**Estímulo:** Manter junit verde antigo e falhar compilação atual não cria testes aprovados para a execução nova; XXE, documentos parciais e counts inconsistentes são invalid/partial. Executar o cenário negativo das assertions nomeadas, com estado/bytes antes e depois.

**Predicado e observáveis:**
```json
{
  "predicate": "Manter junit verde antigo e falhar compilação atual não cria testes aprovados para a execução nova; XXE, documentos parciais e counts inconsistentes são invalid/partial.",
  "observable_paths": [
    "crates/hugit-cli/src/reports/junit.rs",
    "conformance/junit-dialects.json",
    "crates/hugit-cli/tests/junit_provenance.rs"
  ],
  "required_fields": [
    "assertion_id",
    "expected",
    "observed",
    "artifact_refs",
    "outcome",
    "verifier_identity"
  ],
  "verifier_kind": "runtime_or_artifact_predicate"
}
```

**Evidência:** resultado imutável por sujeito/assertion/célula/attempt, bytes referenciados e identidade do verificador. Uma suite verde sem esta assertion não fecha o critério.

**Oráculo SHA-256:** `c6e1c9f915522b719d74e6237d50d9ac6109e3c77bc072fbd3a48515220f7c00`

### A-HUG-029-QS01

Família `T-HUG-029-Q`; etapa `pre_promotion`; estado `specified_not_executed`.

**Dado:** {
  "source": "Sujeito A e versões do contrato fixados; teste de descoberta usa seu artefato de fonte inspecionado, não inventa runtime.",
  "protocols": [
    "P00@3.0",
    "P06@3.0",
    "P08@3.0"
  ],
  "fixture_inputs": [
    "Predecessores admitidos e SHA fixado, ou checkout a obter por HUG-001; descoberta produz sua fonte/fixture, consumidores exigem a fonte qualificada.",
    "Contratos P00, P06, P08 desta revisão; freeze runtime exigido somente para consumidores pós-HUG-059. Evidence histórica não conta como novo ensaio."
  ],
  "fixture_scope": [
    "crates/hugit-cli/src/reports/junit.rs",
    "conformance/junit-dialects.json",
    "crates/hugit-cli/tests/junit_provenance.rs"
  ]
}

**Estímulo:** Fixar versões, ambiente e dados; verificar todos os QS e budget aplicável. Separar erro de setup de violação do produto; conservar medições desfavoráveis.

**Predicado e observáveis:**
```json
{
  "predicate": "Zero cases é no_tests, não sucesso presumido.",
  "observable_paths": [
    "crates/hugit-cli/src/reports/junit.rs",
    "conformance/junit-dialects.json",
    "crates/hugit-cli/tests/junit_provenance.rs"
  ],
  "required_fields": [
    "assertion_id",
    "expected",
    "observed",
    "artifact_refs",
    "outcome",
    "verifier_identity"
  ],
  "verifier_kind": "structured_review_of_observables"
}
```

**Evidência:** resultado imutável por sujeito/assertion/célula/attempt, bytes referenciados e identidade do verificador. Uma suite verde sem esta assertion não fecha o critério.

**Oráculo SHA-256:** `2daaa91a2491e2f0ae97fa0b2b2859b708ee0897d4446a7591908773043c123c`

### A-HUG-029-QS02

Família `T-HUG-029-Q`; etapa `pre_promotion`; estado `specified_not_executed`.

**Dado:** {
  "source": "Sujeito A e versões do contrato fixados; teste de descoberta usa seu artefato de fonte inspecionado, não inventa runtime.",
  "protocols": [
    "P00@3.0",
    "P06@3.0",
    "P08@3.0"
  ],
  "fixture_inputs": [
    "Predecessores admitidos e SHA fixado, ou checkout a obter por HUG-001; descoberta produz sua fonte/fixture, consumidores exigem a fonte qualificada.",
    "Contratos P00, P06, P08 desta revisão; freeze runtime exigido somente para consumidores pós-HUG-059. Evidence histórica não conta como novo ensaio."
  ],
  "fixture_scope": [
    "crates/hugit-cli/src/reports/junit.rs",
    "conformance/junit-dialects.json",
    "crates/hugit-cli/tests/junit_provenance.rs"
  ]
}

**Estímulo:** Fixar versões, ambiente e dados; verificar todos os QS e budget aplicável. Separar erro de setup de violação do produto; conservar medições desfavoráveis.

**Predicado e observáveis:**
```json
{
  "predicate": "Entidades externas/rede desabilitadas; budgets de bytes/profundidade/elementos P08.",
  "observable_paths": [
    "crates/hugit-cli/src/reports/junit.rs",
    "conformance/junit-dialects.json",
    "crates/hugit-cli/tests/junit_provenance.rs"
  ],
  "required_fields": [
    "assertion_id",
    "expected",
    "observed",
    "artifact_refs",
    "outcome",
    "verifier_identity"
  ],
  "verifier_kind": "structured_review_of_observables"
}
```

**Evidência:** resultado imutável por sujeito/assertion/célula/attempt, bytes referenciados e identidade do verificador. Uma suite verde sem esta assertion não fecha o critério.

**Oráculo SHA-256:** `c55a9bd1807c1d7f1b9105fdd1c7fa277f0014b87f39da165f8ea3c748c42e46`

### A-HUG-029-QS03

Família `T-HUG-029-Q`; etapa `pre_promotion`; estado `specified_not_executed`.

**Dado:** {
  "source": "Sujeito A e versões do contrato fixados; teste de descoberta usa seu artefato de fonte inspecionado, não inventa runtime.",
  "protocols": [
    "P00@3.0",
    "P06@3.0",
    "P08@3.0"
  ],
  "fixture_inputs": [
    "Predecessores admitidos e SHA fixado, ou checkout a obter por HUG-001; descoberta produz sua fonte/fixture, consumidores exigem a fonte qualificada.",
    "Contratos P00, P06, P08 desta revisão; freeze runtime exigido somente para consumidores pós-HUG-059. Evidence histórica não conta como novo ensaio."
  ],
  "fixture_scope": [
    "crates/hugit-cli/src/reports/junit.rs",
    "conformance/junit-dialects.json",
    "crates/hugit-cli/tests/junit_provenance.rs"
  ]
}

**Estímulo:** Fixar versões, ambiente e dados; verificar todos os QS e budget aplicável. Separar erro de setup de violação do produto; conservar medições desfavoráveis.

**Predicado e observáveis:**
```json
{
  "predicate": "P08: 32 MiB/report, depth64, ≤100k cases/≤1M nodes; archive ≤100k entries e expansão limitada por quota; inválido nunca vira verde vazio.",
  "observable_paths": [
    "crates/hugit-cli/src/reports/junit.rs",
    "conformance/junit-dialects.json",
    "crates/hugit-cli/tests/junit_provenance.rs"
  ],
  "required_fields": [
    "assertion_id",
    "expected",
    "observed",
    "artifact_refs",
    "outcome",
    "verifier_identity"
  ],
  "verifier_kind": "structured_review_of_observables"
}
```

**Evidência:** resultado imutável por sujeito/assertion/célula/attempt, bytes referenciados e identidade do verificador. Uma suite verde sem esta assertion não fecha o critério.

**Oráculo SHA-256:** `e68f9244955fe6b33721d88bb132699f15a1d57860e267bdd37e2e7c5854d20e`

### A-HUG-029-INV01

Família `T-HUG-029-N`; etapa `pre_promotion`; estado `specified_not_executed`.

**Dado:** {
  "source": "Sujeito A e versões do contrato fixados; teste de descoberta usa seu artefato de fonte inspecionado, não inventa runtime.",
  "protocols": [
    "P00@3.0",
    "P06@3.0",
    "P08@3.0"
  ],
  "fixture_inputs": [
    "Predecessores admitidos e SHA fixado, ou checkout a obter por HUG-001; descoberta produz sua fonte/fixture, consumidores exigem a fonte qualificada.",
    "Contratos P00, P06, P08 desta revisão; freeze runtime exigido somente para consumidores pós-HUG-059. Evidence histórica não conta como novo ensaio."
  ],
  "fixture_scope": [
    "crates/hugit-cli/src/reports/junit.rs",
    "conformance/junit-dialects.json",
    "crates/hugit-cli/tests/junit_provenance.rs"
  ]
}

**Estímulo:** Manter junit verde antigo e falhar compilação atual não cria testes aprovados para a execução nova; XXE, documentos parciais e counts inconsistentes são invalid/partial. Executar o cenário negativo das assertions nomeadas, com estado/bytes antes e depois.

**Predicado e observáveis:**
```json
{
  "predicate": "Validade sintática não prova procedência.",
  "observable_paths": [
    "crates/hugit-cli/src/reports/junit.rs",
    "conformance/junit-dialects.json",
    "crates/hugit-cli/tests/junit_provenance.rs"
  ],
  "required_fields": [
    "assertion_id",
    "expected",
    "observed",
    "artifact_refs",
    "outcome",
    "verifier_identity"
  ],
  "verifier_kind": "runtime_or_artifact_predicate"
}
```

**Evidência:** resultado imutável por sujeito/assertion/célula/attempt, bytes referenciados e identidade do verificador. Uma suite verde sem esta assertion não fecha o critério.

**Oráculo SHA-256:** `6f13c97aba6d182da1ffb75688ed31ed5d7cd2e018309c3b7af6c3db09ba2b21`

### A-HUG-029-INV02

Família `T-HUG-029-N`; etapa `pre_promotion`; estado `specified_not_executed`.

**Dado:** {
  "source": "Sujeito A e versões do contrato fixados; teste de descoberta usa seu artefato de fonte inspecionado, não inventa runtime.",
  "protocols": [
    "P00@3.0",
    "P06@3.0",
    "P08@3.0"
  ],
  "fixture_inputs": [
    "Predecessores admitidos e SHA fixado, ou checkout a obter por HUG-001; descoberta produz sua fonte/fixture, consumidores exigem a fonte qualificada.",
    "Contratos P00, P06, P08 desta revisão; freeze runtime exigido somente para consumidores pós-HUG-059. Evidence histórica não conta como novo ensaio."
  ],
  "fixture_scope": [
    "crates/hugit-cli/src/reports/junit.rs",
    "conformance/junit-dialects.json",
    "crates/hugit-cli/tests/junit_provenance.rs"
  ]
}

**Estímulo:** Manter junit verde antigo e falhar compilação atual não cria testes aprovados para a execução nova; XXE, documentos parciais e counts inconsistentes são invalid/partial. Executar o cenário negativo das assertions nomeadas, com estado/bytes antes e depois.

**Predicado e observáveis:**
```json
{
  "predicate": "Report verde não sobrescreve erro de processo.",
  "observable_paths": [
    "crates/hugit-cli/src/reports/junit.rs",
    "conformance/junit-dialects.json",
    "crates/hugit-cli/tests/junit_provenance.rs"
  ],
  "required_fields": [
    "assertion_id",
    "expected",
    "observed",
    "artifact_refs",
    "outcome",
    "verifier_identity"
  ],
  "verifier_kind": "runtime_or_artifact_predicate"
}
```

**Evidência:** resultado imutável por sujeito/assertion/célula/attempt, bytes referenciados e identidade do verificador. Uma suite verde sem esta assertion não fecha o critério.

**Oráculo SHA-256:** `347f32aa45fc2524a12be697eab5eccba4bfa5292d659958f0730000f89a3c28`

### A-HUG-029-INV03

Família `T-HUG-029-P`; etapa `pre_promotion`; estado `specified_not_executed`.

**Dado:** {
  "source": "Sujeito A e versões do contrato fixados; teste de descoberta usa seu artefato de fonte inspecionado, não inventa runtime.",
  "protocols": [
    "P00@3.0",
    "P06@3.0",
    "P08@3.0"
  ],
  "fixture_inputs": [
    "Predecessores admitidos e SHA fixado, ou checkout a obter por HUG-001; descoberta produz sua fonte/fixture, consumidores exigem a fonte qualificada.",
    "Contratos P00, P06, P08 desta revisão; freeze runtime exigido somente para consumidores pós-HUG-059. Evidence histórica não conta como novo ensaio."
  ],
  "fixture_scope": [
    "crates/hugit-cli/src/reports/junit.rs",
    "conformance/junit-dialects.json",
    "crates/hugit-cli/tests/junit_provenance.rs"
  ]
}

**Estímulo:** Aceitar dialetos enumerados com version/parser; output dir exclusivo por invocation_id e producer witness quando integrado. Arquivo externo sem proveniência é imported/unverified_binding; mtime e XML válido não provam geração nesta execução. Test case, attempt e shard são entidades separadas; processo failure/timeout e shards ausentes impedem suite_pass, mesmo diante de XML verde.

**Predicado e observáveis:**
```json
{
  "predicate": "Shard ausente mantém aggregate incompleto.",
  "observable_paths": [
    "crates/hugit-cli/src/reports/junit.rs",
    "conformance/junit-dialects.json",
    "crates/hugit-cli/tests/junit_provenance.rs"
  ],
  "required_fields": [
    "assertion_id",
    "expected",
    "observed",
    "artifact_refs",
    "outcome",
    "verifier_identity"
  ],
  "verifier_kind": "runtime_or_artifact_predicate"
}
```

**Evidência:** resultado imutável por sujeito/assertion/célula/attempt, bytes referenciados e identidade do verificador. Uma suite verde sem esta assertion não fecha o critério.

**Oráculo SHA-256:** `d905e66c07b82386b05b4fd330de2961eeb6e2af8980134f5653d94c83976609`

### A-HUG-029-DOD01

Família `T-HUG-029-Q`; etapa `pre_promotion`; estado `specified_not_executed`.

**Dado:** {
  "source": "Sujeito A e versões do contrato fixados; teste de descoberta usa seu artefato de fonte inspecionado, não inventa runtime.",
  "protocols": [
    "P00@3.0",
    "P06@3.0",
    "P08@3.0"
  ],
  "fixture_inputs": [
    "Predecessores admitidos e SHA fixado, ou checkout a obter por HUG-001; descoberta produz sua fonte/fixture, consumidores exigem a fonte qualificada.",
    "Contratos P00, P06, P08 desta revisão; freeze runtime exigido somente para consumidores pós-HUG-059. Evidence histórica não conta como novo ensaio."
  ],
  "fixture_scope": [
    "crates/hugit-cli/src/reports/junit.rs",
    "conformance/junit-dialects.json",
    "crates/hugit-cli/tests/junit_provenance.rs"
  ]
}

**Estímulo:** Fixar versões, ambiente e dados; verificar todos os QS e budget aplicável. Separar erro de setup de violação do produto; conservar medições desfavoráveis.

**Predicado e observáveis:**
```json
{
  "predicate": "Fechar HUG-029 somente quando a implementação/artefato de 'JUnit com procedência por execução e agregação completa' satisfaz suas assertions e observáveis obrigatórios; uma suite verde sem resultado individual não fecha os axiomas.",
  "observable_paths": [
    "crates/hugit-cli/src/reports/junit.rs",
    "conformance/junit-dialects.json",
    "crates/hugit-cli/tests/junit_provenance.rs"
  ],
  "required_fields": [
    "assertion_id",
    "expected",
    "observed",
    "artifact_refs",
    "outcome",
    "verifier_identity"
  ],
  "verifier_kind": "structured_review_of_observables"
}
```

**Evidência:** resultado imutável por sujeito/assertion/célula/attempt, bytes referenciados e identidade do verificador. Uma suite verde sem esta assertion não fecha o critério.

**Oráculo SHA-256:** `6b9ed0f1401f6a851271e3a8b927183b43ce4bd4b6b8dc82053848b84540af0e`

### A-HUG-029-DOD02

Família `T-HUG-029-Q`; etapa `pre_promotion`; estado `specified_not_executed`.

**Dado:** {
  "source": "Sujeito A e versões do contrato fixados; teste de descoberta usa seu artefato de fonte inspecionado, não inventa runtime.",
  "protocols": [
    "P00@3.0",
    "P06@3.0",
    "P08@3.0"
  ],
  "fixture_inputs": [
    "Predecessores admitidos e SHA fixado, ou checkout a obter por HUG-001; descoberta produz sua fonte/fixture, consumidores exigem a fonte qualificada.",
    "Contratos P00, P06, P08 desta revisão; freeze runtime exigido somente para consumidores pós-HUG-059. Evidence histórica não conta como novo ensaio."
  ],
  "fixture_scope": [
    "crates/hugit-cli/src/reports/junit.rs",
    "conformance/junit-dialects.json",
    "crates/hugit-cli/tests/junit_provenance.rs"
  ]
}

**Estímulo:** Fixar versões, ambiente e dados; verificar todos os QS e budget aplicável. Separar erro de setup de violação do produto; conservar medições desfavoráveis.

**Predicado e observáveis:**
```json
{
  "predicate": "Todos os outputs crates/hugit-cli/src/reports/junit.rs, conformance/junit-dialects.json, crates/hugit-cli/tests/junit_provenance.rs entregues nos write sets resolvidos; erros de P00, P06, P08 e mudanças de interface revisados pelo owner do contrato.",
  "observable_paths": [
    "crates/hugit-cli/src/reports/junit.rs",
    "conformance/junit-dialects.json",
    "crates/hugit-cli/tests/junit_provenance.rs"
  ],
  "required_fields": [
    "assertion_id",
    "expected",
    "observed",
    "artifact_refs",
    "outcome",
    "verifier_identity"
  ],
  "verifier_kind": "structured_review_of_observables"
}
```

**Evidência:** resultado imutável por sujeito/assertion/célula/attempt, bytes referenciados e identidade do verificador. Uma suite verde sem esta assertion não fecha o critério.

**Oráculo SHA-256:** `88956d57f77770d706b9fbe8836f461a56c2d75f71e3499efe99d17b579f4095`

### A-HUG-029-DOD03

Família `T-HUG-029-R`; etapa `pre_promotion`; estado `specified_not_executed`.

**Dado:** {
  "source": "Sujeito A e versões do contrato fixados; teste de descoberta usa seu artefato de fonte inspecionado, não inventa runtime.",
  "protocols": [
    "P00@3.0",
    "P06@3.0",
    "P08@3.0"
  ],
  "fixture_inputs": [
    "Predecessores admitidos e SHA fixado, ou checkout a obter por HUG-001; descoberta produz sua fonte/fixture, consumidores exigem a fonte qualificada.",
    "Contratos P00, P06, P08 desta revisão; freeze runtime exigido somente para consumidores pós-HUG-059. Evidence histórica não conta como novo ensaio."
  ],
  "fixture_scope": [
    "crates/hugit-cli/src/reports/junit.rs",
    "conformance/junit-dialects.json",
    "crates/hugit-cli/tests/junit_provenance.rs"
  ]
}

**Estímulo:** Interromper nas fronteiras qualificadas; executar: Revogar versão do parser e invalidar apenas derivados; reprocessar bytes preservados sob novo parser. Não transformar relatório antigo em evidência nova nem apagar primeira tentativa.

**Predicado e observáveis:**
```json
{
  "predicate": "Recovery deste pacote ensaiado: Revogar versão do parser e invalidar apenas derivados; reprocessar bytes preservados sob novo parser. Não transformar relatório antigo em evidência nova nem apagar primeira tentativa.",
  "observable_paths": [
    "crates/hugit-cli/src/reports/junit.rs",
    "conformance/junit-dialects.json",
    "crates/hugit-cli/tests/junit_provenance.rs"
  ],
  "required_fields": [
    "assertion_id",
    "expected",
    "observed",
    "artifact_refs",
    "outcome",
    "verifier_identity"
  ],
  "verifier_kind": "structured_review_of_observables"
}
```

**Evidência:** resultado imutável por sujeito/assertion/célula/attempt, bytes referenciados e identidade do verificador. Uma suite verde sem esta assertion não fecha o critério.

**Oráculo SHA-256:** `2d7ff2761d6921e787ccf9b222510b114cd61d25a75c048c4ddf798b153c7017`

### A-HUG-029-DOD04

Família `T-HUG-029-Q`; etapa `pre_promotion`; estado `specified_not_executed`.

**Dado:** {
  "source": "Sujeito A e versões do contrato fixados; teste de descoberta usa seu artefato de fonte inspecionado, não inventa runtime.",
  "protocols": [
    "P00@3.0",
    "P06@3.0",
    "P08@3.0"
  ],
  "fixture_inputs": [
    "Predecessores admitidos e SHA fixado, ou checkout a obter por HUG-001; descoberta produz sua fonte/fixture, consumidores exigem a fonte qualificada.",
    "Contratos P00, P06, P08 desta revisão; freeze runtime exigido somente para consumidores pós-HUG-059. Evidence histórica não conta como novo ensaio."
  ],
  "fixture_scope": [
    "crates/hugit-cli/src/reports/junit.rs",
    "conformance/junit-dialects.json",
    "crates/hugit-cli/tests/junit_provenance.rs"
  ]
}

**Estímulo:** Fixar versões, ambiente e dados; verificar todos os QS e budget aplicável. Separar erro de setup de violação do produto; conservar medições desfavoráveis.

**Predicado e observáveis:**
```json
{
  "predicate": "Qualificar HUG-029 sobre o sujeito A identificado por digest; anexar em B assertions verificadas e aprovação de revisor diferente do autor. Resultados não alteram A; revalidar no candidato final as obrigações atingidas. Nenhum axioma obrigatório dispensado.",
  "observable_paths": [
    "crates/hugit-cli/src/reports/junit.rs",
    "conformance/junit-dialects.json",
    "crates/hugit-cli/tests/junit_provenance.rs"
  ],
  "required_fields": [
    "assertion_id",
    "expected",
    "observed",
    "artifact_refs",
    "outcome",
    "verifier_identity"
  ],
  "verifier_kind": "structured_review_of_observables"
}
```

**Evidência:** resultado imutável por sujeito/assertion/célula/attempt, bytes referenciados e identidade do verificador. Uma suite verde sem esta assertion não fecha o critério.

**Oráculo SHA-256:** `d5d5cf2df25b55c59f0fc18e192edbe6575db06453c97e67069cd8fc0fb286fc`

## Famílias e células de aceitação

### T-HUG-029-N

Owner da família: HUG-029. Células: linux-x64-ext4, macos-arm64-apfs, macos-intel-apfs, windows-x64-ntfs. Etapa: pre_promotion.

**Preparo:** Predecessores admitidos e SHA fixado, ou checkout a obter por HUG-001; descoberta produz sua fonte/fixture, consumidores exigem a fonte qualificada. Contratos P00, P06, P08 desta revisão; freeze runtime exigido somente para consumidores pós-HUG-059. Evidence histórica não conta como novo ensaio.

**Execução:** Manter junit verde antigo e falhar compilação atual não cria testes aprovados para a execução nova; XXE, documentos parciais e counts inconsistentes são invalid/partial. Executar o cenário negativo das assertions nomeadas, com estado/bytes antes e depois.

**Esperado:** Manter junit verde antigo e falhar compilação atual não cria testes aprovados para a execução nova; XXE, documentos parciais e counts inconsistentes são invalid/partial.

**Artifact template:** `evidence/results/T-HUG-029-N/{subject_digest}/{cell_id}/{attempt_id}/result.json`

### T-HUG-029-P

Owner da família: HUG-029. Células: linux-x64-ext4, macos-arm64-apfs, macos-intel-apfs, windows-x64-ntfs. Etapa: pre_promotion.

**Preparo:** Predecessores admitidos e SHA fixado, ou checkout a obter por HUG-001; descoberta produz sua fonte/fixture, consumidores exigem a fonte qualificada. Contratos P00, P06, P08 desta revisão; freeze runtime exigido somente para consumidores pós-HUG-059. Evidence histórica não conta como novo ensaio.

**Execução:** Aceitar dialetos enumerados com version/parser; output dir exclusivo por invocation_id e producer witness quando integrado. Arquivo externo sem proveniência é imported/unverified_binding; mtime e XML válido não provam geração nesta execução. Test case, attempt e shard são entidades separadas; processo failure/timeout e shards ausentes impedem suite_pass, mesmo diante de XML verde.

**Esperado:** Dois runs reais geram relatórios em diretórios separados; casos/pass/fail/error/skipped/retry conservam contagens e primeira falha; suite completa exige todos os shards declarados.

**Artifact template:** `evidence/results/T-HUG-029-P/{subject_digest}/{cell_id}/{attempt_id}/result.json`

### T-HUG-029-Q

Owner da família: HUG-029. Células: linux-x64-ext4, macos-arm64-apfs, macos-intel-apfs, windows-x64-ntfs. Etapa: pre_promotion.

**Preparo:** Predecessores admitidos e SHA fixado, ou checkout a obter por HUG-001; descoberta produz sua fonte/fixture, consumidores exigem a fonte qualificada. Contratos P00, P06, P08 desta revisão; freeze runtime exigido somente para consumidores pós-HUG-059. Evidence histórica não conta como novo ensaio.

**Execução:** Fixar versões, ambiente e dados; verificar todos os QS e budget aplicável. Separar erro de setup de violação do produto; conservar medições desfavoráveis.

**Esperado:** Zero cases é no_tests, não sucesso presumido.; Entidades externas/rede desabilitadas; budgets de bytes/profundidade/elementos P08.; P08: 32 MiB/report, depth64, ≤100k cases/≤1M nodes; archive ≤100k entries e expansão limitada por quota; inválido nunca vira verde vazio.

**Artifact template:** `evidence/results/T-HUG-029-Q/{subject_digest}/{cell_id}/{attempt_id}/result.json`

### T-HUG-029-R

Owner da família: HUG-029. Células: linux-x64-ext4, macos-arm64-apfs, macos-intel-apfs, windows-x64-ntfs. Etapa: pre_promotion.

**Preparo:** Predecessores admitidos e SHA fixado, ou checkout a obter por HUG-001; descoberta produz sua fonte/fixture, consumidores exigem a fonte qualificada. Contratos P00, P06, P08 desta revisão; freeze runtime exigido somente para consumidores pós-HUG-059. Evidence histórica não conta como novo ensaio.

**Execução:** Interromper nas fronteiras qualificadas; executar: Revogar versão do parser e invalidar apenas derivados; reprocessar bytes preservados sob novo parser. Não transformar relatório antigo em evidência nova nem apagar primeira tentativa.

**Esperado:** Revogar versão do parser e invalidar apenas derivados; reprocessar bytes preservados sob novo parser. Não transformar relatório antigo em evidência nova nem apagar primeira tentativa.

**Artifact template:** `evidence/results/T-HUG-029-R/{subject_digest}/{cell_id}/{attempt_id}/result.json`

**Fechamento:** cinco grupos de axiomas demonstrados por assertions verificadas no candidato integrado, sem waiver. HUG-056 fecha somente após C e pós-publicação; demais estados seguem P00/P12.
