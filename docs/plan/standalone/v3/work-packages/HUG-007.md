# HUG-007 — Registro de capacidades, perfis e denominador completo

**Estado:** planned; **owner:** A; **fase:** A; **prioridade:** first_slice.

**Dependências:** HUG-002, HUG-005

**Congelamento de runtime requerido:** false

**Revisões:** AR-13, AR-15, BR-01, BR-02, BR-09

**Contratos vinculados:**
- P00@3.0 — sha256 `5d737267087e24bbcca9548488c719f4cca46fa3435a0ccf31b7b98696fde5cc`
- P12@3.0 — sha256 `3f6367a500989e2a102abda97032a8ce25c1bd0edcbe660dd77a96fe4bb37a64`

## Prontidão

Todos os predecessores possuem evidência integrada e interfaces versionadas compatíveis.

Owner A aceita write-set exato e budget B-DOC; HUG-001 distingue arquivos existentes dos novos.

Os casos P/N/Q/R e os oráculos estão especificados antes do dispatch; fixtures e testes executáveis são entregues pelo próprio pacote ou predecessor. Documentação/descoberta usa oráculos de artefato, não um falso bug de runtime.

Qualquer dado de fonte ainda não observado bloqueia o consumidor; descobrir no pacote predecessor, sem inventar payload.

Usar a especificação aprovada desta revisão; descobertas/contratos e contenções de baseline produzem as pré-condições do freeze, sem ativar implicitamente o runtime v2.

## Outputs e ownership

Caminhos propostos: HUG-001 precisa vincular sua presença/criação ao checkout real antes da implementação. Evidências B/C ficam fora do sujeito A.

- `release/profiles.json`
- `release/capabilities.json`
- `scripts/release/check_profile.py`

**Leituras:** `docs/feature-ledger.md`, `README.md`, `CLAUDE.md`, `crates/hugit-cli/src/lib.rs`

**Recursos exclusivos:** path:release/profiles.json, path:release/capabilities.json, path:scripts/release/check_profile.py

## Interfaces e erros

```json
{
  "interfaces": [
    {
      "protocol": "P00",
      "interfaces": [
        "validate_plan(plan)->validation_report",
        "dispatch(wp,source_manifest,dependency_evidence)->admission",
        "qualify(profile,candidate,evidence)->readiness"
      ]
    },
    {
      "protocol": "P12",
      "interfaces": [
        "resolve_profile(id)->required_cells_and_assertions",
        "qualify_pre(subject_A,evidence_B,verified_records)->pre_readiness",
        "record_promotion(subject_A,evidence_B,destination)->promotion_C",
        "verify_post(subject_A,evidence_B,promotion_C)->post_verified_or_recovery_required"
      ]
    }
  ],
  "errors": [
    {
      "protocol": "P00",
      "codes": [
        "PLAN_INVALID",
        "DEPENDENCY_UNPROVEN",
        "CONTRACT_NOT_FROZEN",
        "SOURCE_DRIFT",
        "PROFILE_INCOMPLETE",
        "PROTOCOL_DIGEST_MISMATCH",
        "ASSERTION_UNCOVERED",
        "NORMATIVE_VIEW_DRIFT",
        "TEST_OWNER_MISMATCH"
      ]
    },
    {
      "protocol": "P12",
      "codes": [
        "REQUIRED_PACKAGE_MISSING",
        "AXIOM_UNSATISFIED",
        "TEST_NOT_RUN",
        "ARTIFACT_MISMATCH",
        "NATIVE_CELL_UNVERIFIED",
        "APPROVAL_UNVERIFIED",
        "CELL_MISSING",
        "EVIDENCE_CONFLICT",
        "SUBJECT_MISMATCH",
        "POST_PUBLICATION_PENDING"
      ]
    }
  ]
}
```

## Unidades verificáveis

1. **HUG-007.S1** — Validar consumidores de freeze por regra geral, protocolo com versão/digest e vínculo por WP; baseline SHA e ownership de cada test family devem ser exatos.
2. **HUG-007.S2** — Gerar todas as vistas de backlog único; comparar integralmente Markdown/JSON/protocolos. Capabilities full e obligations por célula são denominador fechado, sem caminhos de evidence compartilhados por overwrite.
3. **HUG-007.S3** — Executar ataques originais e BR-01/02/09 com controles positivos; remover assertion específica invalida seu critério, mesmo que a suite pai siga passed.

## Responsabilidade nos achados de integração

- BR-01: Assertions próprias P/N/Q/R verificam a parcela implementada por este WP; integração de todos os consumidores é testada na família T-BR-01-C antes de B full. Não exigir o downstream ainda não construído para fechar uma implementação de interface local.
- BR-02: Assertions próprias P/N/Q/R verificam a parcela implementada por este WP; integração de todos os consumidores é testada na família T-BR-02-C antes de B full. Não exigir o downstream ainda não construído para fechar uma implementação de interface local.
- BR-09: Assertions próprias P/N/Q/R verificam a parcela implementada por este WP; integração de todos os consumidores é testada na família T-BR-09-C antes de B full. Não exigir o downstream ainda não construído para fechar uma implementação de interface local.

## Completeness criteria

**HUG-007-CC01 — obrigatório.** Registro tem owner e contrato para cada superfície antiga e nova.

Assertions: `A-HUG-007-CC01`. Famílias: T-HUG-007-P.

**HUG-007-CC02 — obrigatório.** Perfis possuem listas explícitas de pacotes e testes, sem inferir apenas do último nó.

Assertions: `A-HUG-007-CC02`. Famílias: T-HUG-007-P.

**HUG-007-CC03 — obrigatório.** Waiver de qualidade e axioma vazio são proibidos para todos os pacotes.

Assertions: `A-HUG-007-CC03`. Famílias: T-HUG-007-P.

## Success criteria

**HUG-007-SC01 — obrigatório.** Perfil full exige todos os 60 IDs e testes associados; perfil de slice exige fechamento das dependências sem anunciar o restante.

Assertions: `A-HUG-007-SC01`. Famílias: T-HUG-007-P.

**HUG-007-SC02 — obrigatório.** Retirar HUG-041 do perfil, remover um dependency edge ou anunciar capability órfã falha automaticamente.

Assertions: `A-HUG-007-SC02`. Famílias: T-HUG-007-N.

## Quality standards

**HUG-007-QS01 — obrigatório.** Manifestos máquina/humano saem de uma fonte.

Assertions: `A-HUG-007-QS01`. Famílias: T-HUG-007-Q.

**HUG-007-QS02 — obrigatório.** Sem hard-coded 100% quando só slice foi qualificado.

Assertions: `A-HUG-007-QS02`. Famílias: T-HUG-007-Q.

**HUG-007-QS03 — obrigatório.** Manifestos/versionamento e leitura offline; sem efeito no runtime; documentos sem placeholder obrigatório. Validação linear no número de registros e saída de erro limitada.

Assertions: `A-HUG-007-QS03`. Famílias: T-HUG-007-Q.

## Definition of Done — DoD

**HUG-007-DOD01 — obrigatório.** Fechar HUG-007 somente quando a implementação/artefato de 'Registro de capacidades, perfis e denominador completo' satisfaz suas assertions e observáveis obrigatórios; uma suite verde sem resultado individual não fecha os axiomas.

Assertions: `A-HUG-007-DOD01`. Famílias: T-HUG-007-Q.

**HUG-007-DOD02 — obrigatório.** Todos os outputs release/profiles.json, release/capabilities.json, scripts/release/check_profile.py entregues nos write sets resolvidos; erros de P00, P12 e mudanças de interface revisados pelo owner do contrato.

Assertions: `A-HUG-007-DOD02`. Famílias: T-HUG-007-Q.

**HUG-007-DOD03 — obrigatório.** Recovery deste pacote ensaiado: Revogar apenas claims do manifesto incorreto, reter último perfil válido e bloquear publicação até cobertura restaurada; não remover silenciosamente o pacote ausente do escopo full.

Assertions: `A-HUG-007-DOD03`. Famílias: T-HUG-007-R.

**HUG-007-DOD04 — obrigatório.** Qualificar HUG-007 sobre o sujeito A identificado por digest; anexar em B assertions verificadas e aprovação de revisor diferente do autor. Resultados não alteram A; revalidar no candidato final as obrigações atingidas. Nenhum axioma obrigatório dispensado.

Assertions: `A-HUG-007-DOD04`. Famílias: T-HUG-007-Q.

## Invariants

**HUG-007-INV01 — obrigatório.** Full não pode omitir nenhum pacote planejado.

Assertions: `A-HUG-007-INV01`. Famílias: T-HUG-007-N.

**HUG-007-INV02 — obrigatório.** Todos os cinco axiomas são obrigatórios em todo WP.

Assertions: `A-HUG-007-INV02`. Famílias: T-HUG-007-P.

**HUG-007-INV03 — obrigatório.** Capacidade anunciada requer evidência da versão candidata.

Assertions: `A-HUG-007-INV03`. Famílias: T-HUG-007-P.

## Budget e recuperação

Manifestos/versionamento e leitura offline; sem efeito no runtime; documentos sem placeholder obrigatório. Validação linear no número de registros e saída de erro limitada.

**Rollback/recovery:** Revogar apenas claims do manifesto incorreto, reter último perfil válido e bloquear publicação até cobertura restaurada; não remover silenciosamente o pacote ausente do escopo full.

**Rollout:** habilitar só depois das assertions, qualificação de célula e revisão. Flag não anuncia recurso não qualificado.

**Plano de evidências:** pacote B/C destacado do sujeito imutável A; não commitar resultados em A para exigir seu próprio hash.

## Assertions deste pacote

### A-HUG-007-CC01

Família `T-HUG-007-P`; etapa `pre_promotion`; estado `specified_not_executed`.

**Dado:** {
  "source": "Sujeito A e versões do contrato fixados; teste de descoberta usa seu artefato de fonte inspecionado, não inventa runtime.",
  "protocols": [
    "P00@3.0",
    "P12@3.0"
  ],
  "fixture_inputs": [
    "Predecessores admitidos e SHA fixado, ou checkout a obter por HUG-001; descoberta produz sua fonte/fixture, consumidores exigem a fonte qualificada.",
    "Contratos P00, P12 desta revisão; freeze runtime exigido somente para consumidores pós-HUG-059. Evidence histórica não conta como novo ensaio."
  ],
  "fixture_scope": [
    "release/profiles.json",
    "release/capabilities.json",
    "scripts/release/check_profile.py"
  ]
}

**Estímulo:** Validar consumidores de freeze por regra geral, protocolo com versão/digest e vínculo por WP; baseline SHA e ownership de cada test family devem ser exatos. Gerar todas as vistas de backlog único; comparar integralmente Markdown/JSON/protocolos. Capabilities full e obligations por célula são denominador fechado, sem caminhos de evidence compartilhados por overwrite. Executar ataques originais e BR-01/02/09 com controles positivos; remover assertion específica invalida seu critério, mesmo que a suite pai siga passed.

**Predicado e observáveis:**
```json
{
  "predicate": "Registro tem owner e contrato para cada superfície antiga e nova.",
  "observable_paths": [
    "release/profiles.json",
    "release/capabilities.json",
    "scripts/release/check_profile.py"
  ],
  "required_fields": [
    "assertion_id",
    "expected",
    "observed",
    "artifact_refs",
    "outcome",
    "verifier_identity"
  ],
  "verifier_kind": "structured_review_of_observables"
}
```

**Evidência:** resultado imutável por sujeito/assertion/célula/attempt, bytes referenciados e identidade do verificador. Uma suite verde sem esta assertion não fecha o critério.

**Oráculo SHA-256:** `e6e5f76e38eb9cf0a6d08ba6e4c3f32f2c917bfa2fbfaf4163a6151d1729cd5c`

### A-HUG-007-CC02

Família `T-HUG-007-P`; etapa `pre_promotion`; estado `specified_not_executed`.

**Dado:** {
  "source": "Sujeito A e versões do contrato fixados; teste de descoberta usa seu artefato de fonte inspecionado, não inventa runtime.",
  "protocols": [
    "P00@3.0",
    "P12@3.0"
  ],
  "fixture_inputs": [
    "Predecessores admitidos e SHA fixado, ou checkout a obter por HUG-001; descoberta produz sua fonte/fixture, consumidores exigem a fonte qualificada.",
    "Contratos P00, P12 desta revisão; freeze runtime exigido somente para consumidores pós-HUG-059. Evidence histórica não conta como novo ensaio."
  ],
  "fixture_scope": [
    "release/profiles.json",
    "release/capabilities.json",
    "scripts/release/check_profile.py"
  ]
}

**Estímulo:** Validar consumidores de freeze por regra geral, protocolo com versão/digest e vínculo por WP; baseline SHA e ownership de cada test family devem ser exatos. Gerar todas as vistas de backlog único; comparar integralmente Markdown/JSON/protocolos. Capabilities full e obligations por célula são denominador fechado, sem caminhos de evidence compartilhados por overwrite. Executar ataques originais e BR-01/02/09 com controles positivos; remover assertion específica invalida seu critério, mesmo que a suite pai siga passed.

**Predicado e observáveis:**
```json
{
  "predicate": "Perfis possuem listas explícitas de pacotes e testes, sem inferir apenas do último nó.",
  "observable_paths": [
    "release/profiles.json",
    "release/capabilities.json",
    "scripts/release/check_profile.py"
  ],
  "required_fields": [
    "assertion_id",
    "expected",
    "observed",
    "artifact_refs",
    "outcome",
    "verifier_identity"
  ],
  "verifier_kind": "structured_review_of_observables"
}
```

**Evidência:** resultado imutável por sujeito/assertion/célula/attempt, bytes referenciados e identidade do verificador. Uma suite verde sem esta assertion não fecha o critério.

**Oráculo SHA-256:** `77e7ff8b1a59f57f2e63d9b2e2465a8c1050d5b4047164286a1706ec754dc417`

### A-HUG-007-CC03

Família `T-HUG-007-P`; etapa `pre_promotion`; estado `specified_not_executed`.

**Dado:** {
  "source": "Sujeito A e versões do contrato fixados; teste de descoberta usa seu artefato de fonte inspecionado, não inventa runtime.",
  "protocols": [
    "P00@3.0",
    "P12@3.0"
  ],
  "fixture_inputs": [
    "Predecessores admitidos e SHA fixado, ou checkout a obter por HUG-001; descoberta produz sua fonte/fixture, consumidores exigem a fonte qualificada.",
    "Contratos P00, P12 desta revisão; freeze runtime exigido somente para consumidores pós-HUG-059. Evidence histórica não conta como novo ensaio."
  ],
  "fixture_scope": [
    "release/profiles.json",
    "release/capabilities.json",
    "scripts/release/check_profile.py"
  ]
}

**Estímulo:** Validar consumidores de freeze por regra geral, protocolo com versão/digest e vínculo por WP; baseline SHA e ownership de cada test family devem ser exatos. Gerar todas as vistas de backlog único; comparar integralmente Markdown/JSON/protocolos. Capabilities full e obligations por célula são denominador fechado, sem caminhos de evidence compartilhados por overwrite. Executar ataques originais e BR-01/02/09 com controles positivos; remover assertion específica invalida seu critério, mesmo que a suite pai siga passed.

**Predicado e observáveis:**
```json
{
  "predicate": "Waiver de qualidade e axioma vazio são proibidos para todos os pacotes.",
  "observable_paths": [
    "release/profiles.json",
    "release/capabilities.json",
    "scripts/release/check_profile.py"
  ],
  "required_fields": [
    "assertion_id",
    "expected",
    "observed",
    "artifact_refs",
    "outcome",
    "verifier_identity"
  ],
  "verifier_kind": "structured_review_of_observables"
}
```

**Evidência:** resultado imutável por sujeito/assertion/célula/attempt, bytes referenciados e identidade do verificador. Uma suite verde sem esta assertion não fecha o critério.

**Oráculo SHA-256:** `7cbceda27c11c89ea3e54bb5abda7b80f7b6979d7327b5088a54122b1a73933c`

### A-HUG-007-SC01

Família `T-HUG-007-P`; etapa `pre_promotion`; estado `specified_not_executed`.

**Dado:** {
  "source": "Sujeito A e versões do contrato fixados; teste de descoberta usa seu artefato de fonte inspecionado, não inventa runtime.",
  "protocols": [
    "P00@3.0",
    "P12@3.0"
  ],
  "fixture_inputs": [
    "Predecessores admitidos e SHA fixado, ou checkout a obter por HUG-001; descoberta produz sua fonte/fixture, consumidores exigem a fonte qualificada.",
    "Contratos P00, P12 desta revisão; freeze runtime exigido somente para consumidores pós-HUG-059. Evidence histórica não conta como novo ensaio."
  ],
  "fixture_scope": [
    "release/profiles.json",
    "release/capabilities.json",
    "scripts/release/check_profile.py"
  ]
}

**Estímulo:** Validar consumidores de freeze por regra geral, protocolo com versão/digest e vínculo por WP; baseline SHA e ownership de cada test family devem ser exatos. Gerar todas as vistas de backlog único; comparar integralmente Markdown/JSON/protocolos. Capabilities full e obligations por célula são denominador fechado, sem caminhos de evidence compartilhados por overwrite. Executar ataques originais e BR-01/02/09 com controles positivos; remover assertion específica invalida seu critério, mesmo que a suite pai siga passed.

**Predicado e observáveis:**
```json
{
  "predicate": "Perfil full exige todos os 60 IDs e testes associados; perfil de slice exige fechamento das dependências sem anunciar o restante.",
  "observable_paths": [
    "release/profiles.json",
    "release/capabilities.json",
    "scripts/release/check_profile.py"
  ],
  "required_fields": [
    "assertion_id",
    "expected",
    "observed",
    "artifact_refs",
    "outcome",
    "verifier_identity"
  ],
  "verifier_kind": "runtime_or_artifact_predicate"
}
```

**Evidência:** resultado imutável por sujeito/assertion/célula/attempt, bytes referenciados e identidade do verificador. Uma suite verde sem esta assertion não fecha o critério.

**Oráculo SHA-256:** `96e7f5c29167847b536ccae290e993817a769403740a401fb220278cb586d88a`

### A-HUG-007-SC02

Família `T-HUG-007-N`; etapa `pre_promotion`; estado `specified_not_executed`.

**Dado:** {
  "source": "Sujeito A e versões do contrato fixados; teste de descoberta usa seu artefato de fonte inspecionado, não inventa runtime.",
  "protocols": [
    "P00@3.0",
    "P12@3.0"
  ],
  "fixture_inputs": [
    "Predecessores admitidos e SHA fixado, ou checkout a obter por HUG-001; descoberta produz sua fonte/fixture, consumidores exigem a fonte qualificada.",
    "Contratos P00, P12 desta revisão; freeze runtime exigido somente para consumidores pós-HUG-059. Evidence histórica não conta como novo ensaio."
  ],
  "fixture_scope": [
    "release/profiles.json",
    "release/capabilities.json",
    "scripts/release/check_profile.py"
  ]
}

**Estímulo:** Retirar HUG-041 do perfil, remover um dependency edge ou anunciar capability órfã falha automaticamente. Executar o cenário negativo das assertions nomeadas, com estado/bytes antes e depois.

**Predicado e observáveis:**
```json
{
  "predicate": "Retirar HUG-041 do perfil, remover um dependency edge ou anunciar capability órfã falha automaticamente.",
  "observable_paths": [
    "release/profiles.json",
    "release/capabilities.json",
    "scripts/release/check_profile.py"
  ],
  "required_fields": [
    "assertion_id",
    "expected",
    "observed",
    "artifact_refs",
    "outcome",
    "verifier_identity"
  ],
  "verifier_kind": "runtime_or_artifact_predicate"
}
```

**Evidência:** resultado imutável por sujeito/assertion/célula/attempt, bytes referenciados e identidade do verificador. Uma suite verde sem esta assertion não fecha o critério.

**Oráculo SHA-256:** `be9017d782638ed1b234770b28097fe1ffa20fbccc1fa59b588081de9de3fe4e`

### A-HUG-007-QS01

Família `T-HUG-007-Q`; etapa `pre_promotion`; estado `specified_not_executed`.

**Dado:** {
  "source": "Sujeito A e versões do contrato fixados; teste de descoberta usa seu artefato de fonte inspecionado, não inventa runtime.",
  "protocols": [
    "P00@3.0",
    "P12@3.0"
  ],
  "fixture_inputs": [
    "Predecessores admitidos e SHA fixado, ou checkout a obter por HUG-001; descoberta produz sua fonte/fixture, consumidores exigem a fonte qualificada.",
    "Contratos P00, P12 desta revisão; freeze runtime exigido somente para consumidores pós-HUG-059. Evidence histórica não conta como novo ensaio."
  ],
  "fixture_scope": [
    "release/profiles.json",
    "release/capabilities.json",
    "scripts/release/check_profile.py"
  ]
}

**Estímulo:** Fixar versões, ambiente e dados; verificar todos os QS e budget aplicável. Separar erro de setup de violação do produto; conservar medições desfavoráveis.

**Predicado e observáveis:**
```json
{
  "predicate": "Manifestos máquina/humano saem de uma fonte.",
  "observable_paths": [
    "release/profiles.json",
    "release/capabilities.json",
    "scripts/release/check_profile.py"
  ],
  "required_fields": [
    "assertion_id",
    "expected",
    "observed",
    "artifact_refs",
    "outcome",
    "verifier_identity"
  ],
  "verifier_kind": "structured_review_of_observables"
}
```

**Evidência:** resultado imutável por sujeito/assertion/célula/attempt, bytes referenciados e identidade do verificador. Uma suite verde sem esta assertion não fecha o critério.

**Oráculo SHA-256:** `22bd8c910fa167f98067e5d69c8c14e1f0b684cd7b5fe46c99fd3ee6d5715c0f`

### A-HUG-007-QS02

Família `T-HUG-007-Q`; etapa `pre_promotion`; estado `specified_not_executed`.

**Dado:** {
  "source": "Sujeito A e versões do contrato fixados; teste de descoberta usa seu artefato de fonte inspecionado, não inventa runtime.",
  "protocols": [
    "P00@3.0",
    "P12@3.0"
  ],
  "fixture_inputs": [
    "Predecessores admitidos e SHA fixado, ou checkout a obter por HUG-001; descoberta produz sua fonte/fixture, consumidores exigem a fonte qualificada.",
    "Contratos P00, P12 desta revisão; freeze runtime exigido somente para consumidores pós-HUG-059. Evidence histórica não conta como novo ensaio."
  ],
  "fixture_scope": [
    "release/profiles.json",
    "release/capabilities.json",
    "scripts/release/check_profile.py"
  ]
}

**Estímulo:** Fixar versões, ambiente e dados; verificar todos os QS e budget aplicável. Separar erro de setup de violação do produto; conservar medições desfavoráveis.

**Predicado e observáveis:**
```json
{
  "predicate": "Sem hard-coded 100% quando só slice foi qualificado.",
  "observable_paths": [
    "release/profiles.json",
    "release/capabilities.json",
    "scripts/release/check_profile.py"
  ],
  "required_fields": [
    "assertion_id",
    "expected",
    "observed",
    "artifact_refs",
    "outcome",
    "verifier_identity"
  ],
  "verifier_kind": "structured_review_of_observables"
}
```

**Evidência:** resultado imutável por sujeito/assertion/célula/attempt, bytes referenciados e identidade do verificador. Uma suite verde sem esta assertion não fecha o critério.

**Oráculo SHA-256:** `d68a4772d7dd1c885c5a072c7ccdcfbcdc49b666d1e8ed7b3bce7cd481eb18ca`

### A-HUG-007-QS03

Família `T-HUG-007-Q`; etapa `pre_promotion`; estado `specified_not_executed`.

**Dado:** {
  "source": "Sujeito A e versões do contrato fixados; teste de descoberta usa seu artefato de fonte inspecionado, não inventa runtime.",
  "protocols": [
    "P00@3.0",
    "P12@3.0"
  ],
  "fixture_inputs": [
    "Predecessores admitidos e SHA fixado, ou checkout a obter por HUG-001; descoberta produz sua fonte/fixture, consumidores exigem a fonte qualificada.",
    "Contratos P00, P12 desta revisão; freeze runtime exigido somente para consumidores pós-HUG-059. Evidence histórica não conta como novo ensaio."
  ],
  "fixture_scope": [
    "release/profiles.json",
    "release/capabilities.json",
    "scripts/release/check_profile.py"
  ]
}

**Estímulo:** Fixar versões, ambiente e dados; verificar todos os QS e budget aplicável. Separar erro de setup de violação do produto; conservar medições desfavoráveis.

**Predicado e observáveis:**
```json
{
  "predicate": "Manifestos/versionamento e leitura offline; sem efeito no runtime; documentos sem placeholder obrigatório. Validação linear no número de registros e saída de erro limitada.",
  "observable_paths": [
    "release/profiles.json",
    "release/capabilities.json",
    "scripts/release/check_profile.py"
  ],
  "required_fields": [
    "assertion_id",
    "expected",
    "observed",
    "artifact_refs",
    "outcome",
    "verifier_identity"
  ],
  "verifier_kind": "structured_review_of_observables"
}
```

**Evidência:** resultado imutável por sujeito/assertion/célula/attempt, bytes referenciados e identidade do verificador. Uma suite verde sem esta assertion não fecha o critério.

**Oráculo SHA-256:** `ab6809785c6c97e6411abbe5ac9d12f3908c7035c86005c8ee5675276e97b84e`

### A-HUG-007-INV01

Família `T-HUG-007-N`; etapa `pre_promotion`; estado `specified_not_executed`.

**Dado:** {
  "source": "Sujeito A e versões do contrato fixados; teste de descoberta usa seu artefato de fonte inspecionado, não inventa runtime.",
  "protocols": [
    "P00@3.0",
    "P12@3.0"
  ],
  "fixture_inputs": [
    "Predecessores admitidos e SHA fixado, ou checkout a obter por HUG-001; descoberta produz sua fonte/fixture, consumidores exigem a fonte qualificada.",
    "Contratos P00, P12 desta revisão; freeze runtime exigido somente para consumidores pós-HUG-059. Evidence histórica não conta como novo ensaio."
  ],
  "fixture_scope": [
    "release/profiles.json",
    "release/capabilities.json",
    "scripts/release/check_profile.py"
  ]
}

**Estímulo:** Retirar HUG-041 do perfil, remover um dependency edge ou anunciar capability órfã falha automaticamente. Executar o cenário negativo das assertions nomeadas, com estado/bytes antes e depois.

**Predicado e observáveis:**
```json
{
  "predicate": "Full não pode omitir nenhum pacote planejado.",
  "observable_paths": [
    "release/profiles.json",
    "release/capabilities.json",
    "scripts/release/check_profile.py"
  ],
  "required_fields": [
    "assertion_id",
    "expected",
    "observed",
    "artifact_refs",
    "outcome",
    "verifier_identity"
  ],
  "verifier_kind": "runtime_or_artifact_predicate"
}
```

**Evidência:** resultado imutável por sujeito/assertion/célula/attempt, bytes referenciados e identidade do verificador. Uma suite verde sem esta assertion não fecha o critério.

**Oráculo SHA-256:** `ab490bc195298d5302537aa1aeecc2b6740a9e336d2f933e30440c8d67a10bfc`

### A-HUG-007-INV02

Família `T-HUG-007-P`; etapa `pre_promotion`; estado `specified_not_executed`.

**Dado:** {
  "source": "Sujeito A e versões do contrato fixados; teste de descoberta usa seu artefato de fonte inspecionado, não inventa runtime.",
  "protocols": [
    "P00@3.0",
    "P12@3.0"
  ],
  "fixture_inputs": [
    "Predecessores admitidos e SHA fixado, ou checkout a obter por HUG-001; descoberta produz sua fonte/fixture, consumidores exigem a fonte qualificada.",
    "Contratos P00, P12 desta revisão; freeze runtime exigido somente para consumidores pós-HUG-059. Evidence histórica não conta como novo ensaio."
  ],
  "fixture_scope": [
    "release/profiles.json",
    "release/capabilities.json",
    "scripts/release/check_profile.py"
  ]
}

**Estímulo:** Validar consumidores de freeze por regra geral, protocolo com versão/digest e vínculo por WP; baseline SHA e ownership de cada test family devem ser exatos. Gerar todas as vistas de backlog único; comparar integralmente Markdown/JSON/protocolos. Capabilities full e obligations por célula são denominador fechado, sem caminhos de evidence compartilhados por overwrite. Executar ataques originais e BR-01/02/09 com controles positivos; remover assertion específica invalida seu critério, mesmo que a suite pai siga passed.

**Predicado e observáveis:**
```json
{
  "predicate": "Todos os cinco axiomas são obrigatórios em todo WP.",
  "observable_paths": [
    "release/profiles.json",
    "release/capabilities.json",
    "scripts/release/check_profile.py"
  ],
  "required_fields": [
    "assertion_id",
    "expected",
    "observed",
    "artifact_refs",
    "outcome",
    "verifier_identity"
  ],
  "verifier_kind": "runtime_or_artifact_predicate"
}
```

**Evidência:** resultado imutável por sujeito/assertion/célula/attempt, bytes referenciados e identidade do verificador. Uma suite verde sem esta assertion não fecha o critério.

**Oráculo SHA-256:** `0b45b1aff8b7b74d95f42426e43cfa40b70ba951ec8cad382682add4e71ae7e0`

### A-HUG-007-INV03

Família `T-HUG-007-P`; etapa `pre_promotion`; estado `specified_not_executed`.

**Dado:** {
  "source": "Sujeito A e versões do contrato fixados; teste de descoberta usa seu artefato de fonte inspecionado, não inventa runtime.",
  "protocols": [
    "P00@3.0",
    "P12@3.0"
  ],
  "fixture_inputs": [
    "Predecessores admitidos e SHA fixado, ou checkout a obter por HUG-001; descoberta produz sua fonte/fixture, consumidores exigem a fonte qualificada.",
    "Contratos P00, P12 desta revisão; freeze runtime exigido somente para consumidores pós-HUG-059. Evidence histórica não conta como novo ensaio."
  ],
  "fixture_scope": [
    "release/profiles.json",
    "release/capabilities.json",
    "scripts/release/check_profile.py"
  ]
}

**Estímulo:** Validar consumidores de freeze por regra geral, protocolo com versão/digest e vínculo por WP; baseline SHA e ownership de cada test family devem ser exatos. Gerar todas as vistas de backlog único; comparar integralmente Markdown/JSON/protocolos. Capabilities full e obligations por célula são denominador fechado, sem caminhos de evidence compartilhados por overwrite. Executar ataques originais e BR-01/02/09 com controles positivos; remover assertion específica invalida seu critério, mesmo que a suite pai siga passed.

**Predicado e observáveis:**
```json
{
  "predicate": "Capacidade anunciada requer evidência da versão candidata.",
  "observable_paths": [
    "release/profiles.json",
    "release/capabilities.json",
    "scripts/release/check_profile.py"
  ],
  "required_fields": [
    "assertion_id",
    "expected",
    "observed",
    "artifact_refs",
    "outcome",
    "verifier_identity"
  ],
  "verifier_kind": "runtime_or_artifact_predicate"
}
```

**Evidência:** resultado imutável por sujeito/assertion/célula/attempt, bytes referenciados e identidade do verificador. Uma suite verde sem esta assertion não fecha o critério.

**Oráculo SHA-256:** `2727fa829de569c4a4925e606cb13336c432d6f78d4f27bfcc80564da723341e`

### A-HUG-007-DOD01

Família `T-HUG-007-Q`; etapa `pre_promotion`; estado `specified_not_executed`.

**Dado:** {
  "source": "Sujeito A e versões do contrato fixados; teste de descoberta usa seu artefato de fonte inspecionado, não inventa runtime.",
  "protocols": [
    "P00@3.0",
    "P12@3.0"
  ],
  "fixture_inputs": [
    "Predecessores admitidos e SHA fixado, ou checkout a obter por HUG-001; descoberta produz sua fonte/fixture, consumidores exigem a fonte qualificada.",
    "Contratos P00, P12 desta revisão; freeze runtime exigido somente para consumidores pós-HUG-059. Evidence histórica não conta como novo ensaio."
  ],
  "fixture_scope": [
    "release/profiles.json",
    "release/capabilities.json",
    "scripts/release/check_profile.py"
  ]
}

**Estímulo:** Fixar versões, ambiente e dados; verificar todos os QS e budget aplicável. Separar erro de setup de violação do produto; conservar medições desfavoráveis.

**Predicado e observáveis:**
```json
{
  "predicate": "Fechar HUG-007 somente quando a implementação/artefato de 'Registro de capacidades, perfis e denominador completo' satisfaz suas assertions e observáveis obrigatórios; uma suite verde sem resultado individual não fecha os axiomas.",
  "observable_paths": [
    "release/profiles.json",
    "release/capabilities.json",
    "scripts/release/check_profile.py"
  ],
  "required_fields": [
    "assertion_id",
    "expected",
    "observed",
    "artifact_refs",
    "outcome",
    "verifier_identity"
  ],
  "verifier_kind": "structured_review_of_observables"
}
```

**Evidência:** resultado imutável por sujeito/assertion/célula/attempt, bytes referenciados e identidade do verificador. Uma suite verde sem esta assertion não fecha o critério.

**Oráculo SHA-256:** `cf539211befe6b48eb97ba7c77f0079780c1de16bd8e56d791d6e4d181445545`

### A-HUG-007-DOD02

Família `T-HUG-007-Q`; etapa `pre_promotion`; estado `specified_not_executed`.

**Dado:** {
  "source": "Sujeito A e versões do contrato fixados; teste de descoberta usa seu artefato de fonte inspecionado, não inventa runtime.",
  "protocols": [
    "P00@3.0",
    "P12@3.0"
  ],
  "fixture_inputs": [
    "Predecessores admitidos e SHA fixado, ou checkout a obter por HUG-001; descoberta produz sua fonte/fixture, consumidores exigem a fonte qualificada.",
    "Contratos P00, P12 desta revisão; freeze runtime exigido somente para consumidores pós-HUG-059. Evidence histórica não conta como novo ensaio."
  ],
  "fixture_scope": [
    "release/profiles.json",
    "release/capabilities.json",
    "scripts/release/check_profile.py"
  ]
}

**Estímulo:** Fixar versões, ambiente e dados; verificar todos os QS e budget aplicável. Separar erro de setup de violação do produto; conservar medições desfavoráveis.

**Predicado e observáveis:**
```json
{
  "predicate": "Todos os outputs release/profiles.json, release/capabilities.json, scripts/release/check_profile.py entregues nos write sets resolvidos; erros de P00, P12 e mudanças de interface revisados pelo owner do contrato.",
  "observable_paths": [
    "release/profiles.json",
    "release/capabilities.json",
    "scripts/release/check_profile.py"
  ],
  "required_fields": [
    "assertion_id",
    "expected",
    "observed",
    "artifact_refs",
    "outcome",
    "verifier_identity"
  ],
  "verifier_kind": "structured_review_of_observables"
}
```

**Evidência:** resultado imutável por sujeito/assertion/célula/attempt, bytes referenciados e identidade do verificador. Uma suite verde sem esta assertion não fecha o critério.

**Oráculo SHA-256:** `0ab5e92f48a77c2ffb850dfc552c914c216f4835c427480a87a4642c51a80c92`

### A-HUG-007-DOD03

Família `T-HUG-007-R`; etapa `pre_promotion`; estado `specified_not_executed`.

**Dado:** {
  "source": "Sujeito A e versões do contrato fixados; teste de descoberta usa seu artefato de fonte inspecionado, não inventa runtime.",
  "protocols": [
    "P00@3.0",
    "P12@3.0"
  ],
  "fixture_inputs": [
    "Predecessores admitidos e SHA fixado, ou checkout a obter por HUG-001; descoberta produz sua fonte/fixture, consumidores exigem a fonte qualificada.",
    "Contratos P00, P12 desta revisão; freeze runtime exigido somente para consumidores pós-HUG-059. Evidence histórica não conta como novo ensaio."
  ],
  "fixture_scope": [
    "release/profiles.json",
    "release/capabilities.json",
    "scripts/release/check_profile.py"
  ]
}

**Estímulo:** Interromper nas fronteiras qualificadas; executar: Revogar apenas claims do manifesto incorreto, reter último perfil válido e bloquear publicação até cobertura restaurada; não remover silenciosamente o pacote ausente do escopo full.

**Predicado e observáveis:**
```json
{
  "predicate": "Recovery deste pacote ensaiado: Revogar apenas claims do manifesto incorreto, reter último perfil válido e bloquear publicação até cobertura restaurada; não remover silenciosamente o pacote ausente do escopo full.",
  "observable_paths": [
    "release/profiles.json",
    "release/capabilities.json",
    "scripts/release/check_profile.py"
  ],
  "required_fields": [
    "assertion_id",
    "expected",
    "observed",
    "artifact_refs",
    "outcome",
    "verifier_identity"
  ],
  "verifier_kind": "structured_review_of_observables"
}
```

**Evidência:** resultado imutável por sujeito/assertion/célula/attempt, bytes referenciados e identidade do verificador. Uma suite verde sem esta assertion não fecha o critério.

**Oráculo SHA-256:** `b36f0ebab95085633ae8a6a19a0fc59aedc2e61cc4fb8a53e649e28cbe97cfa6`

### A-HUG-007-DOD04

Família `T-HUG-007-Q`; etapa `pre_promotion`; estado `specified_not_executed`.

**Dado:** {
  "source": "Sujeito A e versões do contrato fixados; teste de descoberta usa seu artefato de fonte inspecionado, não inventa runtime.",
  "protocols": [
    "P00@3.0",
    "P12@3.0"
  ],
  "fixture_inputs": [
    "Predecessores admitidos e SHA fixado, ou checkout a obter por HUG-001; descoberta produz sua fonte/fixture, consumidores exigem a fonte qualificada.",
    "Contratos P00, P12 desta revisão; freeze runtime exigido somente para consumidores pós-HUG-059. Evidence histórica não conta como novo ensaio."
  ],
  "fixture_scope": [
    "release/profiles.json",
    "release/capabilities.json",
    "scripts/release/check_profile.py"
  ]
}

**Estímulo:** Fixar versões, ambiente e dados; verificar todos os QS e budget aplicável. Separar erro de setup de violação do produto; conservar medições desfavoráveis.

**Predicado e observáveis:**
```json
{
  "predicate": "Qualificar HUG-007 sobre o sujeito A identificado por digest; anexar em B assertions verificadas e aprovação de revisor diferente do autor. Resultados não alteram A; revalidar no candidato final as obrigações atingidas. Nenhum axioma obrigatório dispensado.",
  "observable_paths": [
    "release/profiles.json",
    "release/capabilities.json",
    "scripts/release/check_profile.py"
  ],
  "required_fields": [
    "assertion_id",
    "expected",
    "observed",
    "artifact_refs",
    "outcome",
    "verifier_identity"
  ],
  "verifier_kind": "structured_review_of_observables"
}
```

**Evidência:** resultado imutável por sujeito/assertion/célula/attempt, bytes referenciados e identidade do verificador. Uma suite verde sem esta assertion não fecha o critério.

**Oráculo SHA-256:** `2558639a30444b18eb74e011ae382643da9ce6baacc781827466e8f0f3626fc0`

### A-T-AR-13-C-contract

Família `T-AR-13-C`; etapa `pre_promotion`; estado `specified_not_executed`.

**Dado:** {
  "fixture_inputs": [
    "Implementações dos pacotes responsáveis integradas e contrato freeze.",
    "Counterexample do review preservado; não usar plano como evidence de runtime."
  ]
}

**Estímulo:** Executar todos os 54 ancestrais de HUG-056 e depois HUG-056. O grafo está satisfeito e HUG-041 segue planned. Isso não afirma que a release já foi publicada errada; demonstra divergência entre escopo do plano e orquestração. Criar release profile machine-readable com required_capabilities→required_packages e gate de fechamento. Para publicação do escopo completo, HUG-041 precisa estar no conjunto obrigatório. Para perfil core, deve estar explicitamente fora e não ser anunciado como concluído. Não usar apenas aciclicidade como validação semântica.

**Predicado e observáveis:**
```json
{
  "predicate": "Teste automático detecta capacidade anunciada sem pacote/gate concluído. Mutação que remove uma dependência necessária falha. Perfil full inclui todos os 56 pacotes ou explica substituição formal; perfil menor não se apresenta como 100% do escopo completo.",
  "required_fields": [
    "expected",
    "observed",
    "artifact_refs",
    "verifier_identity"
  ],
  "verifier_kind": "contract_observables"
}
```

**Evidência:** resultado imutável por sujeito/assertion/célula/attempt, bytes referenciados e identidade do verificador. Uma suite verde sem esta assertion não fecha o critério.

**Oráculo SHA-256:** `5c87ba21ed71ad3095d7ba11510270f0d46e3cc54eb26ef898a70da97db28dce`

## Famílias e células de aceitação

### T-AR-13-C

Owner da família: HUG-007. Células: artifact-review. Etapa: pre_promotion.

**Preparo:** Implementações dos pacotes responsáveis integradas e contrato freeze. Counterexample do review preservado; não usar plano como evidence de runtime.

**Execução:** Executar todos os 54 ancestrais de HUG-056 e depois HUG-056. O grafo está satisfeito e HUG-041 segue planned. Isso não afirma que a release já foi publicada errada; demonstra divergência entre escopo do plano e orquestração. Criar release profile machine-readable com required_capabilities→required_packages e gate de fechamento. Para publicação do escopo completo, HUG-041 precisa estar no conjunto obrigatório. Para perfil core, deve estar explicitamente fora e não ser anunciado como concluído. Não usar apenas aciclicidade como validação semântica.

**Esperado:** Teste automático detecta capacidade anunciada sem pacote/gate concluído. Mutação que remove uma dependência necessária falha. Perfil full inclui todos os 56 pacotes ou explica substituição formal; perfil menor não se apresenta como 100% do escopo completo.

**Artifact template:** `evidence/results/T-AR-13-C/{subject_digest}/{cell_id}/{attempt_id}/result.json`

### T-HUG-007-N

Owner da família: HUG-007. Células: artifact-review. Etapa: pre_promotion.

**Preparo:** Predecessores admitidos e SHA fixado, ou checkout a obter por HUG-001; descoberta produz sua fonte/fixture, consumidores exigem a fonte qualificada. Contratos P00, P12 desta revisão; freeze runtime exigido somente para consumidores pós-HUG-059. Evidence histórica não conta como novo ensaio.

**Execução:** Retirar HUG-041 do perfil, remover um dependency edge ou anunciar capability órfã falha automaticamente. Executar o cenário negativo das assertions nomeadas, com estado/bytes antes e depois.

**Esperado:** Retirar HUG-041 do perfil, remover um dependency edge ou anunciar capability órfã falha automaticamente.

**Artifact template:** `evidence/results/T-HUG-007-N/{subject_digest}/{cell_id}/{attempt_id}/result.json`

### T-HUG-007-P

Owner da família: HUG-007. Células: artifact-review. Etapa: pre_promotion.

**Preparo:** Predecessores admitidos e SHA fixado, ou checkout a obter por HUG-001; descoberta produz sua fonte/fixture, consumidores exigem a fonte qualificada. Contratos P00, P12 desta revisão; freeze runtime exigido somente para consumidores pós-HUG-059. Evidence histórica não conta como novo ensaio.

**Execução:** Validar consumidores de freeze por regra geral, protocolo com versão/digest e vínculo por WP; baseline SHA e ownership de cada test family devem ser exatos. Gerar todas as vistas de backlog único; comparar integralmente Markdown/JSON/protocolos. Capabilities full e obligations por célula são denominador fechado, sem caminhos de evidence compartilhados por overwrite. Executar ataques originais e BR-01/02/09 com controles positivos; remover assertion específica invalida seu critério, mesmo que a suite pai siga passed.

**Esperado:** Perfil full exige todos os 60 IDs e testes associados; perfil de slice exige fechamento das dependências sem anunciar o restante.

**Artifact template:** `evidence/results/T-HUG-007-P/{subject_digest}/{cell_id}/{attempt_id}/result.json`

### T-HUG-007-Q

Owner da família: HUG-007. Células: artifact-review. Etapa: pre_promotion.

**Preparo:** Predecessores admitidos e SHA fixado, ou checkout a obter por HUG-001; descoberta produz sua fonte/fixture, consumidores exigem a fonte qualificada. Contratos P00, P12 desta revisão; freeze runtime exigido somente para consumidores pós-HUG-059. Evidence histórica não conta como novo ensaio.

**Execução:** Fixar versões, ambiente e dados; verificar todos os QS e budget aplicável. Separar erro de setup de violação do produto; conservar medições desfavoráveis.

**Esperado:** Manifestos máquina/humano saem de uma fonte.; Sem hard-coded 100% quando só slice foi qualificado.; Manifestos/versionamento e leitura offline; sem efeito no runtime; documentos sem placeholder obrigatório. Validação linear no número de registros e saída de erro limitada.

**Artifact template:** `evidence/results/T-HUG-007-Q/{subject_digest}/{cell_id}/{attempt_id}/result.json`

### T-HUG-007-R

Owner da família: HUG-007. Células: artifact-review. Etapa: pre_promotion.

**Preparo:** Predecessores admitidos e SHA fixado, ou checkout a obter por HUG-001; descoberta produz sua fonte/fixture, consumidores exigem a fonte qualificada. Contratos P00, P12 desta revisão; freeze runtime exigido somente para consumidores pós-HUG-059. Evidence histórica não conta como novo ensaio.

**Execução:** Interromper nas fronteiras qualificadas; executar: Revogar apenas claims do manifesto incorreto, reter último perfil válido e bloquear publicação até cobertura restaurada; não remover silenciosamente o pacote ausente do escopo full.

**Esperado:** Revogar apenas claims do manifesto incorreto, reter último perfil válido e bloquear publicação até cobertura restaurada; não remover silenciosamente o pacote ausente do escopo full.

**Artifact template:** `evidence/results/T-HUG-007-R/{subject_digest}/{cell_id}/{attempt_id}/result.json`

**Fechamento:** cinco grupos de axiomas demonstrados por assertions verificadas no candidato integrado, sem waiver. HUG-056 fecha somente após C e pós-publicação; demais estados seguem P00/P12.
