# HUG-013 — Perfil de reuso útil e enumerado: outline puro e motores nativos

**Estado:** planned; **owner:** C; **fase:** E; **prioridade:** full_scope.

**Dependências:** HUG-011, HUG-012, HUG-017, HUG-020

**Congelamento de runtime requerido:** true

**Revisões:** AR-11, AR-15

**Contratos vinculados:**
- P00@3.0 — sha256 `5d737267087e24bbcca9548488c719f4cca46fa3435a0ccf31b7b98696fde5cc`
- P04@3.0 — sha256 `e968bb6e71fbb085c3a94f2a706f1573b437f9355dfec1325c7c04c17de43bf9`
- P09@3.0 — sha256 `46cda356f2a1682342d7470e649ed81c902f659d77b0b67ef8b84cf5f1a251ad`

## Prontidão

Todos os predecessores possuem evidência integrada e interfaces versionadas compatíveis.

Owner C aceita write-set exato e budget B-CORE; HUG-001 distingue arquivos existentes dos novos.

Os casos P/N/Q/R e os oráculos estão especificados antes do dispatch; fixtures e testes executáveis são entregues pelo próprio pacote ou predecessor. Documentação/descoberta usa oráculos de artefato, não um falso bug de runtime.

Qualquer dado de fonte ainda não observado bloqueia o consumidor; descobrir no pacote predecessor, sem inventar payload.

Freeze runtime de HUG-059 já integrado é obrigatório.

## Outputs e ownership

Caminhos propostos: HUG-001 precisa vincular sua presença/criação ao checkout real antes da implementação. Evidências B/C ficam fora do sujeito A.

- `crates/hugit-checks/src/reuse_policy.rs`
- `crates/hugit-symbols/src/cached_outline.rs`
- `conformance/reuse-profiles-v2.json`

**Leituras:** `crates/hugit-checks/src/client/`, `crates/hugit-cli/src/checks/run.rs`, `crates/hugit-contracts/`

**Recursos exclusivos:** path:crates/hugit-checks/src/reuse_policy.rs, path:crates/hugit-symbols/src/cached_outline.rs, path:conformance/reuse-profiles-v2.json

## Interfaces e erros

```json
{
  "interfaces": [
    {
      "protocol": "P00",
      "interfaces": [
        "validate_plan(plan)->validation_report",
        "dispatch(wp,source_manifest,dependency_evidence)->admission",
        "qualify(profile,candidate,evidence)->readiness"
      ]
    },
    {
      "protocol": "P04",
      "interfaces": [
        "resolve_origin(source_store,session,generation)->stable_namespace",
        "record_delivery(origin_namespace,source_event_id,collector_instance_id,delivery_id)->event_or_replay",
        "canonical_identity(kind,fields)->typed_id",
        "bind(run,goal_revision,evidence)->binding"
      ]
    },
    {
      "protocol": "P09",
      "interfaces": [
        "reuse_decision(action,inputs,outputs,policy)->eligible_or_reason",
        "outline_v1(bytes,grammar,options)->json_artifact",
        "offer_native_cache(record)->native_decision"
      ]
    }
  ],
  "errors": [
    {
      "protocol": "P00",
      "codes": [
        "PLAN_INVALID",
        "DEPENDENCY_UNPROVEN",
        "CONTRACT_NOT_FROZEN",
        "SOURCE_DRIFT",
        "PROFILE_INCOMPLETE",
        "PROTOCOL_DIGEST_MISMATCH",
        "ASSERTION_UNCOVERED",
        "NORMATIVE_VIEW_DRIFT",
        "TEST_OWNER_MISMATCH"
      ]
    },
    {
      "protocol": "P04",
      "codes": [
        "IDENTITY_CONFLICT",
        "UNASSIGNED",
        "AMBIGUOUS_SOURCE",
        "UNSUPPORTED_SCHEMA",
        "PATH_COLLISION",
        "INVALID_CANONICAL_VALUE",
        "SOURCE_CONTINUITY_UNKNOWN",
        "EMISSION_ID_COLLISION"
      ]
    },
    {
      "protocol": "P09",
      "codes": [
        "REUSE_DENIED",
        "UNKNOWN_INPUT_CLOSURE",
        "OUTPUT_MISSING",
        "POLICY_INCOMPATIBLE",
        "NONDETERMINISTIC_CLASS",
        "NATIVE_REUSE_UNKNOWN"
      ]
    }
  ]
}
```

## Unidades verificáveis

1. **HUG-013.S1** — Qualificar outline-v1: função em processo sobre bytes+linguagem+versões parser/gramática+opções+versão do produtor; saída é JSON preservado, sem efeito externo exigido.
2. **HUG-013.S2** — Manter shell tests, rede, relógio, randomness e bancos fora do allowlist. BuildKit/sccache recebem native_tool_decides, nunca autoridade genérica do Hugit.
3. **HUG-013.S3** — Uma action elegível exige inputs completos, implementação fixada, output disponível e mesma política; política é código/config de usuário, não texto importado.

## Completeness criteria

**HUG-013-CC01 — obrigatório.** Allowlist concreta implementada, não lista de desejos.

Assertions: `A-HUG-013-CC01`. Famílias: T-HUG-013-P.

**HUG-013-CC02 — obrigatório.** Pelo menos um trabalho real é reaproveitado e outputs restaurados/consultáveis.

Assertions: `A-HUG-013-CC02`. Famílias: T-HUG-013-P.

**HUG-013-CC03 — obrigatório.** Checks comuns não elegíveis continuam observáveis com razão de recusa.

Assertions: `A-HUG-013-CC03`. Famílias: T-HUG-013-P.

## Success criteria

**HUG-013-SC01 — obrigatório.** Outline de arquivo real tem MISS depois HIT com zero recomputação e JSON idêntico; mutar grammar/options/input invalida; output ausente força recomputar.

Assertions: `A-HUG-013-SC01`. Famílias: T-HUG-013-P.

**HUG-013-SC02 — obrigatório.** Command shell disfarçado de outline, PATH estável com binário trocado, test flaky ou efeito fora dos outputs não entram em cache seguro.

Assertions: `A-HUG-013-SC02`. Famílias: T-HUG-013-N.

## Quality standards

**HUG-013-QS01 — obrigatório.** Não usar classificação por LLM para decidir pureza.

Assertions: `A-HUG-013-QS01`. Famílias: T-HUG-013-Q.

**HUG-013-QS02 — obrigatório.** Vetor da action key usa encoding canônico e toda versão que muda resultado.

Assertions: `A-HUG-013-QS02`. Famílias: T-HUG-013-Q.

**HUG-013-QS03 — obrigatório.** P11: read default 8 KiB, page 64 KiB, request 1 MiB; DB/transações/filas bounded; admission antes de novos bytes.

Assertions: `A-HUG-013-QS03`. Famílias: T-HUG-013-Q.

## Definition of Done — DoD

**HUG-013-DOD01 — obrigatório.** Fechar HUG-013 somente quando a implementação/artefato de 'Perfil de reuso útil e enumerado: outline puro e motores nativos' satisfaz suas assertions e observáveis obrigatórios; uma suite verde sem resultado individual não fecha os axiomas.

Assertions: `A-HUG-013-DOD01`. Famílias: T-HUG-013-Q.

**HUG-013-DOD02 — obrigatório.** Todos os outputs crates/hugit-checks/src/reuse_policy.rs, crates/hugit-symbols/src/cached_outline.rs, conformance/reuse-profiles-v2.json entregues nos write sets resolvidos; erros de P00, P04, P09 e mudanças de interface revisados pelo owner do contrato.

Assertions: `A-HUG-013-DOD02`. Famílias: T-HUG-013-Q.

**HUG-013-DOD03 — obrigatório.** Recovery deste pacote ensaiado: Revogar perfil por policy epoch, invalidar elegibilidade e reexecutar cálculo; preservar resultados históricos. Nunca restaurar automaticamente side effects ausentes.

Assertions: `A-HUG-013-DOD03`. Famílias: T-HUG-013-R.

**HUG-013-DOD04 — obrigatório.** Qualificar HUG-013 sobre o sujeito A identificado por digest; anexar em B assertions verificadas e aprovação de revisor diferente do autor. Resultados não alteram A; revalidar no candidato final as obrigações atingidas. Nenhum axioma obrigatório dispensado.

Assertions: `A-HUG-013-DOD04`. Famílias: T-HUG-013-Q.

## Invariants

**HUG-013-INV01 — obrigatório.** policy_eligible é decisão interna, nunca payload livre.

Assertions: `A-HUG-013-INV01`. Famílias: T-HUG-013-N.

**HUG-013-INV02 — obrigatório.** Falha de probe ou output missing nega skip.

Assertions: `A-HUG-013-INV02`. Famílias: T-HUG-013-N.

**HUG-013-INV03 — obrigatório.** Native cache validity pertence ao motor qualificado.

Assertions: `A-HUG-013-INV03`. Famílias: T-HUG-013-P.

## Budget e recuperação

P11: read default 8 KiB, page 64 KiB, request 1 MiB; DB/transações/filas bounded; admission antes de novos bytes.

**Rollback/recovery:** Revogar perfil por policy epoch, invalidar elegibilidade e reexecutar cálculo; preservar resultados históricos. Nunca restaurar automaticamente side effects ausentes.

**Rollout:** habilitar só depois das assertions, qualificação de célula e revisão. Flag não anuncia recurso não qualificado.

**Plano de evidências:** pacote B/C destacado do sujeito imutável A; não commitar resultados em A para exigir seu próprio hash.

## Assertions deste pacote

### A-HUG-013-CC01

Família `T-HUG-013-P`; etapa `pre_promotion`; estado `specified_not_executed`.

**Dado:** {
  "source": "Sujeito A e versões do contrato fixados; teste de descoberta usa seu artefato de fonte inspecionado, não inventa runtime.",
  "protocols": [
    "P00@3.0",
    "P04@3.0",
    "P09@3.0"
  ],
  "fixture_inputs": [
    "Predecessores admitidos e SHA fixado, ou checkout a obter por HUG-001; descoberta produz sua fonte/fixture, consumidores exigem a fonte qualificada.",
    "Contratos P00, P04, P09 desta revisão; freeze runtime exigido somente para consumidores pós-HUG-059. Evidence histórica não conta como novo ensaio."
  ],
  "fixture_scope": [
    "crates/hugit-checks/src/reuse_policy.rs",
    "crates/hugit-symbols/src/cached_outline.rs",
    "conformance/reuse-profiles-v2.json"
  ]
}

**Estímulo:** Qualificar outline-v1: função em processo sobre bytes+linguagem+versões parser/gramática+opções+versão do produtor; saída é JSON preservado, sem efeito externo exigido. Manter shell tests, rede, relógio, randomness e bancos fora do allowlist. BuildKit/sccache recebem native_tool_decides, nunca autoridade genérica do Hugit. Uma action elegível exige inputs completos, implementação fixada, output disponível e mesma política; política é código/config de usuário, não texto importado.

**Predicado e observáveis:**
```json
{
  "predicate": "Allowlist concreta implementada, não lista de desejos.",
  "observable_paths": [
    "crates/hugit-checks/src/reuse_policy.rs",
    "crates/hugit-symbols/src/cached_outline.rs",
    "conformance/reuse-profiles-v2.json"
  ],
  "required_fields": [
    "assertion_id",
    "expected",
    "observed",
    "artifact_refs",
    "outcome",
    "verifier_identity"
  ],
  "verifier_kind": "structured_review_of_observables"
}
```

**Evidência:** resultado imutável por sujeito/assertion/célula/attempt, bytes referenciados e identidade do verificador. Uma suite verde sem esta assertion não fecha o critério.

**Oráculo SHA-256:** `b3b93e61cd88bdcd89b79dc374a9ee897d8dd04bd1e4b9624429816c3a9b031b`

### A-HUG-013-CC02

Família `T-HUG-013-P`; etapa `pre_promotion`; estado `specified_not_executed`.

**Dado:** {
  "source": "Sujeito A e versões do contrato fixados; teste de descoberta usa seu artefato de fonte inspecionado, não inventa runtime.",
  "protocols": [
    "P00@3.0",
    "P04@3.0",
    "P09@3.0"
  ],
  "fixture_inputs": [
    "Predecessores admitidos e SHA fixado, ou checkout a obter por HUG-001; descoberta produz sua fonte/fixture, consumidores exigem a fonte qualificada.",
    "Contratos P00, P04, P09 desta revisão; freeze runtime exigido somente para consumidores pós-HUG-059. Evidence histórica não conta como novo ensaio."
  ],
  "fixture_scope": [
    "crates/hugit-checks/src/reuse_policy.rs",
    "crates/hugit-symbols/src/cached_outline.rs",
    "conformance/reuse-profiles-v2.json"
  ]
}

**Estímulo:** Qualificar outline-v1: função em processo sobre bytes+linguagem+versões parser/gramática+opções+versão do produtor; saída é JSON preservado, sem efeito externo exigido. Manter shell tests, rede, relógio, randomness e bancos fora do allowlist. BuildKit/sccache recebem native_tool_decides, nunca autoridade genérica do Hugit. Uma action elegível exige inputs completos, implementação fixada, output disponível e mesma política; política é código/config de usuário, não texto importado.

**Predicado e observáveis:**
```json
{
  "predicate": "Pelo menos um trabalho real é reaproveitado e outputs restaurados/consultáveis.",
  "observable_paths": [
    "crates/hugit-checks/src/reuse_policy.rs",
    "crates/hugit-symbols/src/cached_outline.rs",
    "conformance/reuse-profiles-v2.json"
  ],
  "required_fields": [
    "assertion_id",
    "expected",
    "observed",
    "artifact_refs",
    "outcome",
    "verifier_identity"
  ],
  "verifier_kind": "structured_review_of_observables"
}
```

**Evidência:** resultado imutável por sujeito/assertion/célula/attempt, bytes referenciados e identidade do verificador. Uma suite verde sem esta assertion não fecha o critério.

**Oráculo SHA-256:** `39dfbec22d70691b3b424f09a4eeb68568f5ff197a70f9ae90735cd66806842c`

### A-HUG-013-CC03

Família `T-HUG-013-P`; etapa `pre_promotion`; estado `specified_not_executed`.

**Dado:** {
  "source": "Sujeito A e versões do contrato fixados; teste de descoberta usa seu artefato de fonte inspecionado, não inventa runtime.",
  "protocols": [
    "P00@3.0",
    "P04@3.0",
    "P09@3.0"
  ],
  "fixture_inputs": [
    "Predecessores admitidos e SHA fixado, ou checkout a obter por HUG-001; descoberta produz sua fonte/fixture, consumidores exigem a fonte qualificada.",
    "Contratos P00, P04, P09 desta revisão; freeze runtime exigido somente para consumidores pós-HUG-059. Evidence histórica não conta como novo ensaio."
  ],
  "fixture_scope": [
    "crates/hugit-checks/src/reuse_policy.rs",
    "crates/hugit-symbols/src/cached_outline.rs",
    "conformance/reuse-profiles-v2.json"
  ]
}

**Estímulo:** Qualificar outline-v1: função em processo sobre bytes+linguagem+versões parser/gramática+opções+versão do produtor; saída é JSON preservado, sem efeito externo exigido. Manter shell tests, rede, relógio, randomness e bancos fora do allowlist. BuildKit/sccache recebem native_tool_decides, nunca autoridade genérica do Hugit. Uma action elegível exige inputs completos, implementação fixada, output disponível e mesma política; política é código/config de usuário, não texto importado.

**Predicado e observáveis:**
```json
{
  "predicate": "Checks comuns não elegíveis continuam observáveis com razão de recusa.",
  "observable_paths": [
    "crates/hugit-checks/src/reuse_policy.rs",
    "crates/hugit-symbols/src/cached_outline.rs",
    "conformance/reuse-profiles-v2.json"
  ],
  "required_fields": [
    "assertion_id",
    "expected",
    "observed",
    "artifact_refs",
    "outcome",
    "verifier_identity"
  ],
  "verifier_kind": "structured_review_of_observables"
}
```

**Evidência:** resultado imutável por sujeito/assertion/célula/attempt, bytes referenciados e identidade do verificador. Uma suite verde sem esta assertion não fecha o critério.

**Oráculo SHA-256:** `2bbc348c18478985f4c1ce2fc6a4c6139769c44a43ef308e7eb229706783b674`

### A-HUG-013-SC01

Família `T-HUG-013-P`; etapa `pre_promotion`; estado `specified_not_executed`.

**Dado:** {
  "source": "Sujeito A e versões do contrato fixados; teste de descoberta usa seu artefato de fonte inspecionado, não inventa runtime.",
  "protocols": [
    "P00@3.0",
    "P04@3.0",
    "P09@3.0"
  ],
  "fixture_inputs": [
    "Predecessores admitidos e SHA fixado, ou checkout a obter por HUG-001; descoberta produz sua fonte/fixture, consumidores exigem a fonte qualificada.",
    "Contratos P00, P04, P09 desta revisão; freeze runtime exigido somente para consumidores pós-HUG-059. Evidence histórica não conta como novo ensaio."
  ],
  "fixture_scope": [
    "crates/hugit-checks/src/reuse_policy.rs",
    "crates/hugit-symbols/src/cached_outline.rs",
    "conformance/reuse-profiles-v2.json"
  ]
}

**Estímulo:** Qualificar outline-v1: função em processo sobre bytes+linguagem+versões parser/gramática+opções+versão do produtor; saída é JSON preservado, sem efeito externo exigido. Manter shell tests, rede, relógio, randomness e bancos fora do allowlist. BuildKit/sccache recebem native_tool_decides, nunca autoridade genérica do Hugit. Uma action elegível exige inputs completos, implementação fixada, output disponível e mesma política; política é código/config de usuário, não texto importado.

**Predicado e observáveis:**
```json
{
  "predicate": "Outline de arquivo real tem MISS depois HIT com zero recomputação e JSON idêntico; mutar grammar/options/input invalida; output ausente força recomputar.",
  "observable_paths": [
    "crates/hugit-checks/src/reuse_policy.rs",
    "crates/hugit-symbols/src/cached_outline.rs",
    "conformance/reuse-profiles-v2.json"
  ],
  "required_fields": [
    "assertion_id",
    "expected",
    "observed",
    "artifact_refs",
    "outcome",
    "verifier_identity"
  ],
  "verifier_kind": "runtime_or_artifact_predicate"
}
```

**Evidência:** resultado imutável por sujeito/assertion/célula/attempt, bytes referenciados e identidade do verificador. Uma suite verde sem esta assertion não fecha o critério.

**Oráculo SHA-256:** `df79919b38b1e8d2f320d717add242ef071539cb72680d181a41aec076a36ab7`

### A-HUG-013-SC02

Família `T-HUG-013-N`; etapa `pre_promotion`; estado `specified_not_executed`.

**Dado:** {
  "source": "Sujeito A e versões do contrato fixados; teste de descoberta usa seu artefato de fonte inspecionado, não inventa runtime.",
  "protocols": [
    "P00@3.0",
    "P04@3.0",
    "P09@3.0"
  ],
  "fixture_inputs": [
    "Predecessores admitidos e SHA fixado, ou checkout a obter por HUG-001; descoberta produz sua fonte/fixture, consumidores exigem a fonte qualificada.",
    "Contratos P00, P04, P09 desta revisão; freeze runtime exigido somente para consumidores pós-HUG-059. Evidence histórica não conta como novo ensaio."
  ],
  "fixture_scope": [
    "crates/hugit-checks/src/reuse_policy.rs",
    "crates/hugit-symbols/src/cached_outline.rs",
    "conformance/reuse-profiles-v2.json"
  ]
}

**Estímulo:** Command shell disfarçado de outline, PATH estável com binário trocado, test flaky ou efeito fora dos outputs não entram em cache seguro. Executar o cenário negativo das assertions nomeadas, com estado/bytes antes e depois.

**Predicado e observáveis:**
```json
{
  "predicate": "Command shell disfarçado de outline, PATH estável com binário trocado, test flaky ou efeito fora dos outputs não entram em cache seguro.",
  "observable_paths": [
    "crates/hugit-checks/src/reuse_policy.rs",
    "crates/hugit-symbols/src/cached_outline.rs",
    "conformance/reuse-profiles-v2.json"
  ],
  "required_fields": [
    "assertion_id",
    "expected",
    "observed",
    "artifact_refs",
    "outcome",
    "verifier_identity"
  ],
  "verifier_kind": "runtime_or_artifact_predicate"
}
```

**Evidência:** resultado imutável por sujeito/assertion/célula/attempt, bytes referenciados e identidade do verificador. Uma suite verde sem esta assertion não fecha o critério.

**Oráculo SHA-256:** `dcafbdb58e20064fb7096070753083bce061428dc89ca4a03033f53a82a922c1`

### A-HUG-013-QS01

Família `T-HUG-013-Q`; etapa `pre_promotion`; estado `specified_not_executed`.

**Dado:** {
  "source": "Sujeito A e versões do contrato fixados; teste de descoberta usa seu artefato de fonte inspecionado, não inventa runtime.",
  "protocols": [
    "P00@3.0",
    "P04@3.0",
    "P09@3.0"
  ],
  "fixture_inputs": [
    "Predecessores admitidos e SHA fixado, ou checkout a obter por HUG-001; descoberta produz sua fonte/fixture, consumidores exigem a fonte qualificada.",
    "Contratos P00, P04, P09 desta revisão; freeze runtime exigido somente para consumidores pós-HUG-059. Evidence histórica não conta como novo ensaio."
  ],
  "fixture_scope": [
    "crates/hugit-checks/src/reuse_policy.rs",
    "crates/hugit-symbols/src/cached_outline.rs",
    "conformance/reuse-profiles-v2.json"
  ]
}

**Estímulo:** Fixar versões, ambiente e dados; verificar todos os QS e budget aplicável. Separar erro de setup de violação do produto; conservar medições desfavoráveis.

**Predicado e observáveis:**
```json
{
  "predicate": "Não usar classificação por LLM para decidir pureza.",
  "observable_paths": [
    "crates/hugit-checks/src/reuse_policy.rs",
    "crates/hugit-symbols/src/cached_outline.rs",
    "conformance/reuse-profiles-v2.json"
  ],
  "required_fields": [
    "assertion_id",
    "expected",
    "observed",
    "artifact_refs",
    "outcome",
    "verifier_identity"
  ],
  "verifier_kind": "structured_review_of_observables"
}
```

**Evidência:** resultado imutável por sujeito/assertion/célula/attempt, bytes referenciados e identidade do verificador. Uma suite verde sem esta assertion não fecha o critério.

**Oráculo SHA-256:** `4b6a8a0de8549c9fb56e6c7019c6efdd750aa6a3e3f1672f91914dd9173b8a61`

### A-HUG-013-QS02

Família `T-HUG-013-Q`; etapa `pre_promotion`; estado `specified_not_executed`.

**Dado:** {
  "source": "Sujeito A e versões do contrato fixados; teste de descoberta usa seu artefato de fonte inspecionado, não inventa runtime.",
  "protocols": [
    "P00@3.0",
    "P04@3.0",
    "P09@3.0"
  ],
  "fixture_inputs": [
    "Predecessores admitidos e SHA fixado, ou checkout a obter por HUG-001; descoberta produz sua fonte/fixture, consumidores exigem a fonte qualificada.",
    "Contratos P00, P04, P09 desta revisão; freeze runtime exigido somente para consumidores pós-HUG-059. Evidence histórica não conta como novo ensaio."
  ],
  "fixture_scope": [
    "crates/hugit-checks/src/reuse_policy.rs",
    "crates/hugit-symbols/src/cached_outline.rs",
    "conformance/reuse-profiles-v2.json"
  ]
}

**Estímulo:** Fixar versões, ambiente e dados; verificar todos os QS e budget aplicável. Separar erro de setup de violação do produto; conservar medições desfavoráveis.

**Predicado e observáveis:**
```json
{
  "predicate": "Vetor da action key usa encoding canônico e toda versão que muda resultado.",
  "observable_paths": [
    "crates/hugit-checks/src/reuse_policy.rs",
    "crates/hugit-symbols/src/cached_outline.rs",
    "conformance/reuse-profiles-v2.json"
  ],
  "required_fields": [
    "assertion_id",
    "expected",
    "observed",
    "artifact_refs",
    "outcome",
    "verifier_identity"
  ],
  "verifier_kind": "structured_review_of_observables"
}
```

**Evidência:** resultado imutável por sujeito/assertion/célula/attempt, bytes referenciados e identidade do verificador. Uma suite verde sem esta assertion não fecha o critério.

**Oráculo SHA-256:** `de49e09d7fdf1203b03af89e57c792d2895c753ba0d63762629a8f2d8f3dc8b3`

### A-HUG-013-QS03

Família `T-HUG-013-Q`; etapa `pre_promotion`; estado `specified_not_executed`.

**Dado:** {
  "source": "Sujeito A e versões do contrato fixados; teste de descoberta usa seu artefato de fonte inspecionado, não inventa runtime.",
  "protocols": [
    "P00@3.0",
    "P04@3.0",
    "P09@3.0"
  ],
  "fixture_inputs": [
    "Predecessores admitidos e SHA fixado, ou checkout a obter por HUG-001; descoberta produz sua fonte/fixture, consumidores exigem a fonte qualificada.",
    "Contratos P00, P04, P09 desta revisão; freeze runtime exigido somente para consumidores pós-HUG-059. Evidence histórica não conta como novo ensaio."
  ],
  "fixture_scope": [
    "crates/hugit-checks/src/reuse_policy.rs",
    "crates/hugit-symbols/src/cached_outline.rs",
    "conformance/reuse-profiles-v2.json"
  ]
}

**Estímulo:** Fixar versões, ambiente e dados; verificar todos os QS e budget aplicável. Separar erro de setup de violação do produto; conservar medições desfavoráveis.

**Predicado e observáveis:**
```json
{
  "predicate": "P11: read default 8 KiB, page 64 KiB, request 1 MiB; DB/transações/filas bounded; admission antes de novos bytes.",
  "observable_paths": [
    "crates/hugit-checks/src/reuse_policy.rs",
    "crates/hugit-symbols/src/cached_outline.rs",
    "conformance/reuse-profiles-v2.json"
  ],
  "required_fields": [
    "assertion_id",
    "expected",
    "observed",
    "artifact_refs",
    "outcome",
    "verifier_identity"
  ],
  "verifier_kind": "structured_review_of_observables"
}
```

**Evidência:** resultado imutável por sujeito/assertion/célula/attempt, bytes referenciados e identidade do verificador. Uma suite verde sem esta assertion não fecha o critério.

**Oráculo SHA-256:** `b052576eec48007d0fe21ea49f8f7ccc7e46bf23f5c9993c8419622e8d9ef827`

### A-HUG-013-INV01

Família `T-HUG-013-N`; etapa `pre_promotion`; estado `specified_not_executed`.

**Dado:** {
  "source": "Sujeito A e versões do contrato fixados; teste de descoberta usa seu artefato de fonte inspecionado, não inventa runtime.",
  "protocols": [
    "P00@3.0",
    "P04@3.0",
    "P09@3.0"
  ],
  "fixture_inputs": [
    "Predecessores admitidos e SHA fixado, ou checkout a obter por HUG-001; descoberta produz sua fonte/fixture, consumidores exigem a fonte qualificada.",
    "Contratos P00, P04, P09 desta revisão; freeze runtime exigido somente para consumidores pós-HUG-059. Evidence histórica não conta como novo ensaio."
  ],
  "fixture_scope": [
    "crates/hugit-checks/src/reuse_policy.rs",
    "crates/hugit-symbols/src/cached_outline.rs",
    "conformance/reuse-profiles-v2.json"
  ]
}

**Estímulo:** Command shell disfarçado de outline, PATH estável com binário trocado, test flaky ou efeito fora dos outputs não entram em cache seguro. Executar o cenário negativo das assertions nomeadas, com estado/bytes antes e depois.

**Predicado e observáveis:**
```json
{
  "predicate": "policy_eligible é decisão interna, nunca payload livre.",
  "observable_paths": [
    "crates/hugit-checks/src/reuse_policy.rs",
    "crates/hugit-symbols/src/cached_outline.rs",
    "conformance/reuse-profiles-v2.json"
  ],
  "required_fields": [
    "assertion_id",
    "expected",
    "observed",
    "artifact_refs",
    "outcome",
    "verifier_identity"
  ],
  "verifier_kind": "runtime_or_artifact_predicate"
}
```

**Evidência:** resultado imutável por sujeito/assertion/célula/attempt, bytes referenciados e identidade do verificador. Uma suite verde sem esta assertion não fecha o critério.

**Oráculo SHA-256:** `0df9a5307aab38ee61f3d04180cf881e0c049964c7accb3024ba67d3f4d1b531`

### A-HUG-013-INV02

Família `T-HUG-013-N`; etapa `pre_promotion`; estado `specified_not_executed`.

**Dado:** {
  "source": "Sujeito A e versões do contrato fixados; teste de descoberta usa seu artefato de fonte inspecionado, não inventa runtime.",
  "protocols": [
    "P00@3.0",
    "P04@3.0",
    "P09@3.0"
  ],
  "fixture_inputs": [
    "Predecessores admitidos e SHA fixado, ou checkout a obter por HUG-001; descoberta produz sua fonte/fixture, consumidores exigem a fonte qualificada.",
    "Contratos P00, P04, P09 desta revisão; freeze runtime exigido somente para consumidores pós-HUG-059. Evidence histórica não conta como novo ensaio."
  ],
  "fixture_scope": [
    "crates/hugit-checks/src/reuse_policy.rs",
    "crates/hugit-symbols/src/cached_outline.rs",
    "conformance/reuse-profiles-v2.json"
  ]
}

**Estímulo:** Command shell disfarçado de outline, PATH estável com binário trocado, test flaky ou efeito fora dos outputs não entram em cache seguro. Executar o cenário negativo das assertions nomeadas, com estado/bytes antes e depois.

**Predicado e observáveis:**
```json
{
  "predicate": "Falha de probe ou output missing nega skip.",
  "observable_paths": [
    "crates/hugit-checks/src/reuse_policy.rs",
    "crates/hugit-symbols/src/cached_outline.rs",
    "conformance/reuse-profiles-v2.json"
  ],
  "required_fields": [
    "assertion_id",
    "expected",
    "observed",
    "artifact_refs",
    "outcome",
    "verifier_identity"
  ],
  "verifier_kind": "runtime_or_artifact_predicate"
}
```

**Evidência:** resultado imutável por sujeito/assertion/célula/attempt, bytes referenciados e identidade do verificador. Uma suite verde sem esta assertion não fecha o critério.

**Oráculo SHA-256:** `47d1157da53e78e89a3d5f7793a60ea6d783aae12d0a09a94758aecd2acb093e`

### A-HUG-013-INV03

Família `T-HUG-013-P`; etapa `pre_promotion`; estado `specified_not_executed`.

**Dado:** {
  "source": "Sujeito A e versões do contrato fixados; teste de descoberta usa seu artefato de fonte inspecionado, não inventa runtime.",
  "protocols": [
    "P00@3.0",
    "P04@3.0",
    "P09@3.0"
  ],
  "fixture_inputs": [
    "Predecessores admitidos e SHA fixado, ou checkout a obter por HUG-001; descoberta produz sua fonte/fixture, consumidores exigem a fonte qualificada.",
    "Contratos P00, P04, P09 desta revisão; freeze runtime exigido somente para consumidores pós-HUG-059. Evidence histórica não conta como novo ensaio."
  ],
  "fixture_scope": [
    "crates/hugit-checks/src/reuse_policy.rs",
    "crates/hugit-symbols/src/cached_outline.rs",
    "conformance/reuse-profiles-v2.json"
  ]
}

**Estímulo:** Qualificar outline-v1: função em processo sobre bytes+linguagem+versões parser/gramática+opções+versão do produtor; saída é JSON preservado, sem efeito externo exigido. Manter shell tests, rede, relógio, randomness e bancos fora do allowlist. BuildKit/sccache recebem native_tool_decides, nunca autoridade genérica do Hugit. Uma action elegível exige inputs completos, implementação fixada, output disponível e mesma política; política é código/config de usuário, não texto importado.

**Predicado e observáveis:**
```json
{
  "predicate": "Native cache validity pertence ao motor qualificado.",
  "observable_paths": [
    "crates/hugit-checks/src/reuse_policy.rs",
    "crates/hugit-symbols/src/cached_outline.rs",
    "conformance/reuse-profiles-v2.json"
  ],
  "required_fields": [
    "assertion_id",
    "expected",
    "observed",
    "artifact_refs",
    "outcome",
    "verifier_identity"
  ],
  "verifier_kind": "runtime_or_artifact_predicate"
}
```

**Evidência:** resultado imutável por sujeito/assertion/célula/attempt, bytes referenciados e identidade do verificador. Uma suite verde sem esta assertion não fecha o critério.

**Oráculo SHA-256:** `1b2784499e33cd3f4ebb0490bbf06cb380c2754d1dcb6e8716a148acff779f3c`

### A-HUG-013-DOD01

Família `T-HUG-013-Q`; etapa `pre_promotion`; estado `specified_not_executed`.

**Dado:** {
  "source": "Sujeito A e versões do contrato fixados; teste de descoberta usa seu artefato de fonte inspecionado, não inventa runtime.",
  "protocols": [
    "P00@3.0",
    "P04@3.0",
    "P09@3.0"
  ],
  "fixture_inputs": [
    "Predecessores admitidos e SHA fixado, ou checkout a obter por HUG-001; descoberta produz sua fonte/fixture, consumidores exigem a fonte qualificada.",
    "Contratos P00, P04, P09 desta revisão; freeze runtime exigido somente para consumidores pós-HUG-059. Evidence histórica não conta como novo ensaio."
  ],
  "fixture_scope": [
    "crates/hugit-checks/src/reuse_policy.rs",
    "crates/hugit-symbols/src/cached_outline.rs",
    "conformance/reuse-profiles-v2.json"
  ]
}

**Estímulo:** Fixar versões, ambiente e dados; verificar todos os QS e budget aplicável. Separar erro de setup de violação do produto; conservar medições desfavoráveis.

**Predicado e observáveis:**
```json
{
  "predicate": "Fechar HUG-013 somente quando a implementação/artefato de 'Perfil de reuso útil e enumerado: outline puro e motores nativos' satisfaz suas assertions e observáveis obrigatórios; uma suite verde sem resultado individual não fecha os axiomas.",
  "observable_paths": [
    "crates/hugit-checks/src/reuse_policy.rs",
    "crates/hugit-symbols/src/cached_outline.rs",
    "conformance/reuse-profiles-v2.json"
  ],
  "required_fields": [
    "assertion_id",
    "expected",
    "observed",
    "artifact_refs",
    "outcome",
    "verifier_identity"
  ],
  "verifier_kind": "structured_review_of_observables"
}
```

**Evidência:** resultado imutável por sujeito/assertion/célula/attempt, bytes referenciados e identidade do verificador. Uma suite verde sem esta assertion não fecha o critério.

**Oráculo SHA-256:** `21e4693bb54e547b12068c02f45bd45d261e6a5d1e97d54859fbcda1e25720da`

### A-HUG-013-DOD02

Família `T-HUG-013-Q`; etapa `pre_promotion`; estado `specified_not_executed`.

**Dado:** {
  "source": "Sujeito A e versões do contrato fixados; teste de descoberta usa seu artefato de fonte inspecionado, não inventa runtime.",
  "protocols": [
    "P00@3.0",
    "P04@3.0",
    "P09@3.0"
  ],
  "fixture_inputs": [
    "Predecessores admitidos e SHA fixado, ou checkout a obter por HUG-001; descoberta produz sua fonte/fixture, consumidores exigem a fonte qualificada.",
    "Contratos P00, P04, P09 desta revisão; freeze runtime exigido somente para consumidores pós-HUG-059. Evidence histórica não conta como novo ensaio."
  ],
  "fixture_scope": [
    "crates/hugit-checks/src/reuse_policy.rs",
    "crates/hugit-symbols/src/cached_outline.rs",
    "conformance/reuse-profiles-v2.json"
  ]
}

**Estímulo:** Fixar versões, ambiente e dados; verificar todos os QS e budget aplicável. Separar erro de setup de violação do produto; conservar medições desfavoráveis.

**Predicado e observáveis:**
```json
{
  "predicate": "Todos os outputs crates/hugit-checks/src/reuse_policy.rs, crates/hugit-symbols/src/cached_outline.rs, conformance/reuse-profiles-v2.json entregues nos write sets resolvidos; erros de P00, P04, P09 e mudanças de interface revisados pelo owner do contrato.",
  "observable_paths": [
    "crates/hugit-checks/src/reuse_policy.rs",
    "crates/hugit-symbols/src/cached_outline.rs",
    "conformance/reuse-profiles-v2.json"
  ],
  "required_fields": [
    "assertion_id",
    "expected",
    "observed",
    "artifact_refs",
    "outcome",
    "verifier_identity"
  ],
  "verifier_kind": "structured_review_of_observables"
}
```

**Evidência:** resultado imutável por sujeito/assertion/célula/attempt, bytes referenciados e identidade do verificador. Uma suite verde sem esta assertion não fecha o critério.

**Oráculo SHA-256:** `2986f8af6e2a9a8d5d305327aed8ca2fe3101d904fdb685d89b932b269f42bfa`

### A-HUG-013-DOD03

Família `T-HUG-013-R`; etapa `pre_promotion`; estado `specified_not_executed`.

**Dado:** {
  "source": "Sujeito A e versões do contrato fixados; teste de descoberta usa seu artefato de fonte inspecionado, não inventa runtime.",
  "protocols": [
    "P00@3.0",
    "P04@3.0",
    "P09@3.0"
  ],
  "fixture_inputs": [
    "Predecessores admitidos e SHA fixado, ou checkout a obter por HUG-001; descoberta produz sua fonte/fixture, consumidores exigem a fonte qualificada.",
    "Contratos P00, P04, P09 desta revisão; freeze runtime exigido somente para consumidores pós-HUG-059. Evidence histórica não conta como novo ensaio."
  ],
  "fixture_scope": [
    "crates/hugit-checks/src/reuse_policy.rs",
    "crates/hugit-symbols/src/cached_outline.rs",
    "conformance/reuse-profiles-v2.json"
  ]
}

**Estímulo:** Interromper nas fronteiras qualificadas; executar: Revogar perfil por policy epoch, invalidar elegibilidade e reexecutar cálculo; preservar resultados históricos. Nunca restaurar automaticamente side effects ausentes.

**Predicado e observáveis:**
```json
{
  "predicate": "Recovery deste pacote ensaiado: Revogar perfil por policy epoch, invalidar elegibilidade e reexecutar cálculo; preservar resultados históricos. Nunca restaurar automaticamente side effects ausentes.",
  "observable_paths": [
    "crates/hugit-checks/src/reuse_policy.rs",
    "crates/hugit-symbols/src/cached_outline.rs",
    "conformance/reuse-profiles-v2.json"
  ],
  "required_fields": [
    "assertion_id",
    "expected",
    "observed",
    "artifact_refs",
    "outcome",
    "verifier_identity"
  ],
  "verifier_kind": "structured_review_of_observables"
}
```

**Evidência:** resultado imutável por sujeito/assertion/célula/attempt, bytes referenciados e identidade do verificador. Uma suite verde sem esta assertion não fecha o critério.

**Oráculo SHA-256:** `b7372b4cad5103c54c7edfce3fe84fd4b5155a9fdc9b69651267167ecd45fd50`

### A-HUG-013-DOD04

Família `T-HUG-013-Q`; etapa `pre_promotion`; estado `specified_not_executed`.

**Dado:** {
  "source": "Sujeito A e versões do contrato fixados; teste de descoberta usa seu artefato de fonte inspecionado, não inventa runtime.",
  "protocols": [
    "P00@3.0",
    "P04@3.0",
    "P09@3.0"
  ],
  "fixture_inputs": [
    "Predecessores admitidos e SHA fixado, ou checkout a obter por HUG-001; descoberta produz sua fonte/fixture, consumidores exigem a fonte qualificada.",
    "Contratos P00, P04, P09 desta revisão; freeze runtime exigido somente para consumidores pós-HUG-059. Evidence histórica não conta como novo ensaio."
  ],
  "fixture_scope": [
    "crates/hugit-checks/src/reuse_policy.rs",
    "crates/hugit-symbols/src/cached_outline.rs",
    "conformance/reuse-profiles-v2.json"
  ]
}

**Estímulo:** Fixar versões, ambiente e dados; verificar todos os QS e budget aplicável. Separar erro de setup de violação do produto; conservar medições desfavoráveis.

**Predicado e observáveis:**
```json
{
  "predicate": "Qualificar HUG-013 sobre o sujeito A identificado por digest; anexar em B assertions verificadas e aprovação de revisor diferente do autor. Resultados não alteram A; revalidar no candidato final as obrigações atingidas. Nenhum axioma obrigatório dispensado.",
  "observable_paths": [
    "crates/hugit-checks/src/reuse_policy.rs",
    "crates/hugit-symbols/src/cached_outline.rs",
    "conformance/reuse-profiles-v2.json"
  ],
  "required_fields": [
    "assertion_id",
    "expected",
    "observed",
    "artifact_refs",
    "outcome",
    "verifier_identity"
  ],
  "verifier_kind": "structured_review_of_observables"
}
```

**Evidência:** resultado imutável por sujeito/assertion/célula/attempt, bytes referenciados e identidade do verificador. Uma suite verde sem esta assertion não fecha o critério.

**Oráculo SHA-256:** `208ace68aae2552fa6985e399bbaa8a53083b927cf7b7191c6226aa3e74bce46`

## Famílias e células de aceitação

### T-HUG-013-N

Owner da família: HUG-013. Células: linux-x64-ext4, macos-arm64-apfs, macos-intel-apfs, windows-x64-ntfs. Etapa: pre_promotion.

**Preparo:** Predecessores admitidos e SHA fixado, ou checkout a obter por HUG-001; descoberta produz sua fonte/fixture, consumidores exigem a fonte qualificada. Contratos P00, P04, P09 desta revisão; freeze runtime exigido somente para consumidores pós-HUG-059. Evidence histórica não conta como novo ensaio.

**Execução:** Command shell disfarçado de outline, PATH estável com binário trocado, test flaky ou efeito fora dos outputs não entram em cache seguro. Executar o cenário negativo das assertions nomeadas, com estado/bytes antes e depois.

**Esperado:** Command shell disfarçado de outline, PATH estável com binário trocado, test flaky ou efeito fora dos outputs não entram em cache seguro.

**Artifact template:** `evidence/results/T-HUG-013-N/{subject_digest}/{cell_id}/{attempt_id}/result.json`

### T-HUG-013-P

Owner da família: HUG-013. Células: linux-x64-ext4, macos-arm64-apfs, macos-intel-apfs, windows-x64-ntfs. Etapa: pre_promotion.

**Preparo:** Predecessores admitidos e SHA fixado, ou checkout a obter por HUG-001; descoberta produz sua fonte/fixture, consumidores exigem a fonte qualificada. Contratos P00, P04, P09 desta revisão; freeze runtime exigido somente para consumidores pós-HUG-059. Evidence histórica não conta como novo ensaio.

**Execução:** Qualificar outline-v1: função em processo sobre bytes+linguagem+versões parser/gramática+opções+versão do produtor; saída é JSON preservado, sem efeito externo exigido. Manter shell tests, rede, relógio, randomness e bancos fora do allowlist. BuildKit/sccache recebem native_tool_decides, nunca autoridade genérica do Hugit. Uma action elegível exige inputs completos, implementação fixada, output disponível e mesma política; política é código/config de usuário, não texto importado.

**Esperado:** Outline de arquivo real tem MISS depois HIT com zero recomputação e JSON idêntico; mutar grammar/options/input invalida; output ausente força recomputar.

**Artifact template:** `evidence/results/T-HUG-013-P/{subject_digest}/{cell_id}/{attempt_id}/result.json`

### T-HUG-013-Q

Owner da família: HUG-013. Células: linux-x64-ext4, macos-arm64-apfs, macos-intel-apfs, windows-x64-ntfs. Etapa: pre_promotion.

**Preparo:** Predecessores admitidos e SHA fixado, ou checkout a obter por HUG-001; descoberta produz sua fonte/fixture, consumidores exigem a fonte qualificada. Contratos P00, P04, P09 desta revisão; freeze runtime exigido somente para consumidores pós-HUG-059. Evidence histórica não conta como novo ensaio.

**Execução:** Fixar versões, ambiente e dados; verificar todos os QS e budget aplicável. Separar erro de setup de violação do produto; conservar medições desfavoráveis.

**Esperado:** Não usar classificação por LLM para decidir pureza.; Vetor da action key usa encoding canônico e toda versão que muda resultado.; P11: read default 8 KiB, page 64 KiB, request 1 MiB; DB/transações/filas bounded; admission antes de novos bytes.

**Artifact template:** `evidence/results/T-HUG-013-Q/{subject_digest}/{cell_id}/{attempt_id}/result.json`

### T-HUG-013-R

Owner da família: HUG-013. Células: linux-x64-ext4, macos-arm64-apfs, macos-intel-apfs, windows-x64-ntfs. Etapa: pre_promotion.

**Preparo:** Predecessores admitidos e SHA fixado, ou checkout a obter por HUG-001; descoberta produz sua fonte/fixture, consumidores exigem a fonte qualificada. Contratos P00, P04, P09 desta revisão; freeze runtime exigido somente para consumidores pós-HUG-059. Evidence histórica não conta como novo ensaio.

**Execução:** Interromper nas fronteiras qualificadas; executar: Revogar perfil por policy epoch, invalidar elegibilidade e reexecutar cálculo; preservar resultados históricos. Nunca restaurar automaticamente side effects ausentes.

**Esperado:** Revogar perfil por policy epoch, invalidar elegibilidade e reexecutar cálculo; preservar resultados históricos. Nunca restaurar automaticamente side effects ausentes.

**Artifact template:** `evidence/results/T-HUG-013-R/{subject_digest}/{cell_id}/{attempt_id}/result.json`

**Fechamento:** cinco grupos de axiomas demonstrados por assertions verificadas no candidato integrado, sem waiver. HUG-056 fecha somente após C e pós-publicação; demais estados seguem P00/P12.
