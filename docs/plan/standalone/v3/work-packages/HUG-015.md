# HUG-015 — Schema v2 baseado no hook real e identidades separadas

**Estado:** planned; **owner:** A; **fase:** B; **prioridade:** first_slice.

**Dependências:** HUG-002, HUG-007, HUG-057

**Congelamento de runtime requerido:** false

**Revisões:** AR-06, AR-09, AR-12, AR-15

**Contratos vinculados:**
- P00@3.0 — sha256 `5d737267087e24bbcca9548488c719f4cca46fa3435a0ccf31b7b98696fde5cc`
- P04@3.0 — sha256 `e968bb6e71fbb085c3a94f2a706f1573b437f9355dfec1325c7c04c17de43bf9`
- P05@3.0 — sha256 `9683d0acafd218be2078fba9ca22087a56ca933152d1ba62b0d930778f367564`

## Prontidão

Todos os predecessores possuem evidência integrada e interfaces versionadas compatíveis.

Owner A aceita write-set exato e budget B-CORE; HUG-001 distingue arquivos existentes dos novos.

Os casos P/N/Q/R e os oráculos estão especificados antes do dispatch; fixtures e testes executáveis são entregues pelo próprio pacote ou predecessor. Documentação/descoberta usa oráculos de artefato, não um falso bug de runtime.

Qualquer dado de fonte ainda não observado bloqueia o consumidor; descobrir no pacote predecessor, sem inventar payload.

Usar a especificação aprovada desta revisão; descobertas/contratos e contenções de baseline produzem as pré-condições do freeze, sem ativar implicitamente o runtime v2.

## Outputs e ownership

Caminhos propostos: HUG-001 precisa vincular sua presença/criação ao checkout real antes da implementação. Evidências B/C ficam fora do sujeito A.

- `crates/hugit-contracts/src/v2/events.rs`
- `crates/hugit-contracts/src/v2/identities.rs`
- `conformance/identities-v2.json`

**Leituras:** `crates/hugit-contracts/`, `conformance/`

**Recursos exclusivos:** path:crates/hugit-contracts/src/v2/events.rs, path:crates/hugit-contracts/src/v2/identities.rs, path:conformance/identities-v2.json

## Interfaces e erros

```json
{
  "interfaces": [
    {
      "protocol": "P00",
      "interfaces": [
        "validate_plan(plan)->validation_report",
        "dispatch(wp,source_manifest,dependency_evidence)->admission",
        "qualify(profile,candidate,evidence)->readiness"
      ]
    },
    {
      "protocol": "P04",
      "interfaces": [
        "resolve_origin(source_store,session,generation)->stable_namespace",
        "record_delivery(origin_namespace,source_event_id,collector_instance_id,delivery_id)->event_or_replay",
        "canonical_identity(kind,fields)->typed_id",
        "bind(run,goal_revision,evidence)->binding"
      ]
    },
    {
      "protocol": "P05",
      "interfaces": [
        "open_channel(user_config,root,adapter_version)->capabilities",
        "ingest(channel_id,payload)->trusted_envelope",
        "authorize_read(channel,object,epoch)->decision"
      ]
    }
  ],
  "errors": [
    {
      "protocol": "P00",
      "codes": [
        "PLAN_INVALID",
        "DEPENDENCY_UNPROVEN",
        "CONTRACT_NOT_FROZEN",
        "SOURCE_DRIFT",
        "PROFILE_INCOMPLETE",
        "PROTOCOL_DIGEST_MISMATCH",
        "ASSERTION_UNCOVERED",
        "NORMATIVE_VIEW_DRIFT",
        "TEST_OWNER_MISMATCH"
      ]
    },
    {
      "protocol": "P04",
      "codes": [
        "IDENTITY_CONFLICT",
        "UNASSIGNED",
        "AMBIGUOUS_SOURCE",
        "UNSUPPORTED_SCHEMA",
        "PATH_COLLISION",
        "INVALID_CANONICAL_VALUE",
        "SOURCE_CONTINUITY_UNKNOWN",
        "EMISSION_ID_COLLISION"
      ]
    },
    {
      "protocol": "P05",
      "codes": [
        "CHANNEL_NOT_AUTHORIZED",
        "ROOT_MISMATCH",
        "SOURCE_SPOOFED",
        "SAME_USER_UNTRUSTED",
        "UNSUPPORTED_ADAPTER"
      ]
    }
  ]
}
```

## Unidades verificáveis

1. **HUG-015.S1** — Separar history_id, installation_id, origin_namespace_id/source_event_id estáveis, collector_instance_id/delivery_id efêmeros, goal+revision, invocation, action, snapshot e binding; collector reiniciado nunca cria fato histórico novo.
2. **HUG-015.S2** — Separar blob lógico, representação física, ocorrência e transformação; integers/unidades/encoding/domain tags definidos em P04.
3. **HUG-015.S3** — Origem é atribuída pelo boundary autorizado; source_kind não é confiança nem permissão. Campos legados têm namespace e nível conhecido.

## Completeness criteria

**HUG-015-CC01 — obrigatório.** Schema inclui todos os tipos de P04 e versões dos codecs/parsers/policies.

Assertions: `A-HUG-015-CC01`. Famílias: T-HUG-015-P.

**HUG-015-CC02 — obrigatório.** Fixture do hook HUG-057 é representável sem inventar campo.

Assertions: `A-HUG-015-CC02`. Famílias: T-HUG-015-P.

**HUG-015-CC03 — obrigatório.** Leitor v1 separado preserva bytes e semântica legacy.

Assertions: `A-HUG-015-CC03`. Famílias: T-HUG-015-P.

## Success criteria

**HUG-015-SC01 — obrigatório.** Vetores dos adapters observados roundtrip sem perda; mesma origem/same bytes é replay, same ID/different bytes é conflito; duas instalações criam novos producers sem colidir.

Assertions: `A-HUG-015-SC01`. Famílias: T-HUG-015-P.

**HUG-015-SC02 — obrigatório.** Recompressão não muda blob lógico, redaction muda conteúdo; Unicode/case/path alias não colapsam registros ou sobrescrevem restore.

Assertions: `A-HUG-015-SC02`. Famílias: T-HUG-015-N.

## Quality standards

**HUG-015-QS01 — obrigatório.** Proibir floats/concatenação ambígua nas identidades canônicas.

Assertions: `A-HUG-015-QS01`. Famílias: T-HUG-015-Q.

**HUG-015-QS02 — obrigatório.** Nenhuma migração muda source_time em nome da ordenação.

Assertions: `A-HUG-015-QS02`. Famílias: T-HUG-015-Q.

**HUG-015-QS03 — obrigatório.** P11: read default 8 KiB, page 64 KiB, request 1 MiB; DB/transações/filas bounded; admission antes de novos bytes.

Assertions: `A-HUG-015-QS03`. Famílias: T-HUG-015-Q.

## Definition of Done — DoD

**HUG-015-DOD01 — obrigatório.** Fechar HUG-015 somente quando a implementação/artefato de 'Schema v2 baseado no hook real e identidades separadas' satisfaz suas assertions e observáveis obrigatórios; uma suite verde sem resultado individual não fecha os axiomas.

Assertions: `A-HUG-015-DOD01`. Famílias: T-HUG-015-Q.

**HUG-015-DOD02 — obrigatório.** Todos os outputs crates/hugit-contracts/src/v2/events.rs, crates/hugit-contracts/src/v2/identities.rs, conformance/identities-v2.json entregues nos write sets resolvidos; erros de P00, P04, P05 e mudanças de interface revisados pelo owner do contrato.

Assertions: `A-HUG-015-DOD02`. Famílias: T-HUG-015-Q.

**HUG-015-DOD03 — obrigatório.** Recovery deste pacote ensaiado: Publicar versão adicional do schema e manter leitores da versão já emitida; antes de qualquer write público ainda é permitido substituir draft. Depois de eventos v2, apenas migração explícita e testes de roundtrip.

Assertions: `A-HUG-015-DOD03`. Famílias: T-HUG-015-R.

**HUG-015-DOD04 — obrigatório.** Qualificar HUG-015 sobre o sujeito A identificado por digest; anexar em B assertions verificadas e aprovação de revisor diferente do autor. Resultados não alteram A; revalidar no candidato final as obrigações atingidas. Nenhum axioma obrigatório dispensado.

Assertions: `A-HUG-015-DOD04`. Famílias: T-HUG-015-Q.

## Invariants

**HUG-015-INV01 — obrigatório.** Texto de branch não vira goal original.

Assertions: `A-HUG-015-INV01`. Famílias: T-HUG-015-N.

**HUG-015-INV02 — obrigatório.** Origem/identidade não concede autorização.

Assertions: `A-HUG-015-INV02`. Famílias: T-HUG-015-N.

**HUG-015-INV03 — obrigatório.** Conteúdo e sua ocorrência têm identidades independentes.

Assertions: `A-HUG-015-INV03`. Famílias: T-HUG-015-P.

## Budget e recuperação

P11: read default 8 KiB, page 64 KiB, request 1 MiB; DB/transações/filas bounded; admission antes de novos bytes.

**Rollback/recovery:** Publicar versão adicional do schema e manter leitores da versão já emitida; antes de qualquer write público ainda é permitido substituir draft. Depois de eventos v2, apenas migração explícita e testes de roundtrip.

**Rollout:** habilitar só depois das assertions, qualificação de célula e revisão. Flag não anuncia recurso não qualificado.

**Plano de evidências:** pacote B/C destacado do sujeito imutável A; não commitar resultados em A para exigir seu próprio hash.

## Assertions deste pacote

### A-HUG-015-CC01

Família `T-HUG-015-P`; etapa `pre_promotion`; estado `specified_not_executed`.

**Dado:** {
  "source": "Sujeito A e versões do contrato fixados; teste de descoberta usa seu artefato de fonte inspecionado, não inventa runtime.",
  "protocols": [
    "P00@3.0",
    "P04@3.0",
    "P05@3.0"
  ],
  "fixture_inputs": [
    "Predecessores admitidos e SHA fixado, ou checkout a obter por HUG-001; descoberta produz sua fonte/fixture, consumidores exigem a fonte qualificada.",
    "Contratos P00, P04, P05 desta revisão; freeze runtime exigido somente para consumidores pós-HUG-059. Evidence histórica não conta como novo ensaio."
  ],
  "fixture_scope": [
    "crates/hugit-contracts/src/v2/events.rs",
    "crates/hugit-contracts/src/v2/identities.rs",
    "conformance/identities-v2.json"
  ]
}

**Estímulo:** Separar history_id, installation_id, origin_namespace_id/source_event_id estáveis, collector_instance_id/delivery_id efêmeros, goal+revision, invocation, action, snapshot e binding; collector reiniciado nunca cria fato histórico novo. Separar blob lógico, representação física, ocorrência e transformação; integers/unidades/encoding/domain tags definidos em P04. Origem é atribuída pelo boundary autorizado; source_kind não é confiança nem permissão. Campos legados têm namespace e nível conhecido.

**Predicado e observáveis:**
```json
{
  "predicate": "Schema inclui todos os tipos de P04 e versões dos codecs/parsers/policies.",
  "observable_paths": [
    "crates/hugit-contracts/src/v2/events.rs",
    "crates/hugit-contracts/src/v2/identities.rs",
    "conformance/identities-v2.json"
  ],
  "required_fields": [
    "assertion_id",
    "expected",
    "observed",
    "artifact_refs",
    "outcome",
    "verifier_identity"
  ],
  "verifier_kind": "structured_review_of_observables"
}
```

**Evidência:** resultado imutável por sujeito/assertion/célula/attempt, bytes referenciados e identidade do verificador. Uma suite verde sem esta assertion não fecha o critério.

**Oráculo SHA-256:** `d0e2f512aa63ec4a8f3c64a46bfb188e581a0c8812b44440e8a9fb8f7591de15`

### A-HUG-015-CC02

Família `T-HUG-015-P`; etapa `pre_promotion`; estado `specified_not_executed`.

**Dado:** {
  "source": "Sujeito A e versões do contrato fixados; teste de descoberta usa seu artefato de fonte inspecionado, não inventa runtime.",
  "protocols": [
    "P00@3.0",
    "P04@3.0",
    "P05@3.0"
  ],
  "fixture_inputs": [
    "Predecessores admitidos e SHA fixado, ou checkout a obter por HUG-001; descoberta produz sua fonte/fixture, consumidores exigem a fonte qualificada.",
    "Contratos P00, P04, P05 desta revisão; freeze runtime exigido somente para consumidores pós-HUG-059. Evidence histórica não conta como novo ensaio."
  ],
  "fixture_scope": [
    "crates/hugit-contracts/src/v2/events.rs",
    "crates/hugit-contracts/src/v2/identities.rs",
    "conformance/identities-v2.json"
  ]
}

**Estímulo:** Separar history_id, installation_id, origin_namespace_id/source_event_id estáveis, collector_instance_id/delivery_id efêmeros, goal+revision, invocation, action, snapshot e binding; collector reiniciado nunca cria fato histórico novo. Separar blob lógico, representação física, ocorrência e transformação; integers/unidades/encoding/domain tags definidos em P04. Origem é atribuída pelo boundary autorizado; source_kind não é confiança nem permissão. Campos legados têm namespace e nível conhecido.

**Predicado e observáveis:**
```json
{
  "predicate": "Fixture do hook HUG-057 é representável sem inventar campo.",
  "observable_paths": [
    "crates/hugit-contracts/src/v2/events.rs",
    "crates/hugit-contracts/src/v2/identities.rs",
    "conformance/identities-v2.json"
  ],
  "required_fields": [
    "assertion_id",
    "expected",
    "observed",
    "artifact_refs",
    "outcome",
    "verifier_identity"
  ],
  "verifier_kind": "structured_review_of_observables"
}
```

**Evidência:** resultado imutável por sujeito/assertion/célula/attempt, bytes referenciados e identidade do verificador. Uma suite verde sem esta assertion não fecha o critério.

**Oráculo SHA-256:** `945e8264cdd909c8378032263399e093756043c7c71d99ff832d54db8d70aee9`

### A-HUG-015-CC03

Família `T-HUG-015-P`; etapa `pre_promotion`; estado `specified_not_executed`.

**Dado:** {
  "source": "Sujeito A e versões do contrato fixados; teste de descoberta usa seu artefato de fonte inspecionado, não inventa runtime.",
  "protocols": [
    "P00@3.0",
    "P04@3.0",
    "P05@3.0"
  ],
  "fixture_inputs": [
    "Predecessores admitidos e SHA fixado, ou checkout a obter por HUG-001; descoberta produz sua fonte/fixture, consumidores exigem a fonte qualificada.",
    "Contratos P00, P04, P05 desta revisão; freeze runtime exigido somente para consumidores pós-HUG-059. Evidence histórica não conta como novo ensaio."
  ],
  "fixture_scope": [
    "crates/hugit-contracts/src/v2/events.rs",
    "crates/hugit-contracts/src/v2/identities.rs",
    "conformance/identities-v2.json"
  ]
}

**Estímulo:** Separar history_id, installation_id, origin_namespace_id/source_event_id estáveis, collector_instance_id/delivery_id efêmeros, goal+revision, invocation, action, snapshot e binding; collector reiniciado nunca cria fato histórico novo. Separar blob lógico, representação física, ocorrência e transformação; integers/unidades/encoding/domain tags definidos em P04. Origem é atribuída pelo boundary autorizado; source_kind não é confiança nem permissão. Campos legados têm namespace e nível conhecido.

**Predicado e observáveis:**
```json
{
  "predicate": "Leitor v1 separado preserva bytes e semântica legacy.",
  "observable_paths": [
    "crates/hugit-contracts/src/v2/events.rs",
    "crates/hugit-contracts/src/v2/identities.rs",
    "conformance/identities-v2.json"
  ],
  "required_fields": [
    "assertion_id",
    "expected",
    "observed",
    "artifact_refs",
    "outcome",
    "verifier_identity"
  ],
  "verifier_kind": "structured_review_of_observables"
}
```

**Evidência:** resultado imutável por sujeito/assertion/célula/attempt, bytes referenciados e identidade do verificador. Uma suite verde sem esta assertion não fecha o critério.

**Oráculo SHA-256:** `03a88cb2c8ca3e365efed86dc888a58ec4e8a8ad59bc851078a7837fea3b0eda`

### A-HUG-015-SC01

Família `T-HUG-015-P`; etapa `pre_promotion`; estado `specified_not_executed`.

**Dado:** {
  "source": "Sujeito A e versões do contrato fixados; teste de descoberta usa seu artefato de fonte inspecionado, não inventa runtime.",
  "protocols": [
    "P00@3.0",
    "P04@3.0",
    "P05@3.0"
  ],
  "fixture_inputs": [
    "Predecessores admitidos e SHA fixado, ou checkout a obter por HUG-001; descoberta produz sua fonte/fixture, consumidores exigem a fonte qualificada.",
    "Contratos P00, P04, P05 desta revisão; freeze runtime exigido somente para consumidores pós-HUG-059. Evidence histórica não conta como novo ensaio."
  ],
  "fixture_scope": [
    "crates/hugit-contracts/src/v2/events.rs",
    "crates/hugit-contracts/src/v2/identities.rs",
    "conformance/identities-v2.json"
  ]
}

**Estímulo:** Separar history_id, installation_id, origin_namespace_id/source_event_id estáveis, collector_instance_id/delivery_id efêmeros, goal+revision, invocation, action, snapshot e binding; collector reiniciado nunca cria fato histórico novo. Separar blob lógico, representação física, ocorrência e transformação; integers/unidades/encoding/domain tags definidos em P04. Origem é atribuída pelo boundary autorizado; source_kind não é confiança nem permissão. Campos legados têm namespace e nível conhecido.

**Predicado e observáveis:**
```json
{
  "predicate": "Vetores dos adapters observados roundtrip sem perda; mesma origem/same bytes é replay, same ID/different bytes é conflito; duas instalações criam novos producers sem colidir.",
  "observable_paths": [
    "crates/hugit-contracts/src/v2/events.rs",
    "crates/hugit-contracts/src/v2/identities.rs",
    "conformance/identities-v2.json"
  ],
  "required_fields": [
    "assertion_id",
    "expected",
    "observed",
    "artifact_refs",
    "outcome",
    "verifier_identity"
  ],
  "verifier_kind": "runtime_or_artifact_predicate"
}
```

**Evidência:** resultado imutável por sujeito/assertion/célula/attempt, bytes referenciados e identidade do verificador. Uma suite verde sem esta assertion não fecha o critério.

**Oráculo SHA-256:** `ae902880bbcc7a34484742bc4918d574030643eabecc0ed7e830d318dd0df3b6`

### A-HUG-015-SC02

Família `T-HUG-015-N`; etapa `pre_promotion`; estado `specified_not_executed`.

**Dado:** {
  "source": "Sujeito A e versões do contrato fixados; teste de descoberta usa seu artefato de fonte inspecionado, não inventa runtime.",
  "protocols": [
    "P00@3.0",
    "P04@3.0",
    "P05@3.0"
  ],
  "fixture_inputs": [
    "Predecessores admitidos e SHA fixado, ou checkout a obter por HUG-001; descoberta produz sua fonte/fixture, consumidores exigem a fonte qualificada.",
    "Contratos P00, P04, P05 desta revisão; freeze runtime exigido somente para consumidores pós-HUG-059. Evidence histórica não conta como novo ensaio."
  ],
  "fixture_scope": [
    "crates/hugit-contracts/src/v2/events.rs",
    "crates/hugit-contracts/src/v2/identities.rs",
    "conformance/identities-v2.json"
  ]
}

**Estímulo:** Recompressão não muda blob lógico, redaction muda conteúdo; Unicode/case/path alias não colapsam registros ou sobrescrevem restore. Executar o cenário negativo das assertions nomeadas, com estado/bytes antes e depois.

**Predicado e observáveis:**
```json
{
  "predicate": "Recompressão não muda blob lógico, redaction muda conteúdo; Unicode/case/path alias não colapsam registros ou sobrescrevem restore.",
  "observable_paths": [
    "crates/hugit-contracts/src/v2/events.rs",
    "crates/hugit-contracts/src/v2/identities.rs",
    "conformance/identities-v2.json"
  ],
  "required_fields": [
    "assertion_id",
    "expected",
    "observed",
    "artifact_refs",
    "outcome",
    "verifier_identity"
  ],
  "verifier_kind": "runtime_or_artifact_predicate"
}
```

**Evidência:** resultado imutável por sujeito/assertion/célula/attempt, bytes referenciados e identidade do verificador. Uma suite verde sem esta assertion não fecha o critério.

**Oráculo SHA-256:** `7046fa8aafe5afbcffbc2ef6f01348261072938d99735cbff4f0e7b70dc25ab9`

### A-HUG-015-QS01

Família `T-HUG-015-Q`; etapa `pre_promotion`; estado `specified_not_executed`.

**Dado:** {
  "source": "Sujeito A e versões do contrato fixados; teste de descoberta usa seu artefato de fonte inspecionado, não inventa runtime.",
  "protocols": [
    "P00@3.0",
    "P04@3.0",
    "P05@3.0"
  ],
  "fixture_inputs": [
    "Predecessores admitidos e SHA fixado, ou checkout a obter por HUG-001; descoberta produz sua fonte/fixture, consumidores exigem a fonte qualificada.",
    "Contratos P00, P04, P05 desta revisão; freeze runtime exigido somente para consumidores pós-HUG-059. Evidence histórica não conta como novo ensaio."
  ],
  "fixture_scope": [
    "crates/hugit-contracts/src/v2/events.rs",
    "crates/hugit-contracts/src/v2/identities.rs",
    "conformance/identities-v2.json"
  ]
}

**Estímulo:** Fixar versões, ambiente e dados; verificar todos os QS e budget aplicável. Separar erro de setup de violação do produto; conservar medições desfavoráveis.

**Predicado e observáveis:**
```json
{
  "predicate": "Proibir floats/concatenação ambígua nas identidades canônicas.",
  "observable_paths": [
    "crates/hugit-contracts/src/v2/events.rs",
    "crates/hugit-contracts/src/v2/identities.rs",
    "conformance/identities-v2.json"
  ],
  "required_fields": [
    "assertion_id",
    "expected",
    "observed",
    "artifact_refs",
    "outcome",
    "verifier_identity"
  ],
  "verifier_kind": "structured_review_of_observables"
}
```

**Evidência:** resultado imutável por sujeito/assertion/célula/attempt, bytes referenciados e identidade do verificador. Uma suite verde sem esta assertion não fecha o critério.

**Oráculo SHA-256:** `1c4b3b1bc1a0514e0bb7d94a3e5fb7f3bdd189063aa286cfc369e775db12e667`

### A-HUG-015-QS02

Família `T-HUG-015-Q`; etapa `pre_promotion`; estado `specified_not_executed`.

**Dado:** {
  "source": "Sujeito A e versões do contrato fixados; teste de descoberta usa seu artefato de fonte inspecionado, não inventa runtime.",
  "protocols": [
    "P00@3.0",
    "P04@3.0",
    "P05@3.0"
  ],
  "fixture_inputs": [
    "Predecessores admitidos e SHA fixado, ou checkout a obter por HUG-001; descoberta produz sua fonte/fixture, consumidores exigem a fonte qualificada.",
    "Contratos P00, P04, P05 desta revisão; freeze runtime exigido somente para consumidores pós-HUG-059. Evidence histórica não conta como novo ensaio."
  ],
  "fixture_scope": [
    "crates/hugit-contracts/src/v2/events.rs",
    "crates/hugit-contracts/src/v2/identities.rs",
    "conformance/identities-v2.json"
  ]
}

**Estímulo:** Fixar versões, ambiente e dados; verificar todos os QS e budget aplicável. Separar erro de setup de violação do produto; conservar medições desfavoráveis.

**Predicado e observáveis:**
```json
{
  "predicate": "Nenhuma migração muda source_time em nome da ordenação.",
  "observable_paths": [
    "crates/hugit-contracts/src/v2/events.rs",
    "crates/hugit-contracts/src/v2/identities.rs",
    "conformance/identities-v2.json"
  ],
  "required_fields": [
    "assertion_id",
    "expected",
    "observed",
    "artifact_refs",
    "outcome",
    "verifier_identity"
  ],
  "verifier_kind": "structured_review_of_observables"
}
```

**Evidência:** resultado imutável por sujeito/assertion/célula/attempt, bytes referenciados e identidade do verificador. Uma suite verde sem esta assertion não fecha o critério.

**Oráculo SHA-256:** `94a7c34a3f82265160616439c1631516129f0b42ff1d466376eb5a85c60576d6`

### A-HUG-015-QS03

Família `T-HUG-015-Q`; etapa `pre_promotion`; estado `specified_not_executed`.

**Dado:** {
  "source": "Sujeito A e versões do contrato fixados; teste de descoberta usa seu artefato de fonte inspecionado, não inventa runtime.",
  "protocols": [
    "P00@3.0",
    "P04@3.0",
    "P05@3.0"
  ],
  "fixture_inputs": [
    "Predecessores admitidos e SHA fixado, ou checkout a obter por HUG-001; descoberta produz sua fonte/fixture, consumidores exigem a fonte qualificada.",
    "Contratos P00, P04, P05 desta revisão; freeze runtime exigido somente para consumidores pós-HUG-059. Evidence histórica não conta como novo ensaio."
  ],
  "fixture_scope": [
    "crates/hugit-contracts/src/v2/events.rs",
    "crates/hugit-contracts/src/v2/identities.rs",
    "conformance/identities-v2.json"
  ]
}

**Estímulo:** Fixar versões, ambiente e dados; verificar todos os QS e budget aplicável. Separar erro de setup de violação do produto; conservar medições desfavoráveis.

**Predicado e observáveis:**
```json
{
  "predicate": "P11: read default 8 KiB, page 64 KiB, request 1 MiB; DB/transações/filas bounded; admission antes de novos bytes.",
  "observable_paths": [
    "crates/hugit-contracts/src/v2/events.rs",
    "crates/hugit-contracts/src/v2/identities.rs",
    "conformance/identities-v2.json"
  ],
  "required_fields": [
    "assertion_id",
    "expected",
    "observed",
    "artifact_refs",
    "outcome",
    "verifier_identity"
  ],
  "verifier_kind": "structured_review_of_observables"
}
```

**Evidência:** resultado imutável por sujeito/assertion/célula/attempt, bytes referenciados e identidade do verificador. Uma suite verde sem esta assertion não fecha o critério.

**Oráculo SHA-256:** `4181d08576649716bc6887286f1de9c8e2ce0a3335beeb64b3e173d53a0efea1`

### A-HUG-015-INV01

Família `T-HUG-015-N`; etapa `pre_promotion`; estado `specified_not_executed`.

**Dado:** {
  "source": "Sujeito A e versões do contrato fixados; teste de descoberta usa seu artefato de fonte inspecionado, não inventa runtime.",
  "protocols": [
    "P00@3.0",
    "P04@3.0",
    "P05@3.0"
  ],
  "fixture_inputs": [
    "Predecessores admitidos e SHA fixado, ou checkout a obter por HUG-001; descoberta produz sua fonte/fixture, consumidores exigem a fonte qualificada.",
    "Contratos P00, P04, P05 desta revisão; freeze runtime exigido somente para consumidores pós-HUG-059. Evidence histórica não conta como novo ensaio."
  ],
  "fixture_scope": [
    "crates/hugit-contracts/src/v2/events.rs",
    "crates/hugit-contracts/src/v2/identities.rs",
    "conformance/identities-v2.json"
  ]
}

**Estímulo:** Recompressão não muda blob lógico, redaction muda conteúdo; Unicode/case/path alias não colapsam registros ou sobrescrevem restore. Executar o cenário negativo das assertions nomeadas, com estado/bytes antes e depois.

**Predicado e observáveis:**
```json
{
  "predicate": "Texto de branch não vira goal original.",
  "observable_paths": [
    "crates/hugit-contracts/src/v2/events.rs",
    "crates/hugit-contracts/src/v2/identities.rs",
    "conformance/identities-v2.json"
  ],
  "required_fields": [
    "assertion_id",
    "expected",
    "observed",
    "artifact_refs",
    "outcome",
    "verifier_identity"
  ],
  "verifier_kind": "runtime_or_artifact_predicate"
}
```

**Evidência:** resultado imutável por sujeito/assertion/célula/attempt, bytes referenciados e identidade do verificador. Uma suite verde sem esta assertion não fecha o critério.

**Oráculo SHA-256:** `c42d6bcf15b30bb18abe5ffce62b25ff45b12efe03fb491b90819badf4324740`

### A-HUG-015-INV02

Família `T-HUG-015-N`; etapa `pre_promotion`; estado `specified_not_executed`.

**Dado:** {
  "source": "Sujeito A e versões do contrato fixados; teste de descoberta usa seu artefato de fonte inspecionado, não inventa runtime.",
  "protocols": [
    "P00@3.0",
    "P04@3.0",
    "P05@3.0"
  ],
  "fixture_inputs": [
    "Predecessores admitidos e SHA fixado, ou checkout a obter por HUG-001; descoberta produz sua fonte/fixture, consumidores exigem a fonte qualificada.",
    "Contratos P00, P04, P05 desta revisão; freeze runtime exigido somente para consumidores pós-HUG-059. Evidence histórica não conta como novo ensaio."
  ],
  "fixture_scope": [
    "crates/hugit-contracts/src/v2/events.rs",
    "crates/hugit-contracts/src/v2/identities.rs",
    "conformance/identities-v2.json"
  ]
}

**Estímulo:** Recompressão não muda blob lógico, redaction muda conteúdo; Unicode/case/path alias não colapsam registros ou sobrescrevem restore. Executar o cenário negativo das assertions nomeadas, com estado/bytes antes e depois.

**Predicado e observáveis:**
```json
{
  "predicate": "Origem/identidade não concede autorização.",
  "observable_paths": [
    "crates/hugit-contracts/src/v2/events.rs",
    "crates/hugit-contracts/src/v2/identities.rs",
    "conformance/identities-v2.json"
  ],
  "required_fields": [
    "assertion_id",
    "expected",
    "observed",
    "artifact_refs",
    "outcome",
    "verifier_identity"
  ],
  "verifier_kind": "runtime_or_artifact_predicate"
}
```

**Evidência:** resultado imutável por sujeito/assertion/célula/attempt, bytes referenciados e identidade do verificador. Uma suite verde sem esta assertion não fecha o critério.

**Oráculo SHA-256:** `020ec8338cf299bbe4fe8ac6e509ce053b3a2b4a3893748f547f60fa3cc45c72`

### A-HUG-015-INV03

Família `T-HUG-015-P`; etapa `pre_promotion`; estado `specified_not_executed`.

**Dado:** {
  "source": "Sujeito A e versões do contrato fixados; teste de descoberta usa seu artefato de fonte inspecionado, não inventa runtime.",
  "protocols": [
    "P00@3.0",
    "P04@3.0",
    "P05@3.0"
  ],
  "fixture_inputs": [
    "Predecessores admitidos e SHA fixado, ou checkout a obter por HUG-001; descoberta produz sua fonte/fixture, consumidores exigem a fonte qualificada.",
    "Contratos P00, P04, P05 desta revisão; freeze runtime exigido somente para consumidores pós-HUG-059. Evidence histórica não conta como novo ensaio."
  ],
  "fixture_scope": [
    "crates/hugit-contracts/src/v2/events.rs",
    "crates/hugit-contracts/src/v2/identities.rs",
    "conformance/identities-v2.json"
  ]
}

**Estímulo:** Separar history_id, installation_id, origin_namespace_id/source_event_id estáveis, collector_instance_id/delivery_id efêmeros, goal+revision, invocation, action, snapshot e binding; collector reiniciado nunca cria fato histórico novo. Separar blob lógico, representação física, ocorrência e transformação; integers/unidades/encoding/domain tags definidos em P04. Origem é atribuída pelo boundary autorizado; source_kind não é confiança nem permissão. Campos legados têm namespace e nível conhecido.

**Predicado e observáveis:**
```json
{
  "predicate": "Conteúdo e sua ocorrência têm identidades independentes.",
  "observable_paths": [
    "crates/hugit-contracts/src/v2/events.rs",
    "crates/hugit-contracts/src/v2/identities.rs",
    "conformance/identities-v2.json"
  ],
  "required_fields": [
    "assertion_id",
    "expected",
    "observed",
    "artifact_refs",
    "outcome",
    "verifier_identity"
  ],
  "verifier_kind": "runtime_or_artifact_predicate"
}
```

**Evidência:** resultado imutável por sujeito/assertion/célula/attempt, bytes referenciados e identidade do verificador. Uma suite verde sem esta assertion não fecha o critério.

**Oráculo SHA-256:** `24726adac396a4cfd1881ed45ab97ea039d783deae7877474a9c80410c3bd995`

### A-HUG-015-DOD01

Família `T-HUG-015-Q`; etapa `pre_promotion`; estado `specified_not_executed`.

**Dado:** {
  "source": "Sujeito A e versões do contrato fixados; teste de descoberta usa seu artefato de fonte inspecionado, não inventa runtime.",
  "protocols": [
    "P00@3.0",
    "P04@3.0",
    "P05@3.0"
  ],
  "fixture_inputs": [
    "Predecessores admitidos e SHA fixado, ou checkout a obter por HUG-001; descoberta produz sua fonte/fixture, consumidores exigem a fonte qualificada.",
    "Contratos P00, P04, P05 desta revisão; freeze runtime exigido somente para consumidores pós-HUG-059. Evidence histórica não conta como novo ensaio."
  ],
  "fixture_scope": [
    "crates/hugit-contracts/src/v2/events.rs",
    "crates/hugit-contracts/src/v2/identities.rs",
    "conformance/identities-v2.json"
  ]
}

**Estímulo:** Fixar versões, ambiente e dados; verificar todos os QS e budget aplicável. Separar erro de setup de violação do produto; conservar medições desfavoráveis.

**Predicado e observáveis:**
```json
{
  "predicate": "Fechar HUG-015 somente quando a implementação/artefato de 'Schema v2 baseado no hook real e identidades separadas' satisfaz suas assertions e observáveis obrigatórios; uma suite verde sem resultado individual não fecha os axiomas.",
  "observable_paths": [
    "crates/hugit-contracts/src/v2/events.rs",
    "crates/hugit-contracts/src/v2/identities.rs",
    "conformance/identities-v2.json"
  ],
  "required_fields": [
    "assertion_id",
    "expected",
    "observed",
    "artifact_refs",
    "outcome",
    "verifier_identity"
  ],
  "verifier_kind": "structured_review_of_observables"
}
```

**Evidência:** resultado imutável por sujeito/assertion/célula/attempt, bytes referenciados e identidade do verificador. Uma suite verde sem esta assertion não fecha o critério.

**Oráculo SHA-256:** `1dc65e84fa48fc7bdcd486c31ba5392fc6c8a6410db19d8fabac8454642d69fd`

### A-HUG-015-DOD02

Família `T-HUG-015-Q`; etapa `pre_promotion`; estado `specified_not_executed`.

**Dado:** {
  "source": "Sujeito A e versões do contrato fixados; teste de descoberta usa seu artefato de fonte inspecionado, não inventa runtime.",
  "protocols": [
    "P00@3.0",
    "P04@3.0",
    "P05@3.0"
  ],
  "fixture_inputs": [
    "Predecessores admitidos e SHA fixado, ou checkout a obter por HUG-001; descoberta produz sua fonte/fixture, consumidores exigem a fonte qualificada.",
    "Contratos P00, P04, P05 desta revisão; freeze runtime exigido somente para consumidores pós-HUG-059. Evidence histórica não conta como novo ensaio."
  ],
  "fixture_scope": [
    "crates/hugit-contracts/src/v2/events.rs",
    "crates/hugit-contracts/src/v2/identities.rs",
    "conformance/identities-v2.json"
  ]
}

**Estímulo:** Fixar versões, ambiente e dados; verificar todos os QS e budget aplicável. Separar erro de setup de violação do produto; conservar medições desfavoráveis.

**Predicado e observáveis:**
```json
{
  "predicate": "Todos os outputs crates/hugit-contracts/src/v2/events.rs, crates/hugit-contracts/src/v2/identities.rs, conformance/identities-v2.json entregues nos write sets resolvidos; erros de P00, P04, P05 e mudanças de interface revisados pelo owner do contrato.",
  "observable_paths": [
    "crates/hugit-contracts/src/v2/events.rs",
    "crates/hugit-contracts/src/v2/identities.rs",
    "conformance/identities-v2.json"
  ],
  "required_fields": [
    "assertion_id",
    "expected",
    "observed",
    "artifact_refs",
    "outcome",
    "verifier_identity"
  ],
  "verifier_kind": "structured_review_of_observables"
}
```

**Evidência:** resultado imutável por sujeito/assertion/célula/attempt, bytes referenciados e identidade do verificador. Uma suite verde sem esta assertion não fecha o critério.

**Oráculo SHA-256:** `f58b445fab901d7c73adb307d2f8de717fa2235db2ed1b108b721be527838795`

### A-HUG-015-DOD03

Família `T-HUG-015-R`; etapa `pre_promotion`; estado `specified_not_executed`.

**Dado:** {
  "source": "Sujeito A e versões do contrato fixados; teste de descoberta usa seu artefato de fonte inspecionado, não inventa runtime.",
  "protocols": [
    "P00@3.0",
    "P04@3.0",
    "P05@3.0"
  ],
  "fixture_inputs": [
    "Predecessores admitidos e SHA fixado, ou checkout a obter por HUG-001; descoberta produz sua fonte/fixture, consumidores exigem a fonte qualificada.",
    "Contratos P00, P04, P05 desta revisão; freeze runtime exigido somente para consumidores pós-HUG-059. Evidence histórica não conta como novo ensaio."
  ],
  "fixture_scope": [
    "crates/hugit-contracts/src/v2/events.rs",
    "crates/hugit-contracts/src/v2/identities.rs",
    "conformance/identities-v2.json"
  ]
}

**Estímulo:** Interromper nas fronteiras qualificadas; executar: Publicar versão adicional do schema e manter leitores da versão já emitida; antes de qualquer write público ainda é permitido substituir draft. Depois de eventos v2, apenas migração explícita e testes de roundtrip.

**Predicado e observáveis:**
```json
{
  "predicate": "Recovery deste pacote ensaiado: Publicar versão adicional do schema e manter leitores da versão já emitida; antes de qualquer write público ainda é permitido substituir draft. Depois de eventos v2, apenas migração explícita e testes de roundtrip.",
  "observable_paths": [
    "crates/hugit-contracts/src/v2/events.rs",
    "crates/hugit-contracts/src/v2/identities.rs",
    "conformance/identities-v2.json"
  ],
  "required_fields": [
    "assertion_id",
    "expected",
    "observed",
    "artifact_refs",
    "outcome",
    "verifier_identity"
  ],
  "verifier_kind": "structured_review_of_observables"
}
```

**Evidência:** resultado imutável por sujeito/assertion/célula/attempt, bytes referenciados e identidade do verificador. Uma suite verde sem esta assertion não fecha o critério.

**Oráculo SHA-256:** `95c9233ede60c380857a7f85f9a3925c5cc37391cf95cec0c519f168a1b20db2`

### A-HUG-015-DOD04

Família `T-HUG-015-Q`; etapa `pre_promotion`; estado `specified_not_executed`.

**Dado:** {
  "source": "Sujeito A e versões do contrato fixados; teste de descoberta usa seu artefato de fonte inspecionado, não inventa runtime.",
  "protocols": [
    "P00@3.0",
    "P04@3.0",
    "P05@3.0"
  ],
  "fixture_inputs": [
    "Predecessores admitidos e SHA fixado, ou checkout a obter por HUG-001; descoberta produz sua fonte/fixture, consumidores exigem a fonte qualificada.",
    "Contratos P00, P04, P05 desta revisão; freeze runtime exigido somente para consumidores pós-HUG-059. Evidence histórica não conta como novo ensaio."
  ],
  "fixture_scope": [
    "crates/hugit-contracts/src/v2/events.rs",
    "crates/hugit-contracts/src/v2/identities.rs",
    "conformance/identities-v2.json"
  ]
}

**Estímulo:** Fixar versões, ambiente e dados; verificar todos os QS e budget aplicável. Separar erro de setup de violação do produto; conservar medições desfavoráveis.

**Predicado e observáveis:**
```json
{
  "predicate": "Qualificar HUG-015 sobre o sujeito A identificado por digest; anexar em B assertions verificadas e aprovação de revisor diferente do autor. Resultados não alteram A; revalidar no candidato final as obrigações atingidas. Nenhum axioma obrigatório dispensado.",
  "observable_paths": [
    "crates/hugit-contracts/src/v2/events.rs",
    "crates/hugit-contracts/src/v2/identities.rs",
    "conformance/identities-v2.json"
  ],
  "required_fields": [
    "assertion_id",
    "expected",
    "observed",
    "artifact_refs",
    "outcome",
    "verifier_identity"
  ],
  "verifier_kind": "structured_review_of_observables"
}
```

**Evidência:** resultado imutável por sujeito/assertion/célula/attempt, bytes referenciados e identidade do verificador. Uma suite verde sem esta assertion não fecha o critério.

**Oráculo SHA-256:** `85119a538ca5e4f3e4394b5893e6ddbee9a1cab907c77e2010bdf881e65fd3b5`

### A-T-AR-09-C-contract

Família `T-AR-09-C`; etapa `pre_promotion`; estado `specified_not_executed`.

**Dado:** {
  "fixture_inputs": [
    "Implementações dos pacotes responsáveis integradas e contrato freeze.",
    "Counterexample do review preservado; não usar plano como evidence de runtime."
  ]
}

**Estímulo:** O mesmo log chega de duas invocações e é recomprimido em outro nível. Incluir timestamp/nome no ID perde dedupe. Ignorar política/codec na ocorrência perde rastreabilidade da transformação. Uma árvore tem paths com ambiguidade de separador ou nomes que colidem no filesystem de restore. Separar blob_id=H(domain||bytes lógicos), representation_id para codec/encoding/ciphertext, artifact occurrence para path/mode/media type/produtor, e derivation para redaction/normalização. Prefixo de domínio e encoding canônico versionados. Um manifesto de árvore identifica tipos de entrada e paths, não só uma concatenação ambígua.

**Predicado e observáveis:**
```json
{
  "predicate": "Bytes iguais em runs distintos compartilham blob sem fundir ocorrências. Recompressão mantém blob_id e troca representation_id. Redaction muda conteúdo e registra derivação. Case collisions, Unicode, links e executáveis têm decisão explícita de restore, não overwrite silencioso.",
  "required_fields": [
    "expected",
    "observed",
    "artifact_refs",
    "verifier_identity"
  ],
  "verifier_kind": "contract_observables"
}
```

**Evidência:** resultado imutável por sujeito/assertion/célula/attempt, bytes referenciados e identidade do verificador. Uma suite verde sem esta assertion não fecha o critério.

**Oráculo SHA-256:** `00cd3e10de292ec4af3329d5118e6c32cd1e75903eeb48403ac2559b5925468c`

### A-T-AR-12-C-contract

Família `T-AR-12-C`; etapa `pre_promotion`; estado `specified_not_executed`.

**Dado:** {
  "fixture_inputs": [
    "Implementações dos pacotes responsáveis integradas e contrato freeze.",
    "Counterexample do review preservado; não usar plano como evidence de runtime."
  ]
}

**Estímulo:** O mesmo pacote é restaurado em máquinas A e B mantendo repo_id/session identifiers locais. Ambas continuam trabalhando. Um export de A é importado em B; eventos idênticos devem deduplicar, mas eventos distintos da mesma história não podem conflitar como retransmissão. Paths e marcadores de worktree da máquina original também não são diretamente reutilizáveis. Definir restore-recovery que continua história sob novo boot/producer instance; clone/copy que recebe nova identidade de instalação e import namespace, preservando IDs históricos como origem; import read-only sem ativar hooks/worktree markers do autor. Nunca sobrescrever estado existente por padrão. Declaração explícita de merge/import e matriz de versão.

**Predicado e observáveis:**
```json
{
  "predicate": "Restaurar duas vezes, gerar novos eventos em ambas e importar um bundle repetidamente. Sem colisão, duplicação de uso ou falsa atribuição a diretórios antigos. Restore em caminho existente recusa ou requer operação explícita; pacote hostil não instala executável ou hook.",
  "required_fields": [
    "expected",
    "observed",
    "artifact_refs",
    "verifier_identity"
  ],
  "verifier_kind": "contract_observables"
}
```

**Evidência:** resultado imutável por sujeito/assertion/célula/attempt, bytes referenciados e identidade do verificador. Uma suite verde sem esta assertion não fecha o critério.

**Oráculo SHA-256:** `146b1613490e2402e5178e7a74eebdd8546203a09f088c0e250faf79397ff6b9`

## Famílias e células de aceitação

### T-AR-09-C

Owner da família: HUG-015. Células: artifact-review. Etapa: pre_promotion.

**Preparo:** Implementações dos pacotes responsáveis integradas e contrato freeze. Counterexample do review preservado; não usar plano como evidence de runtime.

**Execução:** O mesmo log chega de duas invocações e é recomprimido em outro nível. Incluir timestamp/nome no ID perde dedupe. Ignorar política/codec na ocorrência perde rastreabilidade da transformação. Uma árvore tem paths com ambiguidade de separador ou nomes que colidem no filesystem de restore. Separar blob_id=H(domain||bytes lógicos), representation_id para codec/encoding/ciphertext, artifact occurrence para path/mode/media type/produtor, e derivation para redaction/normalização. Prefixo de domínio e encoding canônico versionados. Um manifesto de árvore identifica tipos de entrada e paths, não só uma concatenação ambígua.

**Esperado:** Bytes iguais em runs distintos compartilham blob sem fundir ocorrências. Recompressão mantém blob_id e troca representation_id. Redaction muda conteúdo e registra derivação. Case collisions, Unicode, links e executáveis têm decisão explícita de restore, não overwrite silencioso.

**Artifact template:** `evidence/results/T-AR-09-C/{subject_digest}/{cell_id}/{attempt_id}/result.json`

### T-AR-12-C

Owner da família: HUG-015. Células: artifact-review. Etapa: pre_promotion.

**Preparo:** Implementações dos pacotes responsáveis integradas e contrato freeze. Counterexample do review preservado; não usar plano como evidence de runtime.

**Execução:** O mesmo pacote é restaurado em máquinas A e B mantendo repo_id/session identifiers locais. Ambas continuam trabalhando. Um export de A é importado em B; eventos idênticos devem deduplicar, mas eventos distintos da mesma história não podem conflitar como retransmissão. Paths e marcadores de worktree da máquina original também não são diretamente reutilizáveis. Definir restore-recovery que continua história sob novo boot/producer instance; clone/copy que recebe nova identidade de instalação e import namespace, preservando IDs históricos como origem; import read-only sem ativar hooks/worktree markers do autor. Nunca sobrescrever estado existente por padrão. Declaração explícita de merge/import e matriz de versão.

**Esperado:** Restaurar duas vezes, gerar novos eventos em ambas e importar um bundle repetidamente. Sem colisão, duplicação de uso ou falsa atribuição a diretórios antigos. Restore em caminho existente recusa ou requer operação explícita; pacote hostil não instala executável ou hook.

**Artifact template:** `evidence/results/T-AR-12-C/{subject_digest}/{cell_id}/{attempt_id}/result.json`

### T-HUG-015-N

Owner da família: HUG-015. Células: artifact-review. Etapa: pre_promotion.

**Preparo:** Predecessores admitidos e SHA fixado, ou checkout a obter por HUG-001; descoberta produz sua fonte/fixture, consumidores exigem a fonte qualificada. Contratos P00, P04, P05 desta revisão; freeze runtime exigido somente para consumidores pós-HUG-059. Evidence histórica não conta como novo ensaio.

**Execução:** Recompressão não muda blob lógico, redaction muda conteúdo; Unicode/case/path alias não colapsam registros ou sobrescrevem restore. Executar o cenário negativo das assertions nomeadas, com estado/bytes antes e depois.

**Esperado:** Recompressão não muda blob lógico, redaction muda conteúdo; Unicode/case/path alias não colapsam registros ou sobrescrevem restore.

**Artifact template:** `evidence/results/T-HUG-015-N/{subject_digest}/{cell_id}/{attempt_id}/result.json`

### T-HUG-015-P

Owner da família: HUG-015. Células: artifact-review. Etapa: pre_promotion.

**Preparo:** Predecessores admitidos e SHA fixado, ou checkout a obter por HUG-001; descoberta produz sua fonte/fixture, consumidores exigem a fonte qualificada. Contratos P00, P04, P05 desta revisão; freeze runtime exigido somente para consumidores pós-HUG-059. Evidence histórica não conta como novo ensaio.

**Execução:** Separar history_id, installation_id, origin_namespace_id/source_event_id estáveis, collector_instance_id/delivery_id efêmeros, goal+revision, invocation, action, snapshot e binding; collector reiniciado nunca cria fato histórico novo. Separar blob lógico, representação física, ocorrência e transformação; integers/unidades/encoding/domain tags definidos em P04. Origem é atribuída pelo boundary autorizado; source_kind não é confiança nem permissão. Campos legados têm namespace e nível conhecido.

**Esperado:** Vetores dos adapters observados roundtrip sem perda; mesma origem/same bytes é replay, same ID/different bytes é conflito; duas instalações criam novos producers sem colidir.

**Artifact template:** `evidence/results/T-HUG-015-P/{subject_digest}/{cell_id}/{attempt_id}/result.json`

### T-HUG-015-Q

Owner da família: HUG-015. Células: artifact-review. Etapa: pre_promotion.

**Preparo:** Predecessores admitidos e SHA fixado, ou checkout a obter por HUG-001; descoberta produz sua fonte/fixture, consumidores exigem a fonte qualificada. Contratos P00, P04, P05 desta revisão; freeze runtime exigido somente para consumidores pós-HUG-059. Evidence histórica não conta como novo ensaio.

**Execução:** Fixar versões, ambiente e dados; verificar todos os QS e budget aplicável. Separar erro de setup de violação do produto; conservar medições desfavoráveis.

**Esperado:** Proibir floats/concatenação ambígua nas identidades canônicas.; Nenhuma migração muda source_time em nome da ordenação.; P11: read default 8 KiB, page 64 KiB, request 1 MiB; DB/transações/filas bounded; admission antes de novos bytes.

**Artifact template:** `evidence/results/T-HUG-015-Q/{subject_digest}/{cell_id}/{attempt_id}/result.json`

### T-HUG-015-R

Owner da família: HUG-015. Células: artifact-review. Etapa: pre_promotion.

**Preparo:** Predecessores admitidos e SHA fixado, ou checkout a obter por HUG-001; descoberta produz sua fonte/fixture, consumidores exigem a fonte qualificada. Contratos P00, P04, P05 desta revisão; freeze runtime exigido somente para consumidores pós-HUG-059. Evidence histórica não conta como novo ensaio.

**Execução:** Interromper nas fronteiras qualificadas; executar: Publicar versão adicional do schema e manter leitores da versão já emitida; antes de qualquer write público ainda é permitido substituir draft. Depois de eventos v2, apenas migração explícita e testes de roundtrip.

**Esperado:** Publicar versão adicional do schema e manter leitores da versão já emitida; antes de qualquer write público ainda é permitido substituir draft. Depois de eventos v2, apenas migração explícita e testes de roundtrip.

**Artifact template:** `evidence/results/T-HUG-015-R/{subject_digest}/{cell_id}/{attempt_id}/result.json`

**Fechamento:** cinco grupos de axiomas demonstrados por assertions verificadas no candidato integrado, sem waiver. HUG-056 fecha somente após C e pós-publicação; demais estados seguem P00/P12.
