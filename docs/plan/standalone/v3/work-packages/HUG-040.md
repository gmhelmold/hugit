# HUG-040 — Buildx: registro exato finalizado, não latest

**Estado:** planned; **owner:** F; **fase:** E; **prioridade:** full_scope.

**Dependências:** HUG-027, HUG-032, HUG-033, HUG-035

**Congelamento de runtime requerido:** true

**Revisões:** AR-15, AR-16

**Contratos vinculados:**
- P00@3.0 — sha256 `5d737267087e24bbcca9548488c719f4cca46fa3435a0ccf31b7b98696fde5cc`
- P08@3.0 — sha256 `0d6a0d18c9b72c52ff3eef81f0b156b20e753a501bbd95027a4da8ba2b0c46e2`
- P11@3.0 — sha256 `d0d06231bc06a332774e65589b6e696aed4811e4cb1d610b71f84315409d9156`

## Prontidão

Todos os predecessores possuem evidência integrada e interfaces versionadas compatíveis.

Owner F aceita write-set exato e budget B-NATIVE; HUG-001 distingue arquivos existentes dos novos.

Os casos P/N/Q/R e os oráculos estão especificados antes do dispatch; fixtures e testes executáveis são entregues pelo próprio pacote ou predecessor. Documentação/descoberta usa oráculos de artefato, não um falso bug de runtime.

Qualquer dado de fonte ainda não observado bloqueia o consumidor; descobrir no pacote predecessor, sem inventar payload.

Freeze runtime de HUG-059 já integrado é obrigatório.

## Outputs e ownership

Caminhos propostos: HUG-001 precisa vincular sua presença/criação ao checkout real antes da implementação. Evidências B/C ficam fora do sujeito A.

- `adapters/docker/buildx_history.rs`
- `adapters/docker/compatibility.json`
- `adapters/docker/tests/history_finalization.rs`

**Leituras:** `novo adapter docker-buildx/`

**Recursos exclusivos:** path:adapters/docker/buildx_history.rs, path:adapters/docker/compatibility.json, path:adapters/docker/tests/history_finalization.rs

## Interfaces e erros

```json
{
  "interfaces": [
    {
      "protocol": "P00",
      "interfaces": [
        "validate_plan(plan)->validation_report",
        "dispatch(wp,source_manifest,dependency_evidence)->admission",
        "qualify(profile,candidate,evidence)->readiness"
      ]
    },
    {
      "protocol": "P08",
      "interfaces": [
        "collect_report(invocation,producer_witness)->report",
        "aggregate(shard_manifest,attempts)->suite",
        "read_range(cursor,generation,lease)->page_or_CURSOR_INVALIDATED",
        "restore(bundle,mode,authority_anchor)->installation"
      ]
    },
    {
      "protocol": "P11",
      "interfaces": [
        "reserve_installation(repo,envelope)->ticket",
        "reserve_repo(ticket,operation)->allocation",
        "reconcile_budget(ticket,state)->usage",
        "measure(workload)->timing_and_resource_samples"
      ]
    }
  ],
  "errors": [
    {
      "protocol": "P00",
      "codes": [
        "PLAN_INVALID",
        "DEPENDENCY_UNPROVEN",
        "CONTRACT_NOT_FROZEN",
        "SOURCE_DRIFT",
        "PROFILE_INCOMPLETE",
        "PROTOCOL_DIGEST_MISMATCH",
        "ASSERTION_UNCOVERED",
        "NORMATIVE_VIEW_DRIFT",
        "TEST_OWNER_MISMATCH"
      ]
    },
    {
      "protocol": "P08",
      "codes": [
        "REPORT_STALE",
        "SHARD_MISSING",
        "REPORT_INVALID",
        "CURSOR_EXPIRED",
        "RESTORE_TARGET_EXISTS",
        "UNSAFE_PATH",
        "BUNDLE_INCOMPLETE",
        "CURSOR_INVALIDATED",
        "INDEX_NOT_READY"
      ]
    },
    {
      "protocol": "P11",
      "codes": [
        "OVER_QUOTA",
        "NO_TRANSIENT_RESERVE",
        "WAL_PRESSURE",
        "COLLECTOR_LIMIT",
        "BACKLOG_LIMIT",
        "STORAGE_UNAVAILABLE"
      ]
    }
  ]
}
```

## Unidades verificáveis

1. **HUG-040.S1** — Capturar ref exata da execução e aguardar finalização com API/--finalize qualificado ou marcar running/partial.
2. **HUG-040.S2** — Preservar .dockerbuild e metadados selecionados sob privacy/retention; imagem e cache são objetos separados.
3. **HUG-040.S3** — Sem Docker, reportar capability unavailable sem bloquear instalação/core.

## Completeness criteria

**HUG-040-CC01 — obrigatório.** Matriz de Buildx real qualifica option de finalização.

Assertions: `A-HUG-040-CC01`. Famílias: T-HUG-040-P.

**HUG-040-CC02 — obrigatório.** Artefatos e image digests não se confundem com cache keys.

Assertions: `A-HUG-040-CC02`. Famílias: T-HUG-040-P.

**HUG-040-CC03 — obrigatório.** Cópia só dos objetos autorizados.

Assertions: `A-HUG-040-CC03`. Famílias: T-HUG-040-P.

## Success criteria

**HUG-040-SC01 — obrigatório.** Dois builds concorrentes mantêm refs/goals próprios; falha finalizada e build verde geram registros consultáveis com digest e final state.

Assertions: `A-HUG-040-SC01`. Famílias: T-HUG-040-P.

**HUG-040-SC02 — obrigatório.** Export antes do build finalizar, ref latest e CLI sem opção qualificada não recebem completed; URLs do report não são chamadas pelo importer.

Assertions: `A-HUG-040-SC02`. Famílias: T-HUG-040-N.

## Quality standards

**HUG-040-QS01 — obrigatório.** Hugit não instala/inicia Docker automaticamente.

Assertions: `A-HUG-040-QS01`. Famílias: T-HUG-040-Q.

**HUG-040-QS02 — obrigatório.** Timeout de finalizar registra partial, não sucesso inferido.

Assertions: `A-HUG-040-QS02`. Famílias: T-HUG-040-Q.

**HUG-040-QS03 — obrigatório.** Tool optional; ref/digest exatos, deadline finalization explícito; copy-on-request/quotas; native hit/miss medidos, sem tocar cache interno vivo.

Assertions: `A-HUG-040-QS03`. Famílias: T-HUG-040-Q.

## Definition of Done — DoD

**HUG-040-DOD01 — obrigatório.** Fechar HUG-040 somente quando a implementação/artefato de 'Buildx: registro exato finalizado, não latest' satisfaz suas assertions e observáveis obrigatórios; uma suite verde sem resultado individual não fecha os axiomas.

Assertions: `A-HUG-040-DOD01`. Famílias: T-HUG-040-Q.

**HUG-040-DOD02 — obrigatório.** Todos os outputs adapters/docker/buildx_history.rs, adapters/docker/compatibility.json, adapters/docker/tests/history_finalization.rs entregues nos write sets resolvidos; erros de P00, P08, P11 e mudanças de interface revisados pelo owner do contrato.

Assertions: `A-HUG-040-DOD02`. Famílias: T-HUG-040-Q.

**HUG-040-DOD03 — obrigatório.** Recovery deste pacote ensaiado: Desabilitar adapter na versão incompatível, preservar registro parcial com ref e motivo; não cancelar ou limpar build do usuário por erro do recorder, salvo operação explicitamente autorizada.

Assertions: `A-HUG-040-DOD03`. Famílias: T-HUG-040-R.

**HUG-040-DOD04 — obrigatório.** Qualificar HUG-040 sobre o sujeito A identificado por digest; anexar em B assertions verificadas e aprovação de revisor diferente do autor. Resultados não alteram A; revalidar no candidato final as obrigações atingidas. Nenhum axioma obrigatório dispensado.

Assertions: `A-HUG-040-DOD04`. Famílias: T-HUG-040-Q.

## Invariants

**HUG-040-INV01 — obrigatório.** latest não identifica execução concorrente.

Assertions: `A-HUG-040-INV01`. Famílias: T-HUG-040-N.

**HUG-040-INV02 — obrigatório.** Registro .dockerbuild não é imagem nem cache.

Assertions: `A-HUG-040-INV02`. Famílias: T-HUG-040-N.

**HUG-040-INV03 — obrigatório.** Integração opcional não vira dependência core.

Assertions: `A-HUG-040-INV03`. Famílias: T-HUG-040-N.

## Budget e recuperação

Tool optional; ref/digest exatos, deadline finalization explícito; copy-on-request/quotas; native hit/miss medidos, sem tocar cache interno vivo.

**Rollback/recovery:** Desabilitar adapter na versão incompatível, preservar registro parcial com ref e motivo; não cancelar ou limpar build do usuário por erro do recorder, salvo operação explicitamente autorizada.

**Rollout:** habilitar só depois das assertions, qualificação de célula e revisão. Flag não anuncia recurso não qualificado.

**Plano de evidências:** pacote B/C destacado do sujeito imutável A; não commitar resultados em A para exigir seu próprio hash.

## Assertions deste pacote

### A-HUG-040-CC01

Família `T-HUG-040-P`; etapa `pre_promotion`; estado `specified_not_executed`.

**Dado:** {
  "source": "Sujeito A e versões do contrato fixados; teste de descoberta usa seu artefato de fonte inspecionado, não inventa runtime.",
  "protocols": [
    "P00@3.0",
    "P08@3.0",
    "P11@3.0"
  ],
  "fixture_inputs": [
    "Predecessores admitidos e SHA fixado, ou checkout a obter por HUG-001; descoberta produz sua fonte/fixture, consumidores exigem a fonte qualificada.",
    "Contratos P00, P08, P11 desta revisão; freeze runtime exigido somente para consumidores pós-HUG-059. Evidence histórica não conta como novo ensaio."
  ],
  "fixture_scope": [
    "adapters/docker/buildx_history.rs",
    "adapters/docker/compatibility.json",
    "adapters/docker/tests/history_finalization.rs"
  ]
}

**Estímulo:** Capturar ref exata da execução e aguardar finalização com API/--finalize qualificado ou marcar running/partial. Preservar .dockerbuild e metadados selecionados sob privacy/retention; imagem e cache são objetos separados. Sem Docker, reportar capability unavailable sem bloquear instalação/core.

**Predicado e observáveis:**
```json
{
  "predicate": "Matriz de Buildx real qualifica option de finalização.",
  "observable_paths": [
    "adapters/docker/buildx_history.rs",
    "adapters/docker/compatibility.json",
    "adapters/docker/tests/history_finalization.rs"
  ],
  "required_fields": [
    "assertion_id",
    "expected",
    "observed",
    "artifact_refs",
    "outcome",
    "verifier_identity"
  ],
  "verifier_kind": "structured_review_of_observables"
}
```

**Evidência:** resultado imutável por sujeito/assertion/célula/attempt, bytes referenciados e identidade do verificador. Uma suite verde sem esta assertion não fecha o critério.

**Oráculo SHA-256:** `d02671852621972038aa7f45d6e07c1f0e1fb60069a42f39ca6a8d9cc21d0ce3`

### A-HUG-040-CC02

Família `T-HUG-040-P`; etapa `pre_promotion`; estado `specified_not_executed`.

**Dado:** {
  "source": "Sujeito A e versões do contrato fixados; teste de descoberta usa seu artefato de fonte inspecionado, não inventa runtime.",
  "protocols": [
    "P00@3.0",
    "P08@3.0",
    "P11@3.0"
  ],
  "fixture_inputs": [
    "Predecessores admitidos e SHA fixado, ou checkout a obter por HUG-001; descoberta produz sua fonte/fixture, consumidores exigem a fonte qualificada.",
    "Contratos P00, P08, P11 desta revisão; freeze runtime exigido somente para consumidores pós-HUG-059. Evidence histórica não conta como novo ensaio."
  ],
  "fixture_scope": [
    "adapters/docker/buildx_history.rs",
    "adapters/docker/compatibility.json",
    "adapters/docker/tests/history_finalization.rs"
  ]
}

**Estímulo:** Capturar ref exata da execução e aguardar finalização com API/--finalize qualificado ou marcar running/partial. Preservar .dockerbuild e metadados selecionados sob privacy/retention; imagem e cache são objetos separados. Sem Docker, reportar capability unavailable sem bloquear instalação/core.

**Predicado e observáveis:**
```json
{
  "predicate": "Artefatos e image digests não se confundem com cache keys.",
  "observable_paths": [
    "adapters/docker/buildx_history.rs",
    "adapters/docker/compatibility.json",
    "adapters/docker/tests/history_finalization.rs"
  ],
  "required_fields": [
    "assertion_id",
    "expected",
    "observed",
    "artifact_refs",
    "outcome",
    "verifier_identity"
  ],
  "verifier_kind": "structured_review_of_observables"
}
```

**Evidência:** resultado imutável por sujeito/assertion/célula/attempt, bytes referenciados e identidade do verificador. Uma suite verde sem esta assertion não fecha o critério.

**Oráculo SHA-256:** `6c72392a01e3e54adcb6061a3e6d896d4759c165060b8dbd44ab504595e01b2c`

### A-HUG-040-CC03

Família `T-HUG-040-P`; etapa `pre_promotion`; estado `specified_not_executed`.

**Dado:** {
  "source": "Sujeito A e versões do contrato fixados; teste de descoberta usa seu artefato de fonte inspecionado, não inventa runtime.",
  "protocols": [
    "P00@3.0",
    "P08@3.0",
    "P11@3.0"
  ],
  "fixture_inputs": [
    "Predecessores admitidos e SHA fixado, ou checkout a obter por HUG-001; descoberta produz sua fonte/fixture, consumidores exigem a fonte qualificada.",
    "Contratos P00, P08, P11 desta revisão; freeze runtime exigido somente para consumidores pós-HUG-059. Evidence histórica não conta como novo ensaio."
  ],
  "fixture_scope": [
    "adapters/docker/buildx_history.rs",
    "adapters/docker/compatibility.json",
    "adapters/docker/tests/history_finalization.rs"
  ]
}

**Estímulo:** Capturar ref exata da execução e aguardar finalização com API/--finalize qualificado ou marcar running/partial. Preservar .dockerbuild e metadados selecionados sob privacy/retention; imagem e cache são objetos separados. Sem Docker, reportar capability unavailable sem bloquear instalação/core.

**Predicado e observáveis:**
```json
{
  "predicate": "Cópia só dos objetos autorizados.",
  "observable_paths": [
    "adapters/docker/buildx_history.rs",
    "adapters/docker/compatibility.json",
    "adapters/docker/tests/history_finalization.rs"
  ],
  "required_fields": [
    "assertion_id",
    "expected",
    "observed",
    "artifact_refs",
    "outcome",
    "verifier_identity"
  ],
  "verifier_kind": "structured_review_of_observables"
}
```

**Evidência:** resultado imutável por sujeito/assertion/célula/attempt, bytes referenciados e identidade do verificador. Uma suite verde sem esta assertion não fecha o critério.

**Oráculo SHA-256:** `1a281bf6e0f9417f675c1c18109c81ed42cadb1f16f224ca628e6fe0a1ae3827`

### A-HUG-040-SC01

Família `T-HUG-040-P`; etapa `pre_promotion`; estado `specified_not_executed`.

**Dado:** {
  "source": "Sujeito A e versões do contrato fixados; teste de descoberta usa seu artefato de fonte inspecionado, não inventa runtime.",
  "protocols": [
    "P00@3.0",
    "P08@3.0",
    "P11@3.0"
  ],
  "fixture_inputs": [
    "Predecessores admitidos e SHA fixado, ou checkout a obter por HUG-001; descoberta produz sua fonte/fixture, consumidores exigem a fonte qualificada.",
    "Contratos P00, P08, P11 desta revisão; freeze runtime exigido somente para consumidores pós-HUG-059. Evidence histórica não conta como novo ensaio."
  ],
  "fixture_scope": [
    "adapters/docker/buildx_history.rs",
    "adapters/docker/compatibility.json",
    "adapters/docker/tests/history_finalization.rs"
  ]
}

**Estímulo:** Capturar ref exata da execução e aguardar finalização com API/--finalize qualificado ou marcar running/partial. Preservar .dockerbuild e metadados selecionados sob privacy/retention; imagem e cache são objetos separados. Sem Docker, reportar capability unavailable sem bloquear instalação/core.

**Predicado e observáveis:**
```json
{
  "predicate": "Dois builds concorrentes mantêm refs/goals próprios; falha finalizada e build verde geram registros consultáveis com digest e final state.",
  "observable_paths": [
    "adapters/docker/buildx_history.rs",
    "adapters/docker/compatibility.json",
    "adapters/docker/tests/history_finalization.rs"
  ],
  "required_fields": [
    "assertion_id",
    "expected",
    "observed",
    "artifact_refs",
    "outcome",
    "verifier_identity"
  ],
  "verifier_kind": "runtime_or_artifact_predicate"
}
```

**Evidência:** resultado imutável por sujeito/assertion/célula/attempt, bytes referenciados e identidade do verificador. Uma suite verde sem esta assertion não fecha o critério.

**Oráculo SHA-256:** `ae07a64edaff204cdc659591b0858fba6c0368fa6a7eb0b344f59b77cabb0d3a`

### A-HUG-040-SC02

Família `T-HUG-040-N`; etapa `pre_promotion`; estado `specified_not_executed`.

**Dado:** {
  "source": "Sujeito A e versões do contrato fixados; teste de descoberta usa seu artefato de fonte inspecionado, não inventa runtime.",
  "protocols": [
    "P00@3.0",
    "P08@3.0",
    "P11@3.0"
  ],
  "fixture_inputs": [
    "Predecessores admitidos e SHA fixado, ou checkout a obter por HUG-001; descoberta produz sua fonte/fixture, consumidores exigem a fonte qualificada.",
    "Contratos P00, P08, P11 desta revisão; freeze runtime exigido somente para consumidores pós-HUG-059. Evidence histórica não conta como novo ensaio."
  ],
  "fixture_scope": [
    "adapters/docker/buildx_history.rs",
    "adapters/docker/compatibility.json",
    "adapters/docker/tests/history_finalization.rs"
  ]
}

**Estímulo:** Export antes do build finalizar, ref latest e CLI sem opção qualificada não recebem completed; URLs do report não são chamadas pelo importer. Executar o cenário negativo das assertions nomeadas, com estado/bytes antes e depois.

**Predicado e observáveis:**
```json
{
  "predicate": "Export antes do build finalizar, ref latest e CLI sem opção qualificada não recebem completed; URLs do report não são chamadas pelo importer.",
  "observable_paths": [
    "adapters/docker/buildx_history.rs",
    "adapters/docker/compatibility.json",
    "adapters/docker/tests/history_finalization.rs"
  ],
  "required_fields": [
    "assertion_id",
    "expected",
    "observed",
    "artifact_refs",
    "outcome",
    "verifier_identity"
  ],
  "verifier_kind": "runtime_or_artifact_predicate"
}
```

**Evidência:** resultado imutável por sujeito/assertion/célula/attempt, bytes referenciados e identidade do verificador. Uma suite verde sem esta assertion não fecha o critério.

**Oráculo SHA-256:** `6a18eefd18b40defa61c6e63bf20760fd12aea123dce4435b39377997fc6643e`

### A-HUG-040-QS01

Família `T-HUG-040-Q`; etapa `pre_promotion`; estado `specified_not_executed`.

**Dado:** {
  "source": "Sujeito A e versões do contrato fixados; teste de descoberta usa seu artefato de fonte inspecionado, não inventa runtime.",
  "protocols": [
    "P00@3.0",
    "P08@3.0",
    "P11@3.0"
  ],
  "fixture_inputs": [
    "Predecessores admitidos e SHA fixado, ou checkout a obter por HUG-001; descoberta produz sua fonte/fixture, consumidores exigem a fonte qualificada.",
    "Contratos P00, P08, P11 desta revisão; freeze runtime exigido somente para consumidores pós-HUG-059. Evidence histórica não conta como novo ensaio."
  ],
  "fixture_scope": [
    "adapters/docker/buildx_history.rs",
    "adapters/docker/compatibility.json",
    "adapters/docker/tests/history_finalization.rs"
  ]
}

**Estímulo:** Fixar versões, ambiente e dados; verificar todos os QS e budget aplicável. Separar erro de setup de violação do produto; conservar medições desfavoráveis.

**Predicado e observáveis:**
```json
{
  "predicate": "Hugit não instala/inicia Docker automaticamente.",
  "observable_paths": [
    "adapters/docker/buildx_history.rs",
    "adapters/docker/compatibility.json",
    "adapters/docker/tests/history_finalization.rs"
  ],
  "required_fields": [
    "assertion_id",
    "expected",
    "observed",
    "artifact_refs",
    "outcome",
    "verifier_identity"
  ],
  "verifier_kind": "structured_review_of_observables"
}
```

**Evidência:** resultado imutável por sujeito/assertion/célula/attempt, bytes referenciados e identidade do verificador. Uma suite verde sem esta assertion não fecha o critério.

**Oráculo SHA-256:** `e61b1dfbbbe4082e51aeae2dfe794be89a2bc4c6ecbb8e9681ce39fe7b0bbe87`

### A-HUG-040-QS02

Família `T-HUG-040-Q`; etapa `pre_promotion`; estado `specified_not_executed`.

**Dado:** {
  "source": "Sujeito A e versões do contrato fixados; teste de descoberta usa seu artefato de fonte inspecionado, não inventa runtime.",
  "protocols": [
    "P00@3.0",
    "P08@3.0",
    "P11@3.0"
  ],
  "fixture_inputs": [
    "Predecessores admitidos e SHA fixado, ou checkout a obter por HUG-001; descoberta produz sua fonte/fixture, consumidores exigem a fonte qualificada.",
    "Contratos P00, P08, P11 desta revisão; freeze runtime exigido somente para consumidores pós-HUG-059. Evidence histórica não conta como novo ensaio."
  ],
  "fixture_scope": [
    "adapters/docker/buildx_history.rs",
    "adapters/docker/compatibility.json",
    "adapters/docker/tests/history_finalization.rs"
  ]
}

**Estímulo:** Fixar versões, ambiente e dados; verificar todos os QS e budget aplicável. Separar erro de setup de violação do produto; conservar medições desfavoráveis.

**Predicado e observáveis:**
```json
{
  "predicate": "Timeout de finalizar registra partial, não sucesso inferido.",
  "observable_paths": [
    "adapters/docker/buildx_history.rs",
    "adapters/docker/compatibility.json",
    "adapters/docker/tests/history_finalization.rs"
  ],
  "required_fields": [
    "assertion_id",
    "expected",
    "observed",
    "artifact_refs",
    "outcome",
    "verifier_identity"
  ],
  "verifier_kind": "structured_review_of_observables"
}
```

**Evidência:** resultado imutável por sujeito/assertion/célula/attempt, bytes referenciados e identidade do verificador. Uma suite verde sem esta assertion não fecha o critério.

**Oráculo SHA-256:** `4bce2c13fce279f62392224099e3aecd212b59912bd0eeebcdaee3acf567da93`

### A-HUG-040-QS03

Família `T-HUG-040-Q`; etapa `pre_promotion`; estado `specified_not_executed`.

**Dado:** {
  "source": "Sujeito A e versões do contrato fixados; teste de descoberta usa seu artefato de fonte inspecionado, não inventa runtime.",
  "protocols": [
    "P00@3.0",
    "P08@3.0",
    "P11@3.0"
  ],
  "fixture_inputs": [
    "Predecessores admitidos e SHA fixado, ou checkout a obter por HUG-001; descoberta produz sua fonte/fixture, consumidores exigem a fonte qualificada.",
    "Contratos P00, P08, P11 desta revisão; freeze runtime exigido somente para consumidores pós-HUG-059. Evidence histórica não conta como novo ensaio."
  ],
  "fixture_scope": [
    "adapters/docker/buildx_history.rs",
    "adapters/docker/compatibility.json",
    "adapters/docker/tests/history_finalization.rs"
  ]
}

**Estímulo:** Fixar versões, ambiente e dados; verificar todos os QS e budget aplicável. Separar erro de setup de violação do produto; conservar medições desfavoráveis.

**Predicado e observáveis:**
```json
{
  "predicate": "Tool optional; ref/digest exatos, deadline finalization explícito; copy-on-request/quotas; native hit/miss medidos, sem tocar cache interno vivo.",
  "observable_paths": [
    "adapters/docker/buildx_history.rs",
    "adapters/docker/compatibility.json",
    "adapters/docker/tests/history_finalization.rs"
  ],
  "required_fields": [
    "assertion_id",
    "expected",
    "observed",
    "artifact_refs",
    "outcome",
    "verifier_identity"
  ],
  "verifier_kind": "structured_review_of_observables"
}
```

**Evidência:** resultado imutável por sujeito/assertion/célula/attempt, bytes referenciados e identidade do verificador. Uma suite verde sem esta assertion não fecha o critério.

**Oráculo SHA-256:** `78718165535a0ab2371a36f5c629de925215d2e937c7d35ee1701de91ae01c10`

### A-HUG-040-INV01

Família `T-HUG-040-N`; etapa `pre_promotion`; estado `specified_not_executed`.

**Dado:** {
  "source": "Sujeito A e versões do contrato fixados; teste de descoberta usa seu artefato de fonte inspecionado, não inventa runtime.",
  "protocols": [
    "P00@3.0",
    "P08@3.0",
    "P11@3.0"
  ],
  "fixture_inputs": [
    "Predecessores admitidos e SHA fixado, ou checkout a obter por HUG-001; descoberta produz sua fonte/fixture, consumidores exigem a fonte qualificada.",
    "Contratos P00, P08, P11 desta revisão; freeze runtime exigido somente para consumidores pós-HUG-059. Evidence histórica não conta como novo ensaio."
  ],
  "fixture_scope": [
    "adapters/docker/buildx_history.rs",
    "adapters/docker/compatibility.json",
    "adapters/docker/tests/history_finalization.rs"
  ]
}

**Estímulo:** Export antes do build finalizar, ref latest e CLI sem opção qualificada não recebem completed; URLs do report não são chamadas pelo importer. Executar o cenário negativo das assertions nomeadas, com estado/bytes antes e depois.

**Predicado e observáveis:**
```json
{
  "predicate": "latest não identifica execução concorrente.",
  "observable_paths": [
    "adapters/docker/buildx_history.rs",
    "adapters/docker/compatibility.json",
    "adapters/docker/tests/history_finalization.rs"
  ],
  "required_fields": [
    "assertion_id",
    "expected",
    "observed",
    "artifact_refs",
    "outcome",
    "verifier_identity"
  ],
  "verifier_kind": "runtime_or_artifact_predicate"
}
```

**Evidência:** resultado imutável por sujeito/assertion/célula/attempt, bytes referenciados e identidade do verificador. Uma suite verde sem esta assertion não fecha o critério.

**Oráculo SHA-256:** `c3f20bd112e8b4431bc0d40d503821918d9b130384566b8cb504f9bbd44f87a0`

### A-HUG-040-INV02

Família `T-HUG-040-N`; etapa `pre_promotion`; estado `specified_not_executed`.

**Dado:** {
  "source": "Sujeito A e versões do contrato fixados; teste de descoberta usa seu artefato de fonte inspecionado, não inventa runtime.",
  "protocols": [
    "P00@3.0",
    "P08@3.0",
    "P11@3.0"
  ],
  "fixture_inputs": [
    "Predecessores admitidos e SHA fixado, ou checkout a obter por HUG-001; descoberta produz sua fonte/fixture, consumidores exigem a fonte qualificada.",
    "Contratos P00, P08, P11 desta revisão; freeze runtime exigido somente para consumidores pós-HUG-059. Evidence histórica não conta como novo ensaio."
  ],
  "fixture_scope": [
    "adapters/docker/buildx_history.rs",
    "adapters/docker/compatibility.json",
    "adapters/docker/tests/history_finalization.rs"
  ]
}

**Estímulo:** Export antes do build finalizar, ref latest e CLI sem opção qualificada não recebem completed; URLs do report não são chamadas pelo importer. Executar o cenário negativo das assertions nomeadas, com estado/bytes antes e depois.

**Predicado e observáveis:**
```json
{
  "predicate": "Registro .dockerbuild não é imagem nem cache.",
  "observable_paths": [
    "adapters/docker/buildx_history.rs",
    "adapters/docker/compatibility.json",
    "adapters/docker/tests/history_finalization.rs"
  ],
  "required_fields": [
    "assertion_id",
    "expected",
    "observed",
    "artifact_refs",
    "outcome",
    "verifier_identity"
  ],
  "verifier_kind": "runtime_or_artifact_predicate"
}
```

**Evidência:** resultado imutável por sujeito/assertion/célula/attempt, bytes referenciados e identidade do verificador. Uma suite verde sem esta assertion não fecha o critério.

**Oráculo SHA-256:** `267c004da638f879a5394ec735dbaa9a06232b6ae98baaea4d21013d74ce23aa`

### A-HUG-040-INV03

Família `T-HUG-040-N`; etapa `pre_promotion`; estado `specified_not_executed`.

**Dado:** {
  "source": "Sujeito A e versões do contrato fixados; teste de descoberta usa seu artefato de fonte inspecionado, não inventa runtime.",
  "protocols": [
    "P00@3.0",
    "P08@3.0",
    "P11@3.0"
  ],
  "fixture_inputs": [
    "Predecessores admitidos e SHA fixado, ou checkout a obter por HUG-001; descoberta produz sua fonte/fixture, consumidores exigem a fonte qualificada.",
    "Contratos P00, P08, P11 desta revisão; freeze runtime exigido somente para consumidores pós-HUG-059. Evidence histórica não conta como novo ensaio."
  ],
  "fixture_scope": [
    "adapters/docker/buildx_history.rs",
    "adapters/docker/compatibility.json",
    "adapters/docker/tests/history_finalization.rs"
  ]
}

**Estímulo:** Export antes do build finalizar, ref latest e CLI sem opção qualificada não recebem completed; URLs do report não são chamadas pelo importer. Executar o cenário negativo das assertions nomeadas, com estado/bytes antes e depois.

**Predicado e observáveis:**
```json
{
  "predicate": "Integração opcional não vira dependência core.",
  "observable_paths": [
    "adapters/docker/buildx_history.rs",
    "adapters/docker/compatibility.json",
    "adapters/docker/tests/history_finalization.rs"
  ],
  "required_fields": [
    "assertion_id",
    "expected",
    "observed",
    "artifact_refs",
    "outcome",
    "verifier_identity"
  ],
  "verifier_kind": "runtime_or_artifact_predicate"
}
```

**Evidência:** resultado imutável por sujeito/assertion/célula/attempt, bytes referenciados e identidade do verificador. Uma suite verde sem esta assertion não fecha o critério.

**Oráculo SHA-256:** `b3f5f83075de7f25dc094a66054eeeecdd3caff2681197d09260657299cc5ad1`

### A-HUG-040-DOD01

Família `T-HUG-040-Q`; etapa `pre_promotion`; estado `specified_not_executed`.

**Dado:** {
  "source": "Sujeito A e versões do contrato fixados; teste de descoberta usa seu artefato de fonte inspecionado, não inventa runtime.",
  "protocols": [
    "P00@3.0",
    "P08@3.0",
    "P11@3.0"
  ],
  "fixture_inputs": [
    "Predecessores admitidos e SHA fixado, ou checkout a obter por HUG-001; descoberta produz sua fonte/fixture, consumidores exigem a fonte qualificada.",
    "Contratos P00, P08, P11 desta revisão; freeze runtime exigido somente para consumidores pós-HUG-059. Evidence histórica não conta como novo ensaio."
  ],
  "fixture_scope": [
    "adapters/docker/buildx_history.rs",
    "adapters/docker/compatibility.json",
    "adapters/docker/tests/history_finalization.rs"
  ]
}

**Estímulo:** Fixar versões, ambiente e dados; verificar todos os QS e budget aplicável. Separar erro de setup de violação do produto; conservar medições desfavoráveis.

**Predicado e observáveis:**
```json
{
  "predicate": "Fechar HUG-040 somente quando a implementação/artefato de 'Buildx: registro exato finalizado, não latest' satisfaz suas assertions e observáveis obrigatórios; uma suite verde sem resultado individual não fecha os axiomas.",
  "observable_paths": [
    "adapters/docker/buildx_history.rs",
    "adapters/docker/compatibility.json",
    "adapters/docker/tests/history_finalization.rs"
  ],
  "required_fields": [
    "assertion_id",
    "expected",
    "observed",
    "artifact_refs",
    "outcome",
    "verifier_identity"
  ],
  "verifier_kind": "structured_review_of_observables"
}
```

**Evidência:** resultado imutável por sujeito/assertion/célula/attempt, bytes referenciados e identidade do verificador. Uma suite verde sem esta assertion não fecha o critério.

**Oráculo SHA-256:** `9dc4deb9cedc8c3824b955c707715abfaedb34f6e5df72b74058c6971bc26199`

### A-HUG-040-DOD02

Família `T-HUG-040-Q`; etapa `pre_promotion`; estado `specified_not_executed`.

**Dado:** {
  "source": "Sujeito A e versões do contrato fixados; teste de descoberta usa seu artefato de fonte inspecionado, não inventa runtime.",
  "protocols": [
    "P00@3.0",
    "P08@3.0",
    "P11@3.0"
  ],
  "fixture_inputs": [
    "Predecessores admitidos e SHA fixado, ou checkout a obter por HUG-001; descoberta produz sua fonte/fixture, consumidores exigem a fonte qualificada.",
    "Contratos P00, P08, P11 desta revisão; freeze runtime exigido somente para consumidores pós-HUG-059. Evidence histórica não conta como novo ensaio."
  ],
  "fixture_scope": [
    "adapters/docker/buildx_history.rs",
    "adapters/docker/compatibility.json",
    "adapters/docker/tests/history_finalization.rs"
  ]
}

**Estímulo:** Fixar versões, ambiente e dados; verificar todos os QS e budget aplicável. Separar erro de setup de violação do produto; conservar medições desfavoráveis.

**Predicado e observáveis:**
```json
{
  "predicate": "Todos os outputs adapters/docker/buildx_history.rs, adapters/docker/compatibility.json, adapters/docker/tests/history_finalization.rs entregues nos write sets resolvidos; erros de P00, P08, P11 e mudanças de interface revisados pelo owner do contrato.",
  "observable_paths": [
    "adapters/docker/buildx_history.rs",
    "adapters/docker/compatibility.json",
    "adapters/docker/tests/history_finalization.rs"
  ],
  "required_fields": [
    "assertion_id",
    "expected",
    "observed",
    "artifact_refs",
    "outcome",
    "verifier_identity"
  ],
  "verifier_kind": "structured_review_of_observables"
}
```

**Evidência:** resultado imutável por sujeito/assertion/célula/attempt, bytes referenciados e identidade do verificador. Uma suite verde sem esta assertion não fecha o critério.

**Oráculo SHA-256:** `6bb467a33313a9df5a178acf14293763eeb7a65bf95c46c023fe047c36a6852d`

### A-HUG-040-DOD03

Família `T-HUG-040-R`; etapa `pre_promotion`; estado `specified_not_executed`.

**Dado:** {
  "source": "Sujeito A e versões do contrato fixados; teste de descoberta usa seu artefato de fonte inspecionado, não inventa runtime.",
  "protocols": [
    "P00@3.0",
    "P08@3.0",
    "P11@3.0"
  ],
  "fixture_inputs": [
    "Predecessores admitidos e SHA fixado, ou checkout a obter por HUG-001; descoberta produz sua fonte/fixture, consumidores exigem a fonte qualificada.",
    "Contratos P00, P08, P11 desta revisão; freeze runtime exigido somente para consumidores pós-HUG-059. Evidence histórica não conta como novo ensaio."
  ],
  "fixture_scope": [
    "adapters/docker/buildx_history.rs",
    "adapters/docker/compatibility.json",
    "adapters/docker/tests/history_finalization.rs"
  ]
}

**Estímulo:** Interromper nas fronteiras qualificadas; executar: Desabilitar adapter na versão incompatível, preservar registro parcial com ref e motivo; não cancelar ou limpar build do usuário por erro do recorder, salvo operação explicitamente autorizada.

**Predicado e observáveis:**
```json
{
  "predicate": "Recovery deste pacote ensaiado: Desabilitar adapter na versão incompatível, preservar registro parcial com ref e motivo; não cancelar ou limpar build do usuário por erro do recorder, salvo operação explicitamente autorizada.",
  "observable_paths": [
    "adapters/docker/buildx_history.rs",
    "adapters/docker/compatibility.json",
    "adapters/docker/tests/history_finalization.rs"
  ],
  "required_fields": [
    "assertion_id",
    "expected",
    "observed",
    "artifact_refs",
    "outcome",
    "verifier_identity"
  ],
  "verifier_kind": "structured_review_of_observables"
}
```

**Evidência:** resultado imutável por sujeito/assertion/célula/attempt, bytes referenciados e identidade do verificador. Uma suite verde sem esta assertion não fecha o critério.

**Oráculo SHA-256:** `3132df88345cecbff39736a9bdd075696b542b07018444f66fc7022c75683e1e`

### A-HUG-040-DOD04

Família `T-HUG-040-Q`; etapa `pre_promotion`; estado `specified_not_executed`.

**Dado:** {
  "source": "Sujeito A e versões do contrato fixados; teste de descoberta usa seu artefato de fonte inspecionado, não inventa runtime.",
  "protocols": [
    "P00@3.0",
    "P08@3.0",
    "P11@3.0"
  ],
  "fixture_inputs": [
    "Predecessores admitidos e SHA fixado, ou checkout a obter por HUG-001; descoberta produz sua fonte/fixture, consumidores exigem a fonte qualificada.",
    "Contratos P00, P08, P11 desta revisão; freeze runtime exigido somente para consumidores pós-HUG-059. Evidence histórica não conta como novo ensaio."
  ],
  "fixture_scope": [
    "adapters/docker/buildx_history.rs",
    "adapters/docker/compatibility.json",
    "adapters/docker/tests/history_finalization.rs"
  ]
}

**Estímulo:** Fixar versões, ambiente e dados; verificar todos os QS e budget aplicável. Separar erro de setup de violação do produto; conservar medições desfavoráveis.

**Predicado e observáveis:**
```json
{
  "predicate": "Qualificar HUG-040 sobre o sujeito A identificado por digest; anexar em B assertions verificadas e aprovação de revisor diferente do autor. Resultados não alteram A; revalidar no candidato final as obrigações atingidas. Nenhum axioma obrigatório dispensado.",
  "observable_paths": [
    "adapters/docker/buildx_history.rs",
    "adapters/docker/compatibility.json",
    "adapters/docker/tests/history_finalization.rs"
  ],
  "required_fields": [
    "assertion_id",
    "expected",
    "observed",
    "artifact_refs",
    "outcome",
    "verifier_identity"
  ],
  "verifier_kind": "structured_review_of_observables"
}
```

**Evidência:** resultado imutável por sujeito/assertion/célula/attempt, bytes referenciados e identidade do verificador. Uma suite verde sem esta assertion não fecha o critério.

**Oráculo SHA-256:** `2cd053a0b77dee5ead846003567c56c6eca566ec511aff6329315279519aaf0c`

## Famílias e células de aceitação

### T-HUG-040-N

Owner da família: HUG-040. Células: docker-linux-ext4. Etapa: pre_promotion.

**Preparo:** Predecessores admitidos e SHA fixado, ou checkout a obter por HUG-001; descoberta produz sua fonte/fixture, consumidores exigem a fonte qualificada. Contratos P00, P08, P11 desta revisão; freeze runtime exigido somente para consumidores pós-HUG-059. Evidence histórica não conta como novo ensaio.

**Execução:** Export antes do build finalizar, ref latest e CLI sem opção qualificada não recebem completed; URLs do report não são chamadas pelo importer. Executar o cenário negativo das assertions nomeadas, com estado/bytes antes e depois.

**Esperado:** Export antes do build finalizar, ref latest e CLI sem opção qualificada não recebem completed; URLs do report não são chamadas pelo importer.

**Artifact template:** `evidence/results/T-HUG-040-N/{subject_digest}/{cell_id}/{attempt_id}/result.json`

### T-HUG-040-P

Owner da família: HUG-040. Células: docker-linux-ext4. Etapa: pre_promotion.

**Preparo:** Predecessores admitidos e SHA fixado, ou checkout a obter por HUG-001; descoberta produz sua fonte/fixture, consumidores exigem a fonte qualificada. Contratos P00, P08, P11 desta revisão; freeze runtime exigido somente para consumidores pós-HUG-059. Evidence histórica não conta como novo ensaio.

**Execução:** Capturar ref exata da execução e aguardar finalização com API/--finalize qualificado ou marcar running/partial. Preservar .dockerbuild e metadados selecionados sob privacy/retention; imagem e cache são objetos separados. Sem Docker, reportar capability unavailable sem bloquear instalação/core.

**Esperado:** Dois builds concorrentes mantêm refs/goals próprios; falha finalizada e build verde geram registros consultáveis com digest e final state.

**Artifact template:** `evidence/results/T-HUG-040-P/{subject_digest}/{cell_id}/{attempt_id}/result.json`

### T-HUG-040-Q

Owner da família: HUG-040. Células: docker-linux-ext4. Etapa: pre_promotion.

**Preparo:** Predecessores admitidos e SHA fixado, ou checkout a obter por HUG-001; descoberta produz sua fonte/fixture, consumidores exigem a fonte qualificada. Contratos P00, P08, P11 desta revisão; freeze runtime exigido somente para consumidores pós-HUG-059. Evidence histórica não conta como novo ensaio.

**Execução:** Fixar versões, ambiente e dados; verificar todos os QS e budget aplicável. Separar erro de setup de violação do produto; conservar medições desfavoráveis.

**Esperado:** Hugit não instala/inicia Docker automaticamente.; Timeout de finalizar registra partial, não sucesso inferido.; Tool optional; ref/digest exatos, deadline finalization explícito; copy-on-request/quotas; native hit/miss medidos, sem tocar cache interno vivo.

**Artifact template:** `evidence/results/T-HUG-040-Q/{subject_digest}/{cell_id}/{attempt_id}/result.json`

### T-HUG-040-R

Owner da família: HUG-040. Células: docker-linux-ext4. Etapa: pre_promotion.

**Preparo:** Predecessores admitidos e SHA fixado, ou checkout a obter por HUG-001; descoberta produz sua fonte/fixture, consumidores exigem a fonte qualificada. Contratos P00, P08, P11 desta revisão; freeze runtime exigido somente para consumidores pós-HUG-059. Evidence histórica não conta como novo ensaio.

**Execução:** Interromper nas fronteiras qualificadas; executar: Desabilitar adapter na versão incompatível, preservar registro parcial com ref e motivo; não cancelar ou limpar build do usuário por erro do recorder, salvo operação explicitamente autorizada.

**Esperado:** Desabilitar adapter na versão incompatível, preservar registro parcial com ref e motivo; não cancelar ou limpar build do usuário por erro do recorder, salvo operação explicitamente autorizada.

**Artifact template:** `evidence/results/T-HUG-040-R/{subject_digest}/{cell_id}/{attempt_id}/result.json`

**Fechamento:** cinco grupos de axiomas demonstrados por assertions verificadas no candidato integrado, sem waiver. HUG-056 fecha somente após C e pós-publicação; demais estados seguem P00/P12.
